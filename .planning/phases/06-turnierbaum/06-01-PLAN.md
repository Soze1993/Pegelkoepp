---
phase: 06-turnierbaum
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - Claude/server/game-types/kegler-des-abends.test.js
autonomous: true
requirements:
  - TOURNAMENT-01
  - TOURNAMENT-02
must_haves:
  truths:
    - "Test suite covers DE bracket initState for 4, 8, and 12-player brackets"
    - "Test suite covers applyThrow accumulation and match resolution"
    - "Test suite covers Grand Final 2-throw logic and tiebreak detection"
    - "Test suite covers immutability and determinism"
    - "All tests fail before new engine is written (RED phase)"
  artifacts:
    - path: "Claude/server/game-types/kegler-des-abends.test.js"
      provides: "Full DE engine test suite — Wave 0 contract"
      contains: "state.bracket"
  key_links:
    - from: "kegler-des-abends.test.js"
      to: "kegler-des-abends.js"
      via: "require('./kegler-des-abends')"
      pattern: "require\\('./kegler-des-abends'\\)"
---

<objective>
Rewrite `kegler-des-abends.test.js` with a complete Double-Elimination engine test suite BEFORE the engine is implemented. Every test will fail against the current engine (RED), making the contract explicit for the Wave 1 implementer.

Purpose: Establish the exact behavioral contract the new DE engine must satisfy. Tests become the specification.
Output: `kegler-des-abends.test.js` — fully rewritten, ~14 tests covering the new DE engine interface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/phases/06-turnierbaum/06-CONTEXT.md
@Claude/.planning/phases/06-turnierbaum/06-VALIDATION.md

<interfaces>
<!-- Current test file interface — being REPLACED, not extended -->
<!-- Source: Claude/server/game-types/kegler-des-abends.test.js (current) -->

Current file uses state.spieler, state.matches, state.mid, state.wRound — ALL of which go away.
New tests must use state.bracket[] (flat slot array).

<!-- Analog test structure: vier-gewinnt.test.js pattern (exact copy for C1–C5) -->
'use strict';
const { test } = require('node:test');
const assert = require('node:assert/strict');
const kda = require('./kegler-des-abends');

<!-- New DE bracket state shape (from RESEARCH.md Pattern 1 + CONTEXT.md D-01..D-08) -->
// initState(players4, { seed: 'test' }) must return:
{
  bracket: [            // flat array of match slot objects
    { id: 'W-R1-1', bracket: 'W', round: 1,
      p1: { id, name, emoji }, p2: { id, name, emoji },
      throws: [], winner: null, loser: null, done: false, isBye: false,
      tiebreak: false, advancesWinnerTo: string|null, advancesLoserTo: string|null,
      throwsRequired: 2 },
    // ... more slots
    { id: 'GF', bracket: 'GF', round: 1,
      throwsRequired: 4,   // Grand Final: 2 throws per player
      advancesWinnerTo: null, advancesLoserTo: null }
  ],
  done: false,
  gewinner: null,
  _seed: string|null
}

<!-- applyThrow new signature (CONTEXT.md D-08) -->
// applyThrow(state, player_id, value)
//   player_id: actual player row ID (integer)
//   value: pin count 0–9

<!-- 4-player bracket minimum structure (RESEARCH.md Open Question 1) -->
// Minimum 5 slots: W-R1-1, W-R1-2, W-Final, L-Final, GF

<!-- 8-player bracket: 17 slots max -->
// W-R1 (4) + W-R2 (2) + W-Semi (1) + W-Final (1) + L-R1 (2) + L-R2 (2) + L-R3 (1) + L-R4 (1) + L-R5 (1) + GF (1) = 16 + 1 = 17

<!-- reconstructState gap (PATTERNS.md) -->
// reconstructState in games.js calls: gameModule.initState(players) with NO config
// The new engine must handle initState(players) with no config — uses random shuffle
// This is acceptable because crash-recovery of NEW KDA games happens from bracket state
// stored in DB after first throw; OLD games are finished and never enter reconstructState
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Rewrite test suite — contract tests (C1–C5) adapted for DE bracket shape</name>
  <files>Claude/server/game-types/kegler-des-abends.test.js</files>
  <read_first>
    - Claude/server/game-types/kegler-des-abends.test.js (current file — understand what is being replaced)
    - Claude/server/game-types/vier-gewinnt.test.js (exact structural analog for C1–C5 pattern)
  </read_first>
  <behavior>
    - C1: kda exports { id: string, name: string, initState: function, applyThrow: function, isFinished: function, getFinalResults: function }
    - C2: kda.isFinished(kda.initState(players4, { seed: 'test' })) returns false
    - C3: applyThrow(state, match.p1.id, 7) does not mutate input state — deepEqual snapshot matches after call
    - C4: Two independent playOutTournament(initState(players4, { seed: 'det' })) calls produce deepEqual final states — p1 always wins (value 9), p2 always loses (value 0)
    - C5: getFinalResults on completed tournament returns Array with exactly 1 entry where winner: true
  </behavior>
  <action>
    OVERWRITE the entire file. Start with `'use strict';`, then `require('node:test')` and `require('node:assert/strict')`, then `require('./kegler-des-abends')`.

    Define player fixtures at the top (not inside tests): players4 (ids 1–4), players8 (ids 1–8), players6 (ids 1–6).

    Write C1–C5 contract tests adapted for the new bracket shape:

    C3 must use `state.bracket.find(m => !m.done && !m.isBye && m.p1 && m.p2)` to find the first active match, then call `kda.applyThrow(state, match.p1.id, 7)`.

    C4 helper `playOutTournament(state)` must use `s.bracket.find(m => !m.done && !m.isBye && m.p1 && m.p2)` to find pending match and submit p1 throws (value 9) then p2 throws (value 0) per match, up to 200 iterations.

    These tests MUST FAIL against the current engine because the current engine has no `state.bracket`. That is the intended RED state.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/game-types/kegler-des-abends.test.js 2>&1 | head -30</automated>
  </verify>
  <done>C1–C5 tests exist in file. Running against current engine: C2 fails (state.bracket not found), C3 fails (cannot find active match), C4 fails, C5 fails. C1 passes (module shape is preserved). Failure output confirms RED state.</done>
</task>

<task type="tdd">
  <name>Task 2: Write KDA-specific DE bracket tests (KDA1–KDA9)</name>
  <files>Claude/server/game-types/kegler-des-abends.test.js</files>
  <read_first>
    - Claude/server/game-types/kegler-des-abends.test.js (after Task 1 — append to existing file)
    - Claude/.planning/phases/06-turnierbaum/06-VALIDATION.md (per-task verification map for required coverage)
    - Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md (KDA1 test example on lines 261–275)
  </read_first>
  <behavior>
    - KDA1: initState(players4, {seed:'test'}) returns { bracket: Array, done: false, gewinner: null }; bracket.length >= 5; every slot has keys: id, p1, p2, throws, done, isBye, bracket, advancesWinnerTo, advancesLoserTo
    - KDA2: initState(players8, {seed:'test'}) returns bracket with GF slot (bracket==='GF'); all non-bye W-R1 slots have both p1 and p2 populated
    - KDA3: initState(players6, {seed:'test'}) returns bracket where exactly 2 slots have isBye===true; bye slots have done===true and winner set to the seeded player; no p2 on bye slots
    - KDA4: applyThrow accumulates — after one throw (p1 throws 7), match.throws.length === 1, match.done === false
    - KDA5: applyThrow resolves match after 2 throws — player with higher pin count is match.winner, match.done === true, downstream slot (advancesWinnerTo) has winner populated as p1 or p2
    - KDA6: Tie detection — after two equal throws (both 5), match.tiebreak === true, match.done === false, match.throwsRequired === 4
    - KDA7: Grand Final — GF slot has throwsRequired === 4; after 2 throws for p1 and 1 throw for p2, GF.done === false; after all 4 throws with p1 scoring more, GF.done === true, state.gewinner.id === p1.id, state.done === true
    - KDA8: applyThrow ignores throw for non-active match — if player is not in any active match, state returned is deepEqual to input (stale throw guard per D-08 security requirement)
    - KDA9: Player count validation — initState throws Error for players.length < 4; initState throws Error for players.length > 12
  </behavior>
  <action>
    APPEND tests KDA1–KDA9 to the file written in Task 1. Do not replace Task 1 content.

    KDA7 Grand Final test: use a full 4-player playthrough helper that gets two players to the GF, then manually submit 4 throws (p1: 9,8 and p2: 7,6) using sequential applyThrow calls. Assert state.done === true and state.gewinner.id === the p1 finalist.

    KDA9 player count test: use assert.throws(() => kda.initState([{id:1,name:'A',emoji:'A'},{id:2,name:'B',emoji:'B'},{id:3,name:'C',emoji:'C'}], {}), /4.+12|4–12|requires 4/i) for 3 players. Use a 13-player array similarly for the upper bound.

    These tests MUST FAIL against the current engine (KDA1 fails — no bracket field; KDA9 fails — no validation). Confirm RED output.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && npm run test:games 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run test:games output shows: C1 passes (module shape), remaining C2–C5 and KDA1–KDA9 tests fail with errors referencing "state.bracket", "Cannot read properties of undefined", or similar. This is the expected RED state confirming the test suite specifies the new DE engine interface. Test file must not contain any `describe` blocks — only flat `test()` calls matching the node:test pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → engine module | Test file calls engine functions directly; no HTTP, no DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W0-01 | Tampering | Test fixtures | accept | Test player fixtures use simple integer ids — no injection risk in a test-only file |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — node:test and node:assert/strict are built-in |
</threat_model>

<verification>
Run `npm run test:games` — expect C1 to pass (module shape unchanged), all others to fail with errors about missing `state.bracket`. This confirms the RED state.

The failing test output is the intended deliverable of Wave 0. Wave 1 will make all these tests green.
</verification>

<success_criteria>
- `kegler-des-abends.test.js` is fully rewritten with 14 tests (C1–C5, KDA1–KDA9)
- No test references `state.spieler`, `state.matches`, `state.mid`, or `state.wRound`
- C1 passes against current engine
- C2–C5 and KDA1–KDA9 all fail against current engine (RED state confirmed)
- `npm run test:games` exits non-zero with ≥ 13 failures
</success_criteria>

<output>
Create `Claude/.planning/phases/06-turnierbaum/06-01-SUMMARY.md` when done.
</output>
