---
phase: 06-turnierbaum
plan: 04
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - Claude/public/tv.js
autonomous: true
requirements:
  - TOURNAMENT-03
must_haves:
  truths:
    - "TV shows the full DE bracket tree when a new KDA game is active"
    - "TV updates the bracket after every throw without page reload"
    - "In-progress score shown immediately — before match is complete ('7 ⚫')"
    - "KDA branch fires BEFORE the state.players null-guard (Pitfall 5)"
    - "All player names and scores use textContent — no innerHTML with DB data"
    - "TV does not break for any existing game types (no regression)"
  artifacts:
    - path: "Claude/public/tv.js"
      provides: "KDA bracket branch in renderGame + renderKDABracket TV renderer"
  key_links:
    - from: "tv.js renderGame"
      to: "renderKDABracket"
      via: "if (state && state.bracket) branch BEFORE state.players guard"
      pattern: "state\\.bracket"
    - from: "socket throw:applied"
      to: "renderGame"
      via: "existing socket.on handler at line 20"
      pattern: "throw:applied"
---

<objective>
Add a KDA-specific branch to `tv.js renderGame` and implement `renderKDABracket` for the TV — a full-screen landscape bracket tree with large slots, no scrolling, live in-progress scores. The branch must be inserted BEFORE the existing `state.players` null-guard.

Purpose: Deliver TOURNAMENT-03 — bracket progress displayed live on TV via existing Socket.io events.
Output: Modified `tv.js` with `renderKDABracket` function (~80 lines) and one-line change to `renderGame`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/phases/06-turnierbaum/06-CONTEXT.md
@Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md
@Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md

<interfaces>
<!-- tv.js current renderGame — THE CRITICAL CHANGE LOCATION -->
<!-- Source: Claude/public/tv.js lines 37–82 -->
function renderGame(state) {
  if (!state || !state.players) return;  // <-- CURRENT: this guard blocks KDA new format
  idleEl.style.display = 'none';
  gameEl.classList.add('active');
  playerListEl.replaceChildren();
  // ... player list rendering ...
}

<!-- REQUIRED CHANGE — KDA branch BEFORE the null-guard (RESEARCH.md Pitfall 5, Pattern 4) -->
function renderGame(state) {
  // KDA new format: bracket tree display — MUST come before state.players guard
  if (state && state.bracket) { renderKDABracket(state); return; }
  if (!state || !state.players) return;
  // ... all existing code unchanged below ...
}

<!-- Existing DOM elements in tv.js (lines 1–8) -->
const connDot      = document.getElementById('connDot');
const idleEl       = document.getElementById('idle');
const gameEl       = document.getElementById('game');
const playerListEl = document.getElementById('playerList');
const lastWinnerEl = document.getElementById('lastWinnerText');

<!-- Socket events that drive renderGame (tv.js lines 20–22) — NO CHANGE NEEDED -->
socket.on('throw:applied', ({ state }) => renderGame(state));
socket.on('undo:applied',  ({ state }) => renderGame(state));
socket.on('game:started',  ({ state, gameId }) => { socket.emit('join', gameId); renderGame(state); });

<!-- DOM construction pattern from existing tv.js (lines 43–81) -->
playerListEl.replaceChildren();
const li = document.createElement('li');
li.className = 'player-row';
const nameEl = document.createElement('span');
nameEl.className = 'player-name';
nameEl.textContent = (player.emoji != null ? player.emoji : '') + ' ' + player.name;  // textContent only
playerListEl.appendChild(li);

<!-- gameEl.replaceChildren pattern (tv.js line 43) -->
playerListEl.replaceChildren();  // or: gameEl.replaceChildren(container)

<!-- TV bracket slot sizes from UI-SPEC (06-UI-SPEC.md lines 175–183) -->
// 4-player: 200px × 80px
// 8-player: 160px × 72px  (most common)
// 12-player: 140px × 64px (tightest fit)
// Determine from bracket.filter(m => m.bracket === 'W' && m.round === 1).length

<!-- TV color tokens (all from existing :root in index.html) -->
var(--bg): #0f0f14    — TV background
var(--card): #22222e  — slot background
var(--ac): #e8b84b    — active slot border, in-progress score, GF border
var(--grn): #4caf7d   — winner score
var(--red): #e05252   — loser score (60% opacity)
var(--brd): #2e2e3e   — default border, connector lines
var(--mut): #8884a0   — round labels, pending player placeholder

<!-- TV section labels from copywriting contract (06-UI-SPEC.md lines 340–368) -->
"Winner Bracket" / "Loser Bracket" (section titles, Bebas Neue 36px)
Round labels: "W · Runde 1", "W · Runde 2", "W · Halbfinale", "W · Finale",
              "L · Runde 1", "L · Runde 2", "L · Halbfinale", "L · Finale", "Großes Finale"

<!-- TV player row layout (06-UI-SPEC.md lines 186–193) -->
// [emoji + name (22px DM Sans 600)]    [score (28px Bebas Neue)]
// In-progress score: color var(--ac), suffix ' ⚫'
// Winner score: color var(--grn)
// Loser score: color var(--red), opacity 0.6
// Pending score: '—', color var(--mut)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add KDA branch to renderGame (one-line guard fix) and implement renderKDABracket</name>
  <files>Claude/public/tv.js</files>
  <read_first>
    - Claude/public/tv.js (full file — 100 lines; understand current renderGame structure before editing)
    - Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md lines 167–199 (TV bracket tree spec — container, slot sizes, player row layout, connector lines)
    - Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md lines 383–449 (renderKDABracket skeleton, gameEl.replaceChildren pattern)
    - Claude/.planning/phases/06-turnierbaum/06-RESEARCH.md lines 330–354 (Pattern 4 — TV renderGame guard fix, renderKDABracket structure)
  </read_first>
  <behavior>
    - renderGame(state) with { bracket: [...], done: false } calls renderKDABracket and returns without reaching the state.players guard
    - renderGame(state) with { players: [...] } continues to existing logic unchanged
    - renderGame(null) returns without error (existing guard still works)
    - renderKDABracket sets idleEl.style.display = 'none' and adds 'active' class to gameEl
    - renderKDABracket builds bracket container with textContent for all player names and scores
    - Active match slot gets border 2px solid var(--ac) and box-shadow '0 0 16px #e8b84b33'
    - In-progress score shows integer + ' ⚫' (D-14 pattern)
  </behavior>
  <action>
    Change 1 — `renderGame` guard (line 38): Replace `function renderGame(state) {` + `if (!state || !state.players) return;` with the two-line form: `function renderGame(state) {` then `if (state && state.bracket) { renderKDABracket(state); return; }` then `if (!state || !state.players) return;`. All code below the guard remains UNCHANGED.

    Change 2 — Add `renderKDABracket(state)` function after `getScore` (after line 99):

    The function body:
    1. `idleEl.style.display = 'none'; gameEl.classList.add('active');`
    2. Determine slot size from W-R1 match count: `const wR1Count = state.bracket.filter(m => m.bracket === 'W' && m.round === 1).length;` Then: slotWidth = wR1Count <= 2 ? 200 : wR1Count <= 4 ? 160 : 140; slotHeight = wR1Count <= 2 ? 80 : wR1Count <= 4 ? 72 : 64.
    3. Create outer container: `const container = document.createElement('div'); container.className = 'kda-tv-bracket'; container.style.cssText = 'width:100vw;height:100vh;overflow:hidden;background:var(--bg);padding:32px;box-sizing:border-box;display:grid;gap:24px';`
    4. Build W bracket section: create `div` with label textContent "Winner Bracket" (Bebas Neue 36px, color var(--ac)). Get unique W-bracket rounds: `const wRounds = [...new Set(state.bracket.filter(m => m.bracket === 'W').map(m => m.round))].sort()`. For each round: create `div.tv-bracket-col` with `style="display:flex;flex-direction:column;justify-content:space-around"`. Add round label above: textContent = round label from copywriting contract. For each slot in this round: call `buildTVSlotEl(slot, slotWidth, slotHeight)` and append.
    5. Build L bracket section: same pattern for `m.bracket === 'L'` slots.
    6. Build GF column: find GF slot, create column with label "Großes Finale" (16px DM Sans 600, var(--mut)). Build slot using `buildTVSlotEl`.
    7. `gameEl.replaceChildren(container);`

    Add private function `buildTVSlotEl(slot, w, h)`:
    - Create `div.tv-bracket-slot` with style: width, height, background var(--card), border-radius 8px, display flex, flex-direction column, justify-content space-around, padding 8px 12px, box-sizing border-box.
    - Border: if active (`!slot.done && !slot.isBye && slot.p1 && slot.p2`): border `2px solid var(--ac)`, box-shadow `0 0 16px #e8b84b33`. Else if `slot.isBye`: border `1px dashed var(--brd)`, opacity 0.45. Else: border `1px solid var(--brd)`.
    - For each player [slot.p1, slot.p2]: create a row div with display flex, justify-content space-between, align-items center.
      - Name span: textContent = p ? (p.emoji + ' ' + p.name) : '—'; font-size 22px; font-weight 600; color = p ? 'var(--txt)' : 'var(--mut)'. Use `textContent` — NOT innerHTML.
      - Score span: compute score from slot.throws for this player. If no throws for this player: textContent '—', color var(--mut). If throws exist but match not done: textContent = String(sum) + ' ⚫', color var(--ac). If match done and this player is winner: textContent = String(sum), color var(--grn). If match done and this player is loser: textContent = String(sum), color var(--red), opacity 0.6.
      - Score span font: font-size 28px, font-family var(--fh) (Bebas Neue).
    - If `slot.isBye` and slot.winner: set first row name to slot.winner.emoji + ' ' + slot.winner.name (textContent); second row name to '—'.
    - Return element.

    Add round label mapping function `kdaTVRoundLabel(bracket, round, totalRounds)`:
    - bracket 'W': round === totalRounds ? 'W · Finale' : round === totalRounds-1 ? 'W · Halbfinale' : `W · Runde ${round}`
    - bracket 'L': round === totalRounds ? 'L · Finale' : round === totalRounds-1 ? 'L · Halbfinale' : `L · Runde ${round}`
    - Use textContent to set label text.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node -e "const fs=require('fs');const t=fs.readFileSync('public/tv.js','utf8');const lines=t.split('\n');const renderGameLine=lines.findIndex(l=>l.includes('function renderGame'));console.log('kda branch before guard:',lines[renderGameLine+1].includes('state.bracket'));console.log('players guard still present:',t.includes('if (!state || !state.players) return'));console.log('renderKDABracket exists:',t.includes('function renderKDABracket'));console.log('buildTVSlotEl exists:',t.includes('function buildTVSlotEl'));console.log('no innerHTML player:',!t.match(/innerHTML.*\\.name/));"</automated>
  </verify>
  <done>
    All five checks print true. The KDA branch (`state.bracket` check) is on the line immediately after `function renderGame(state) {`. The existing `state.players` guard follows on the next line. `renderKDABracket` and `buildTVSlotEl` functions exist. No `innerHTML` with `.name` property access.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket state → TV DOM | state.bracket[].p1/p2 player names arrive via Socket.io; rendered into TV DOM |
| TV DOM | Display-only; no user input, no form submissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | XSS | buildTVSlotEl — player names and scores in TV DOM | mitigate | All name and score rendering uses `textContent` throughout — no `innerHTML` with DB strings. Verified by automated check. Consistent with existing tv.js pattern (line 52). |
| T-06-04-02 | Tampering | renderGame null-guard bypass (Pitfall 5) | mitigate | KDA branch added before guard: `if (state && state.bracket) { renderKDABracket(state); return; }` — TV cannot reach stale null-guard path for new KDA games |
| T-06-04-03 | Tampering | Existing game types regression | accept | All existing code below the null-guard is unchanged. KDA branch returns early before any existing rendering code executes. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
Manual verification (VALIDATION.md):
1. Start a new KDA game on tablet — TV immediately shows full bracket tree (not idle screen).
2. Enter P1 throw on tablet — TV shows "X ⚫" in the active match slot before P2 throws (per D-14).
3. Complete a match — TV updates: winner row green, loser row red, next active slot gets amber glow.
4. Start any non-KDA game — TV renders existing player-list view without regression.

Automated: verify checks from Task 1 all print true.

Full suite: `npm test` exits 0 (server-side tests unaffected by tv.js change).
</verification>

<success_criteria>
- `renderGame` has KDA branch `if (state && state.bracket)` on first line BEFORE `state.players` guard
- `renderKDABracket` builds W bracket, L bracket, and GF column DOM entirely via `createElement` + `textContent`
- Active slots styled with amber border + glow per D-13
- In-progress scores show integer + ' ⚫' in var(--ac) color per D-14
- Slot size adapts based on W-R1 match count (200px for 4-player, 160px for 8-player, 140px for 12-player)
- All existing non-KDA game types render identically on TV (no regression)
- `npm test` exits 0
</success_criteria>

<output>
Create `Claude/.planning/phases/06-turnierbaum/06-04-SUMMARY.md` when done.
</output>
