---
phase: 06-turnierbaum
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - Claude/server/game-types/kegler-des-abends.js
autonomous: true
requirements:
  - TOURNAMENT-01
  - TOURNAMENT-02
must_haves:
  truths:
    - "initState generates a full predetermined DE bracket for 4, 6, 8, and 12 players"
    - "Byes are auto-resolved in initState — bye slots are done=true at tournament start"
    - "applyThrow accumulates pin counts and resolves matches when all required throws are in"
    - "Grand Final requires 4 throws total; tiebreak adds 2 throws per tie round"
    - "Stray throws for non-active matches are ignored (security guard)"
    - "All npm run test:games tests pass GREEN"
  artifacts:
    - path: "Claude/server/game-types/kegler-des-abends.js"
      provides: "New DE bracket engine — replaces old round-by-round engine"
      exports: "{ id: 'kda', name: 'Kegler des Abends', initState, applyThrow, isFinished, getFinalResults }"
  key_links:
    - from: "Claude/server/routes/games.js"
      to: "Claude/server/game-types/kegler-des-abends.js"
      via: "gameTypes['kda'].applyThrow(state, player_id, value)"
      pattern: "applyThrow"
    - from: "kegler-des-abends.js"
      to: "reconstructState"
      via: "initState(players) called without config on crash recovery"
      pattern: "initState"
---

<objective>
Replace the existing `kegler-des-abends.js` engine with a true Double-Elimination bracket engine. All match slots are predetermined at tournament start. Pin-count throws accumulate per match; winner is determined automatically. Byes are resolved immediately in `initState`.

Purpose: Implement the Wave 0 test contract. All 14 tests in `kegler-des-abends.test.js` must pass GREEN.
Output: `kegler-des-abends.js` — fully rewritten DE engine (~200 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/phases/06-turnierbaum/06-CONTEXT.md
@Claude/.planning/phases/06-turnierbaum/06-RESEARCH.md
@Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md

<interfaces>
<!-- Current engine interface — being REPLACED -->
<!-- Source: Claude/server/game-types/kegler-des-abends.js lines 37–148 -->

module.exports = {
  id: 'kda',
  name: 'Kegler des Abends',
  initState(players, config = {}) { ... },  // currently builds state.matches[] (round-by-round)
  applyThrow(state, matchId, winnerId) { ... },  // matchId = "playerId", winnerId = "value"
  isFinished(state) { return state.done; },
  getFinalResults(state) { ... }
}

<!-- seededShuffle (MUST be preserved verbatim — crash recovery dependency) -->
<!-- Source: Claude/server/game-types/kegler-des-abends.js lines 3–25 -->
function seededShuffle(arr, seed) {
  const a = arr.slice();
  let s = 0;
  for (let i = 0; i < String(seed).length; i++) {
    s = (s * 31 + String(seed).charCodeAt(i)) >>> 0;
  }
  if (s === 0) s = 1;
  function next() {
    s = (s * 1664525 + 1013904223) >>> 0;
    return s / 0x100000000;
  }
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(next() * (i + 1));
    const t = a[i]; a[i] = a[j]; a[j] = t;
  }
  return a;
}

<!-- vier-gewinnt.js module skeleton analog -->
<!-- Source: Claude/server/game-types/vier-gewinnt.js lines 23–93 -->
'use strict';
module.exports = {
  id: 'viergewinnt', name: 'Vier Gewinnt',
  initState(players) { return { ..., done: false, winner: null }; },
  applyThrow(state, playerId, value, meta = {}) {
    const s = JSON.parse(JSON.stringify(state));
    if (s.done) return s;
    // ... logic ...
    return s;
  },
  isFinished(state) { return state.done; },
  getFinalResults(state) {
    // returns [{playerId, score, winner: bool}, ...]
  }
};

<!-- reconstructState in games.js calls initState WITHOUT config (line 245) -->
// function reconstructState(game) {
//   ...
//   let state = gameModule.initState(players);  // <-- no config.seed passed
//   for (const t of throws) {
//     state = gameModule.applyThrow(state, t.player_id, t.value, parsedMeta);
//   }
// }
// IMPLICATION: initState must accept (players) with no config; uses random shuffle.
// Crash recovery for new KDA games is safe because: (a) old KDA games are finished
// (status='finished') and never pass through rebuildActiveGames; (b) new KDA games
// store their throws with actual player_id values, so replay works regardless of
// bracket slot ordering — applyThrow finds the active match by player_id lookup.

<!-- DE bracket slot structure (CONTEXT.md D-01, RESEARCH.md Pattern 1) -->
{
  id: 'W-R1-1',          // unique string
  round: 1,              // integer
  bracket: 'W',          // 'W' | 'L' | 'GF'
  p1: { id, name, emoji } | null,
  p2: { id, name, emoji } | null,
  throws: [],            // [{ playerId, throwIndex, value }]
  winner: null,          // player object when resolved
  loser: null,           // player object when resolved
  done: false,
  isBye: false,
  tiebreak: false,
  advancesWinnerTo: 'W-R2-1',  // slot id string, or null for GF winner
  advancesLoserTo:  'L-R1-1',  // slot id string, or null for GF loser
  throwsRequired: 2             // 2 for standard (1/player), 4 for GF (2/player)
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement initState — bracket generation with byes and predetermined routing</name>
  <files>Claude/server/game-types/kegler-des-abends.js</files>
  <read_first>
    - Claude/server/game-types/kegler-des-abends.js (current file — copy seededShuffle and shuffle verbatim)
    - Claude/server/game-types/vier-gewinnt.js lines 23–39 (module declaration pattern)
    - Claude/.planning/phases/06-turnierbaum/06-RESEARCH.md lines 200–265 (DE bracket slot generation, 4-player and 8-player examples)
    - Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md lines 81–115 (initState seed pattern, bracket slot structure)
    - Claude/server/game-types/kegler-des-abends.test.js (Wave 0 tests — these define the behavioral contract)
  </read_first>
  <behavior>
    - initState(players4, {seed:'test'}) returns state where state.bracket is an Array of >= 5 slots, state.done === false, state.gewinner === null, state._seed === 'test'
    - initState(players6, {seed:'x'}) returns state where state.bracket has exactly 2 slots with isBye===true, those slots have done===true and winner set to the bye-receiving player
    - initState(players8, {seed:'x'}) returns state where GF slot exists (bracket==='GF') and all W-R1 slots have p1 and p2 populated
    - initState([p1,p2,p3]) throws Error matching /4.+12|4–12|requires 4/i
    - initState with 13 players throws Error matching same pattern
    - initState(players4) with no config arg does not throw (uses random shuffle)
  </behavior>
  <action>
    OVERWRITE the entire file. Open with `'use strict';`.

    Copy `seededShuffle` (lines 3–25 of current file) and `shuffle` (lines 27–35) VERBATIM — do not modify a single character.

    Write a private `buildBracket(seededPlayers)` function that:
    1. Determines bracket size: nextPow2 = smallest power of 2 >= players.length (minimum 4).
    2. Pads the seeded player array to nextPow2 slots — extra slots are null (bye positions).
    3. Generates all W-bracket slots (W-R1-N, W-R2-N, W-Semi, W-Final) with predetermined advancesWinnerTo and advancesLoserTo routing. For power-of-2 brackets, the standard seeding pairs are: seed 1 vs seed nextPow2, seed 2 vs seed (nextPow2-1), etc. (top half of bracket vs bottom half).
    4. Generates all L-bracket slots. The L bracket structure for N=4: one slot (L-Final), receives losers of W-R1-1 and W-R1-2. For N=8: L-R1 (2 slots, receive paired losers from W-R1), L-R2 (2 slots, L-R1 winners vs losers from W-R2), L-R3 (1 slot), L-R4 (1 slot, vs loser of W-Semi), L-R5 (1 slot, vs loser of W-Final). Use standard DE routing tables.
    5. Appends a single GF slot: { id: 'GF', bracket: 'GF', round: 1, p1: null, p2: null, throws: [], winner: null, loser: null, done: false, isBye: false, tiebreak: false, advancesWinnerTo: null, advancesLoserTo: null, throwsRequired: 4 }.
    6. Returns the flat bracket array.

    Write a private `resolveByeSlots(bracket)` function: iterates over all slots where isBye===true, sets slot.done=true, sets slot.winner to the one non-null player, then calls advancePlayer to populate the downstream slot.

    Write a private `advancePlayer(bracket, slotId, player)` function: finds slot by id, sets p1 if null else p2.

    In `module.exports.initState(players, config = {})`:
    1. Validate: if players.length < 4 || players.length > 12, throw new Error('KDA requires 4–12 players (per D-12)').
    2. Extract seed: `const seed = (config && config.seed != null) ? String(config.seed) : null`.
    3. Build player objects: `const pl = (seed !== null ? seededShuffle : shuffle)(players.map(p => ({ id: p.id, name: p.name, emoji: p.emoji })), seed)`.
    4. Call `buildBracket(pl)` to get the bracket array.
    5. Call `resolveByeSlots(bracket)` to auto-advance bye slots.
    6. Return `{ bracket, done: false, gewinner: null, _seed: seed }`.

    Keep `isFinished(state) { return state.done; }` exactly as in vier-gewinnt.js pattern.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/game-types/kegler-des-abends.test.js 2>&1 | grep -E "^(ok|not ok)" | head -20</automated>
  </verify>
  <done>
    Tests KDA1 (4-player bracket shape), KDA2 (8-player GF slot exists), KDA3 (6-player byes), KDA9 (player count validation) all pass. C1 (module shape) and C2 (isFinished false) pass. Tests C3, C4, C5, KDA4–KDA8 remain failing (applyThrow not yet implemented).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement applyThrow and getFinalResults — pin accumulation, match resolution, bracket advancement</name>
  <files>Claude/server/game-types/kegler-des-abends.js</files>
  <read_first>
    - Claude/server/game-types/kegler-des-abends.js (after Task 1 — current state of the file)
    - Claude/.planning/phases/06-turnierbaum/06-RESEARCH.md lines 269–305 (applyThrow Pattern 2, tiebreak handling)
    - Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md lines 138–180 (applyThrow accumulation skeleton)
    - Claude/server/game-types/kegler-des-abends.test.js (KDA4–KDA8 tests — behavioral contract)
  </read_first>
  <behavior>
    - applyThrow(state, player_id, value): after one throw (p1 throws 7), match.throws.length === 1, match.done === false
    - applyThrow resolves: after p1 throws 9 and p2 throws 0, match.done === true, match.winner.id === p1.id, match.loser.id === p2.id; the slot referenced by match.advancesWinnerTo now has winner as p1 or p2
    - Tie: after p1 throws 5 and p2 throws 5, match.tiebreak === true, match.done === false, match.throwsRequired === 4
    - GF after 4 throws (p1: 9+8=17, p2: 7+6=13): state.done === true, state.gewinner.id === p1.id
    - Stray throw (player not in any active match): returned state is deepEqual to input state
    - applyThrow does not mutate input (deep-clone guard)
    - getFinalResults on finished tournament returns array with exactly 1 entry where winner: true; all player ids present
  </behavior>
  <action>
    ADD `applyThrow` and `getFinalResults` to the module.exports object in the file from Task 1. Do not re-implement initState.

    `applyThrow(state, player_id, value)`:
    1. `const s = JSON.parse(JSON.stringify(state));`
    2. `if (s.done) return s;`
    3. Find active match: `const match = s.bracket.find(m => !m.done && !m.isBye && m.p1 && m.p2 && (m.p1.id === player_id || m.p2.id === player_id));`
    4. `if (!match) return s;` — stray throw guard (per D-08, security T-06-02-04)
    5. Validate value is integer 0–9: `if (typeof value !== 'number' || value < 0 || value > 9) return s;` — security T-06-02-01
    6. Record: `const throwIndex = match.throws.length; match.throws.push({ playerId: player_id, throwIndex, value });`
    7. Determine required: `const req = match.throwsRequired || (match.bracket === 'GF' ? 4 : 2);`
    8. If `match.throws.length >= req`: compute p1Total and p2Total by filtering match.throws by playerId and summing value. If equal: `match.tiebreak = true; match.throwsRequired = req + 2;`. Else: set match.winner, match.loser, match.done = true; call `advancePlayer(s.bracket, match.advancesWinnerTo, match.winner)` and `if (match.advancesLoserTo) advancePlayer(s.bracket, match.advancesLoserTo, match.loser)`. Then check GF: `const gf = s.bracket.find(m => m.bracket === 'GF'); if (gf && gf.done) { s.done = true; s.gewinner = gf.winner; }`.
    9. `return s;`

    `getFinalResults(state)`:
    Collect unique player ids from all bracket slots (p1 and p2, skip nulls). Return array of `{ playerId: p.id, score: placement_score, winner: state.gewinner ? p.id === state.gewinner.id : false }`. Placement score: use tournament finish position — GF winner gets 0 losses credit; GF loser gets -1; others get negative count based on round eliminated. Simplest valid implementation: score = 0 for winner, score = -1 for all others (matches existing pattern from old engine).
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && npm run test:games 2>&1 | tail -25</automated>
  </verify>
  <done>
    `npm run test:games` exits 0. All 14 tests pass (C1–C5, KDA1–KDA9). Output shows "# tests 14", "# pass 14", "# fail 0". No test references state.spieler, state.matches, state.mid, or state.wRound.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → applyThrow | player_id and value arrive from POST /api/games/:id/throws; untrusted integers |
| applyThrow → bracket | engine modifies cloned state; original state never mutated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | applyThrow value param (pin count) | mitigate | Guard: `if (typeof value !== 'number' \|\| value < 0 \|\| value > 9) return s;` at top of applyThrow — rejects out-of-range pin counts per ASVS V5 |
| T-06-02-02 | Tampering | applyThrow player_id (must be in active match) | mitigate | Guard: `const match = s.bracket.find(...)` returns null for non-active players → `if (!match) return s;` — stray throw ignored |
| T-06-02-03 | Tampering | Duplicate throw via throw_index | accept | Existing UNIQUE constraint on (game_id, player_id, throw_index) in throws table enforced at API layer (games.js) — engine does not need to re-check |
| T-06-02-04 | Tampering | Throw for completed match | mitigate | Match lookup guard: `!m.done` in find() predicate — completed matches never matched |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
`npm run test:games` exits 0 with 14 passing tests.

`npm test` exits 0 (full suite — all 194 existing tests plus 14 new KDA tests pass).

Cross-check: `node -e "const k = require('./server/game-types/kegler-des-abends'); console.log(k.id, k.name)"` from Claude/ directory prints `kda Kegler des Abends`.
</verification>

<success_criteria>
- `kegler-des-abends.js` exports `{ id: 'kda', name: 'Kegler des Abends', initState, applyThrow, isFinished, getFinalResults }`
- `seededShuffle` function is preserved verbatim (identical to current lines 3–25)
- `initState` throws for player count outside 4–12 range
- `applyThrow` deep-clones state first, guards against stray throws and out-of-range pin values
- All 14 tests from `kegler-des-abends.test.js` pass GREEN
- `npm test` exits 0 (no regression in existing 194 tests)
</success_criteria>

<output>
Create `Claude/.planning/phases/06-turnierbaum/06-02-SUMMARY.md` when done.
</output>
