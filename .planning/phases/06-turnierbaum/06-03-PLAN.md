---
phase: 06-turnierbaum
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - Claude/public/index.html
autonomous: true
requirements:
  - TOURNAMENT-01
  - TOURNAMENT-02
must_haves:
  truths:
    - "KDA start dialog validates 4–12 players with inline counter and correct error messages"
    - "renderKDASpiel detects old vs new state format and routes to the correct renderer"
    - "New CSS bracket tree renders W-bracket top, L-bracket below, GF rightmost"
    - "Tapping active bracket slot opens the bottom-sheet throw-entry modal"
    - "Modal collects pin counts (0–9) per player, submits sequentially, closes on success"
    - "Grand Final modal collects 2 throws per player (4 total) before submitting"
    - "Tiebreak renders inline 'Stechen' section and repeats until winner found"
    - "All player names and scores use textContent — no innerHTML with DB data"
  artifacts:
    - path: "Claude/public/index.html"
      provides: "DE bracket tree UI + throw-entry modal + format detection switch"
  key_links:
    - from: "renderKDASpiel"
      to: "renderKDABracket"
      via: "if (state.bracket) branch"
      pattern: "state\\.bracket"
    - from: "renderKDABracket"
      to: "submitThrow"
      via: "modal confirm button onclick"
      pattern: "submitThrow"
    - from: "startKDA"
      to: "kdaSel.length"
      via: "< 4 || > 12 guard"
      pattern: "kdaSel\\.length"
---

<objective>
Update `index.html` to replace the old KDA renderer and winner-selection UI with a full DE bracket tree renderer, a bottom-sheet throw-entry modal, and a format-detection switch that preserves old KDA game display. Also update the KDA start dialog player count validation to enforce 4–12 players.

Purpose: Deliver TOURNAMENT-01 (tablet UI + validation) and TOURNAMENT-02 (bracket tree + throw entry).
Output: Modified `index.html` with new KDA functions: `renderKDASpiel` (dispatch), `renderKDABracket` (tree), `renderKDALegacy` (old renderer preserved), throw modal HTML + `openKDAWurfModal` + `submitKDAWurfe`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/phases/06-turnierbaum/06-CONTEXT.md
@Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md
@Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md

<interfaces>
<!-- submitThrow — reuse as-is, do not modify -->
<!-- Source: Claude/public/index.html lines 394–406 -->
async function submitThrow(player_id, throw_index, value, meta) {
  var body = { player_id: player_id, throw_index: throw_index, value: value };
  if (meta !== undefined) body.meta = meta;
  var res = await fetch('/api/games/' + S.aktSpiel.gameId + '/throws', {
    method: 'POST', headers: { 'content-type': 'application/json' },
    body: JSON.stringify(body)
  });
  if (!res.ok) {
    if (res.status === 409) { notify('Doppelter Wurf', 'var(--red)'); }
    else { notify('Fehler beim Wurf', 'var(--red)'); }
    return null;
  }
  return await res.json();
}

<!-- State update pattern after submitThrow — copy verbatim (index.html lines 797–800 pattern) -->
var data = await submitThrow(p.id, throwIndex, value, undefined);
if (!data) return;
S.aktSpiel.state = data.state;
if (data.finished) { handleGameFinished(data.state); } else { renderSpielenTab(); }

<!-- startKDA — current version to be modified (index.html lines 907–921) -->
async function startKDA() {
  if (kdaSel.length < 2) { notify('Mind. 2 Spieler', 'var(--red)'); return; }
  // ... rest unchanged
}

<!-- KDA start modal HTML — current (index.html lines 220–228) -->
<div class="mo" id="m-kda"><div class="md">
  <h2>🏆 Kegler des Abends</h2>
  <div class="fg"><label>Datum</label><input type="date" id="m-kda-datum"></div>
  <div class="fg"><label>Spieler (min. 2)</label><div id="m-kda-wahl" ...></div></div>
  <div style="display:flex;gap:10px;margin-top:18px">
    <button class="btn bp" style="flex:1" onclick="startKDA()">Starten</button>
    <button class="btn bg" onclick="closeM('m-kda')">Abbrechen</button>
  </div>
</div></div>

<!-- openM / closeM pattern — existing -->
function openM(id) { document.getElementById(id).style.display = 'flex'; }
function closeM(id) { document.getElementById(id).style.display = 'none'; }

<!-- Modal CSS classes (existing, from index.html :root) -->
.mo { position:fixed;inset:0;background:#00000088;z-index:100;display:none;align-items:flex-end }
.md { background:var(--bg2);border-radius:16px 16px 0 0;padding:20px;width:100%;max-height:80vh;overflow-y:auto }
.btn.bp { background:var(--ac);color:#000;font-weight:700 }
.btn.bg { background:transparent;border:1.5px solid var(--brd);color:var(--txt) }
.kb { /* pin number button */ }
.kgrid { /* 5-column pin grid */ display:grid;grid-template-columns:repeat(5,1fr);gap:6px }
.badge.bgrn, .badge.bamb, .badge.bred, .badge.bgry { /* status badges */ }
--ac: #e8b84b; --grn: #4caf7d; --red: #e05252; --mut: #8884a0; --brd: #2e2e3e; --card: #22222e; --bg2: #17171f

<!-- renderKDASpiel current function — lines 931 of index.html (INLINE, one long line) -->
// Current function uses state.spieler, state.matches, state.mid, state.bye, state.wRound
// This entire function body becomes renderKDALegacy (preserved as-is)
// New renderKDASpiel is a format-detection dispatcher

<!-- Old KDA game display detection (RESEARCH.md Pattern 3, UI-SPEC Interaction Contracts) -->
// Old format: state.matches && !state.bracket
// New format: state.bracket (flat slot array)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update KDA start dialog (D-12 validation) and add throw modal HTML</name>
  <files>Claude/public/index.html</files>
  <read_first>
    - Claude/public/index.html lines 220–228 (KDA start modal HTML — the block to modify)
    - Claude/public/index.html lines 902–931 (startKDA, togKDA, renderKDAWahl, renderKDASpiel functions)
    - Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md lines 259–264 (start dialog changes spec)
    - Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md lines 206–253 (match entry modal spec)
  </read_first>
  <action>
    Change 1 — Start modal HTML (line 223): Replace `"Spieler (min. 2)"` with `"Spieler (min. 4, max. 12)"`. After the `#m-kda-wahl` div, add a counter span: `<div id="m-kda-counter" style="font-size:12px;color:var(--mut);margin-top:6px;text-align:center">0 Spieler ausgewählt</div>`. Change the start button text from `"Starten"` to `"Turnier starten"`.

    Change 2 — Add throw entry modal HTML: After the closing `</div></div>` of `#m-kda` (line 228), insert a new modal `<div class="mo" id="m-kda-wurf"><div class="md">`. The modal must contain: a title element `<div id="m-kda-wurf-title" ...>` (Bebas Neue 20px, color var(--ac)), a match label badge `<span id="m-kda-wurf-badge" class="badge bamb"></span>`, a close button (`×` with onclick="closeM('m-kda-wurf')"), and a container div `<div id="m-kda-wurf-body"></div>` where the player throw rows and pin grids are injected by JS. At bottom: `<button class="btn bp" id="m-kda-wurf-confirm" style="width:100%;margin-top:12px;opacity:.4" disabled onclick="submitKDAWurfe()">Wurf bestätigen</button>` and `<button class="btn bg" style="width:100%;margin-top:8px" onclick="closeM('m-kda-wurf')">Abbrechen</button>`. Add `role="dialog"`, `aria-modal="true"`, `aria-labelledby="m-kda-wurf-title"` to the `.md` div.

    Change 3 — Update `startKDA()` function: replace `if (kdaSel.length < 2)` with `if (kdaSel.length < 4 || kdaSel.length > 12)` and change the notify text to `'Mind. 4 Spieler (max. 12)'`.

    Change 4 — Update `togKDA()` function: after toggling the selection, call `updateKDACounter()`.

    Change 5 — Add `updateKDACounter()` function: reads `kdaSel.length`, sets `#m-kda-counter` textContent to `[N] Spieler ausgewählt` (green color var(--grn) when 4 ≤ N ≤ 12, red color var(--red) otherwise). Update renderKDAWahl() to call updateKDACounter() at the end.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node -e "const fs=require('fs');const h=fs.readFileSync('public/index.html','utf8');console.log('counter:',h.includes('m-kda-counter'));console.log('modal:',h.includes('m-kda-wurf'));console.log('min4:',h.includes('kdaSel.length < 4'));console.log('max12:',h.includes('kdaSel.length > 12'));console.log('turnier:',h.includes('Turnier starten'));"</automated>
  </verify>
  <done>
    All five checks print `true`. `#m-kda-counter` div exists in HTML. `#m-kda-wurf` modal exists. `startKDA()` guards with `< 4 || > 12`. Start button reads "Turnier starten".
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement bracket renderer and throw-entry modal logic</name>
  <files>Claude/public/index.html</files>
  <read_first>
    - Claude/public/index.html lines 902–931 (current KDA functions — renderKDASpiel body becomes renderKDALegacy)
    - Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md lines 114–165 (Bracket Tree tablet spec — slot sizes, CSS classes, connector line approach)
    - Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md lines 270–303 (throw entry flow spec)
    - Claude/.planning/phases/06-turnierbaum/06-PATTERNS.md lines 347–373 (DOM construction pattern — textContent only)
    - Claude/.planning/phases/06-turnierbaum/06-UI-SPEC.md lines 339–410 (copywriting contract — German labels)
  </read_first>
  <action>
    Replace the existing `renderKDASpiel` function and add new functions. All DOM manipulation uses `createElement` + `textContent`. No `innerHTML` calls with player data (DB strings).

    Function 1 — `renderKDASpiel(el, state)`: Format detection dispatcher.
    If `!state`: `el.innerHTML = '';` return. If `state.bracket`: call `renderKDABracket(el, state)` and return. Else: call `renderKDALegacy(el, state)` (the old renderer).

    Function 2 — `renderKDALegacy(el, state)`: The current `renderKDASpiel` function body, renamed. Move the entire existing function body here unchanged.

    Function 3 — `renderKDABracket(el, state)`: Builds the CSS bracket tree from `state.bracket`.
    - Create outer `div.kda-bracket-scroll` with `style="overflow-x:auto;overflow-y:hidden;-webkit-overflow-scrolling:touch"`.
    - Create inner `div.kda-bracket` with `style="display:inline-flex;flex-direction:column;min-width:max(640px,${columns * 160}px);padding:16px"`.
    - Create `div.kda-bracket-section` for W bracket, set section title textContent = "Winner Bracket".
    - Create `div` with `style="display:flex;gap:16px"` for W bracket columns. For each distinct round in W bracket slots, create `div.bracket-col` with `style="display:flex;flex-direction:column;justify-content:space-around;gap:16px"`. Add round label div above: textContent = round label from copywriting contract (W-R1 → "W · Runde 1", W-Final → "W · Finale", etc.). For each slot in this round, call `buildBracketSlotEl(slot)` and append.
    - Repeat for L bracket section (title "Loser Bracket").
    - Add GF column: create slot element for the GF slot using `buildBracketSlotEl`.
    - Append the scroll container to `el` using `el.replaceChildren(scrollContainer)`.

    If `state.done && state.gewinner`: prepend a winner banner div before the bracket: textContent = `state.gewinner.emoji + ' ' + state.gewinner.name + ' — Kegler des Abends!'`. Style: Bebas Neue 32px, color var(--ac), text-align center, padding 16px.

    Function 4 — `buildBracketSlotEl(slot)`: Returns a `div.bracket-slot` element.
    - Size: style width 140px, min-height 56px (44px minimum for active slots), border-radius 8px, background var(--card), border 1px solid var(--brd), cursor default.
    - If `slot.isBye`: set opacity 0.45, border-style dashed, add aria-label = "Freilos — " + (slot.winner ? slot.winner.name : '—').
    - If active slot (`!slot.done && !slot.isBye && slot.p1 && slot.p2`): set border-color var(--ac), background #e8b84b11, cursor pointer, min-height 44px, role=button, tabindex=0, aria-label = slot.p1.name + ' vs ' + slot.p2.name + ' — Jetzt spielen'. Add onclick: `openKDAWurfModal(slot.id)`.
    - Build two player rows (p1 and p2). Each row: `div` with display:flex, justify-content:space-between, padding 6px 8px. Player name span: textContent = (p ? p.emoji + ' ' + p.name : '—'), color var(--mut) for null. Score span: textContent = computed score from slot.throws for this player (sum for GF, single value otherwise), color var(--ac) for in-progress, var(--grn) for winner row, var(--red) for loser row. If in-progress (throw exists but match not done): append ' ⚫' to score textContent.
    - If `slot.done`: add a 1px var(--brd) divider between the two rows.
    - Return the element.

    Function 5 — `openKDAWurfModal(slotId)`: Opens `#m-kda-wurf`.
    - Find slot by id in `S.aktSpiel.state.bracket`.
    - Set `#m-kda-wurf-title` textContent to round label + ' · Match ' + N (derive match number from slot id suffix).
    - Set `#m-kda-wurf-badge` className to appropriate badge class (bgrn for W, bamb for L/GF).
    - Store current slot id in a module-level variable `kdaWurfSlotId = slotId`.
    - Set module-level `kdaWurfVals = {}` (keyed by player id, values for each throw index).
    - Call `renderKDAWurfBody()` to build the modal body.
    - Open the modal: `openM('m-kda-wurf')`.

    Function 6 — `renderKDAWurfBody()`: Rebuilds `#m-kda-wurf-body` content.
    - Find slot from `S.aktSpiel.state.bracket` by `kdaWurfSlotId`.
    - For each player [slot.p1, slot.p2]: create a player section with avatar (textContent = emoji), name (textContent = name), and pin grid(s).
    - Pin grid: `div.kgrid` with 10 buttons (0–9) using `.kb` class. On tap: `kdaSelPin(playerId, throwIdx, pinValue)`. Selected pin: add class `sel` (amber highlight).
    - For Grand Final (`slot.bracket === 'GF'`): render two throw rows per player labelled "Wurf 1" and "Wurf 2". Show running total per player below their rows (textContent = sum of selected values).
    - For tiebreak (`slot.tiebreak === true` and match is still open): add "Stechen — Gleichstand!" section header in amber, with one throw input per player.
    - Update confirm button: enabled (opacity 1, remove disabled) only when all required pins selected (`allPinsSelected(slot)`).

    Function 7 — `kdaSelPin(playerId, throwIdx, value)`: Updates `kdaWurfVals[playerId + '-' + throwIdx] = value`. Calls `renderKDAWurfBody()` to re-render with selection highlighted.

    Function 8 — `submitKDAWurfe()` (async): Reads `kdaWurfSlotId` and `kdaWurfVals`. Finds slot. Determines throw order: p1's throws first (throwIdx 0, 1 for GF), then p2's throws (throwIdx incremented from current match.throws.length). Submit sequentially using the pattern from PATTERNS.md: `await submitThrow(playerId, throwIndex, value)` for each throw, checking `!data` after each call, updating `S.aktSpiel.state = data.state` and calling `renderSpielenTab()` after each. After all throws submitted: close modal with `closeM('m-kda-wurf')`. The `throw_index` passed to submitThrow is the sequential position in `match.throws` — derive from `match.throws.length` before each call (read from the server-returned state.bracket after each submitThrow response).

    Add CSS block (inside the existing `<style>` tag in index.html): Rules for `.kda-bracket-scroll`, `.kda-bracket`, `.bracket-col`, `.bracket-slot`, `.bracket-round-label`, `.bracket-section-title`. Connector lines between slots: use `::before`/`::after` pseudo-elements on `.bracket-slot` with `border-right` and `border-top/bottom` in var(--brd) 2px. Bracket slot winner row: background `#4caf7d11`. Loser row: background `#e0525211`.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node -e "const fs=require('fs');const h=fs.readFileSync('public/index.html','utf8');console.log('renderKDABracket:',h.includes('function renderKDABracket'));console.log('renderKDALegacy:',h.includes('function renderKDALegacy'));console.log('state.bracket:',h.includes('state.bracket'));console.log('openKDAWurfModal:',h.includes('function openKDAWurfModal'));console.log('submitKDAWurfe:',h.includes('function submitKDAWurfe'));console.log('no innerHTML kda:', !h.match(/innerHTML.*player\.(name|emoji)/i));console.log('textContent:',h.includes('textContent'));"</automated>
  </verify>
  <done>
    All checks print true. `renderKDABracket`, `renderKDALegacy`, `openKDAWurfModal`, `submitKDAWurfe` functions exist. `state.bracket` detection present. No `innerHTML` with `player.name` or `player.emoji`. `textContent` used throughout KDA functions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB state → DOM | Player names and emoji from state.bracket[].p1/p2 rendered into bracket slot DOM |
| User input → submitThrow | Pin count values (0–9) collected in modal, sent to API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | XSS | renderKDABracket — player names in bracket slots | mitigate | All player name and emoji rendering uses `textContent` throughout `buildBracketSlotEl` — no `innerHTML` with DB strings. Verified by automated check. |
| T-06-03-02 | XSS | renderKDAWurfBody — player names in modal | mitigate | Modal player name and emoji set via `textContent` only — same pattern as existing TV player list |
| T-06-03-03 | Tampering | Pin count from modal (0–9) | mitigate | Frontend: only values 0–9 available as buttons — user cannot type arbitrary value. Backend: applyThrow guards `value < 0 \|\| value > 9` (implemented in Plan 02). |
| T-06-03-04 | Tampering | Throw for non-active slot | mitigate | `openKDAWurfModal` only called from active slot onclick handlers; `submitKDAWurfe` reads slot from current state |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
Manual visual verification (noted in VALIDATION.md as required):
1. Start a 4-player KDA game on tablet — bracket tree renders with W-bracket, L-bracket, GF columns.
2. Tap an active bracket slot — throw modal opens with pin grids for both players.
3. Enter pin counts and confirm — bracket updates.
4. Open a pre-Phase-6 KDA game from history — old match-list view renders (no crash, uses renderKDALegacy).
5. Try starting KDA with 3 players — error notification "Mind. 4 Spieler (max. 12)" appears.

Automated: `node -e` checks from Task 1 and Task 2 verify all pass.
</verification>

<success_criteria>
- `renderKDASpiel` dispatches to `renderKDABracket` when `state.bracket` present, to `renderKDALegacy` otherwise
- `renderKDALegacy` is the old `renderKDASpiel` body — old KDA games still render correctly
- `startKDA()` guards `kdaSel.length < 4 || kdaSel.length > 12` (per D-12)
- `#m-kda-counter` updates live as players are selected, green when 4–12, red otherwise
- `buildBracketSlotEl` sets all player names and scores via `textContent` — no innerHTML with DB strings
- `submitKDAWurfe` submits throws sequentially (per RESEARCH.md Pitfall 2), 4 calls for Grand Final
- Tiebreak state (`slot.tiebreak === true`) causes modal to show "Stechen" section
</success_criteria>

<output>
Create `Claude/.planning/phases/06-turnierbaum/06-03-SUMMARY.md` when done.
</output>
