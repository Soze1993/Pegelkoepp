---
phase: 10-team-gewinner-gastkegler
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db/index.js
  - server/routes/players.js
  - server/routes/abende.js
  - public/index.html
autonomous: true
requirements:
  - GUEST-01
  - GUEST-02
  - GUEST-03
  - GUEST-04
tags:
  - guests
  - players
  - frontend
  - migration
must_haves:
  truths:
    - "Eine neue Spalte `is_guest INTEGER NOT NULL DEFAULT 0` existiert in der `players`-Tabelle nach Server-Restart"
    - "Im 'Spieler hinzufügen'-Dialog gibt es eine 'Gast?'-Checkbox"
    - "Hinzufügen eines Spielers mit aktivierter Checkbox sendet einen POST an `/api/players` mit `is_guest: true`"
    - "GET /api/players gibt das Feld `is_guest` für jeden Spieler zurück"
    - "Gastkegler erscheinen in der Spielerliste mit Label-Suffix '(Gast)' hinter dem Namen"
    - "Gastkegler erscheinen in den drei Spielauswahl-Modals (BK, VG, FJ) mit '(Gast)'-Suffix und sind auswählbar"
    - "Gastkegler werden NICHT lokal mit `mkSp(Date.now(), ...)` erzeugt — sondern via API mit Server-vergebener id"
    - "Beim Beenden des Kegelabends (POST /api/abende/:id/end) werden alle nicht-archvierten Gäste in einem zweiten UPDATE archiviert"
    - "Historische Spieldaten (game_players, throws) der Gäste bleiben nach Archivierung intakt"
  artifacts:
    - path: "server/db/index.js"
      provides: "ALTER TABLE players ADD COLUMN is_guest INTEGER NOT NULL DEFAULT 0 — idempotent in migrations-Array"
      contains: "is_guest INTEGER NOT NULL DEFAULT 0"
    - path: "server/routes/players.js"
      provides: "GET liefert is_guest, POST nimmt is_guest entgegen"
      contains: "is_guest"
    - path: "server/routes/abende.js"
      provides: "Guest-Auto-Archive in POST /:id/end"
      contains: "is_guest = 1"
    - path: "public/index.html"
      provides: "Gast-Checkbox + async addSpieler + (Gast)-Label in 4 Render-Punkten"
      contains: "m-sp-gast"
  key_links:
    - from: "public/index.html addSpieler()"
      to: "POST /api/players"
      via: "fetch mit same-origin Session-Cookie"
      pattern: "fetch\\('/api/players'"
    - from: "public/index.html renderSpielerListe/renderBKWahl/renderVGWahl/renderFJWahl"
      to: "(Gast)-Suffix"
      via: "ternary s.is_guest ? s.name+' (Gast)' : s.name"
      pattern: "s\\.is_guest \\? s\\.name\\+' \\(Gast\\)'"
    - from: "server/routes/abende.js POST /:id/end"
      to: "UPDATE players SET archived = 1 WHERE is_guest = 1 AND archived = 0"
      via: "Zweite db.prepare(...).run() Anweisung nach abend-UPDATE"
      pattern: "WHERE is_guest = 1 AND archived = 0"
---

<objective>
Implementiert Gastkegler end-to-end: DB-Migration `is_guest`-Spalte, API-Erweiterung in `/api/players` (GET + POST), automatische Gast-Archivierung in `/api/abende/:id/end`, und Frontend-Integration in `public/index.html` (Checkbox im Add-Dialog, async POST in `addSpieler()`, `(Gast)`-Label in 4 Render-Funktionen).

Purpose: GUEST-01 (Gast erstellen), GUEST-02 ((Gast)-Kennzeichnung sichtbar), GUEST-03 (Frontend-Label-Anteil; Stats-Ausschluss kommt aus Plan 10-01), GUEST-04 (Auto-Archivierung am Abend-Ende).
Output: 4 modifizierte Dateien, DB-Schema erweitert, Gastkegler-Flow funktional.
</objective>

<artifacts_this_phase_produces>
Symbols and behaviors created or changed by Plan 10-02:

**DB:**
- `players.is_guest` — neue SQL-Spalte vom Typ `INTEGER NOT NULL DEFAULT 0`

**Backend API:**
- `GET /api/players` response shape extended: `[{ id, name, emoji, is_guest }]` (added field `is_guest`)
- `POST /api/players` request body extended: accepts optional `is_guest: boolean`; response shape extended with `is_guest: 0|1`
- `POST /api/abende/:id/end` side-effect extended: now also runs `UPDATE players SET archived = 1 WHERE is_guest = 1 AND archived = 0`

**Frontend (public/index.html):**
- New DOM element id: `m-sp-gast` (checkbox in Add-Player dialog)
- `addSpieler()` — signature changed from sync to `async`; now performs `fetch('/api/players', {method:'POST'})`
- `renderSpielerListe()` — name template now includes `(s.is_guest ? s.name+' (Gast)' : s.name)`
- `renderBKWahl()` — same `(Gast)`-suffix ternary in player chip rendering
- `renderVGWahl()` — same `(Gast)`-suffix ternary in player chip rendering (both team columns)
- `renderFJWahl()` — same `(Gast)`-suffix ternary in both Fuchs- and Jäger-grids

**NOT created by this plan (already exist or out of scope):**
- `is_guest = 0` guard in `/api/stats` and `/api/stats/year` SELECTs (created by Plan 10-01 Task 3)
- New endpoints, new routes, new auth (none)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-team-gewinner-gastkegler/10-CONTEXT.md
@.planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md
@server/db/index.js
@server/routes/players.js
@server/routes/abende.js
@public/index.html
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DB-Migration `is_guest`-Spalte + Players-API (per D-07, D-08, D-09)</name>
  <files>server/db/index.js, server/routes/players.js</files>
  <read_first>
    - server/db/index.js (vollständig — insbesondere migrations-Array Z.26-33 und try/catch-Loop Z.35-42)
    - server/routes/players.js (vollständig — GET Z.9-14, POST Z.17-33)
    - .planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md (db/index.js und players.js Sektionen)
  </read_first>
  <behavior>
    - Nach Server-Start existiert die Spalte `is_guest` in `players`-Tabelle (idempotent: zweiter Start läuft fehlerfrei durch wegen try/catch auf "duplicate column name")
    - GET /api/players gibt für jeden Spieler `is_guest: 0` oder `is_guest: 1` zurück
    - POST /api/players mit Body `{name:"Max", emoji:"🦊", is_guest:true}` legt einen Spieler mit `is_guest = 1` an und gibt `{id, name:"Max", emoji:"🦊", is_guest:1}` zurück
    - POST /api/players ohne `is_guest`-Feld im Body legt einen regulären Spieler mit `is_guest = 0` an und gibt `{id, name, emoji, is_guest:0}` zurück
    - POST /api/players mit `is_guest: "truthy string"` oder `is_guest: 1` wird ebenfalls als Gast behandelt (coercion via `is_guest ? 1 : 0`)
    - Validierung von `name` und `emoji` bleibt unverändert; 400 bei leerem name
  </behavior>
  <action>
    **server/db/index.js:**
    Ein neues SQL-String-Element ans Ende des `migrations`-Arrays (vor der schließenden `]` Z.33) anhängen: `'ALTER TABLE players ADD COLUMN is_guest INTEGER NOT NULL DEFAULT 0'`. Die bestehende try/catch-Loop (Z.35-42) behandelt das Element idempotent — kein anderer Code-Change in dieser Datei.

    **server/routes/players.js:**
    1. GET-Handler (Z.9-14): SELECT-String von `'SELECT id, name, emoji FROM players WHERE archived = 0 ORDER BY id ASC'` zu `'SELECT id, name, emoji, is_guest FROM players WHERE archived = 0 ORDER BY id ASC'` erweitern.
    2. POST-Handler (Z.17-33):
       - Destructuring um `is_guest` erweitern: `const { name, emoji, is_guest } = req.body || {};`
       - Nach der emoji-Validierung eine Zeile einfügen: `const guestFlag = is_guest ? 1 : 0;`
       - INSERT-Statement von `'INSERT INTO players (name, emoji) VALUES (?, ?)'` zu `'INSERT INTO players (name, emoji, is_guest) VALUES (?, ?, ?)'` ändern; `.run(cleanName, cleanEmoji, guestFlag)`.
       - Response-Objekt um `is_guest: guestFlag` erweitern.
    PUT-Handler (Z.36-50) und ARCHIVE-Handler (Z.53-63) bleiben unverändert.
  </action>
  <verify>
    <automated>node -e "const db=require('./server/db'); const cols=db.prepare(\"PRAGMA table_info(players)\").all().map(c=>c.name); if(!cols.includes('is_guest')){console.error('FAIL no is_guest column, cols='+cols.join(',')); process.exit(1)} const p=require('fs').readFileSync('server/routes/players.js','utf8'); if(!/SELECT id, name, emoji, is_guest FROM players/.test(p)){console.error('FAIL GET SELECT missing is_guest'); process.exit(1)} if(!/INSERT INTO players \(name, emoji, is_guest\)/.test(p)){console.error('FAIL POST INSERT missing is_guest'); process.exit(1)} if(!/is_guest \? 1 : 0/.test(p)){console.error('FAIL POST coercion missing'); process.exit(1)} console.log('OK migration + api wired')"</automated>
  </verify>
  <acceptance_criteria>
    - behavior: After running `require('./server/db')` (which triggers migrations), `PRAGMA table_info(players)` returns a row with `name === 'is_guest'`
    - behavior: Running the migration a second time does not throw (idempotent via existing try/catch on "duplicate column name")
    - source-assertion: `server/db/index.js` migrations-Array contains the string `'ALTER TABLE players ADD COLUMN is_guest INTEGER NOT NULL DEFAULT 0'`
    - source-assertion: `server/routes/players.js` GET handler SELECT contains `is_guest`
    - source-assertion: `server/routes/players.js` POST handler INSERT statement is exactly `INSERT INTO players (name, emoji, is_guest) VALUES (?, ?, ?)`
    - source-assertion: `server/routes/players.js` POST handler contains the coercion `is_guest ? 1 : 0`
    - source-assertion: `server/routes/players.js` POST response object includes a key `is_guest`
    - test-command: `node -c server/db/index.js` and `node -c server/routes/players.js` both exit 0
  </acceptance_criteria>
  <done>
    DB hat `is_guest`-Spalte, GET liefert das Feld zurück, POST nimmt es entgegen und persistiert es. Voraussetzung für Frontend (Task 3) und Abend-Archivierung (Task 2) erfüllt.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Gast-Auto-Archivierung in POST /api/abende/:id/end (per D-12, D-13, GUEST-04)</name>
  <files>server/routes/abende.js</files>
  <read_first>
    - server/routes/abende.js (vollständig — POST /:id/end Z.150-160)
    - server/routes/players.js Z.52-63 (analog: archive-Pattern `UPDATE players SET archived = 1 WHERE …`)
    - .planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md (abende.js Sektion)
  </read_first>
  <behavior>
    - POST /api/abende/:id/end mit gültiger id → abend wird beendet AND alle Spieler mit `is_guest = 1 AND archived = 0` werden archiviert (`archived = 1`). Response: `{ ok: true }`.
    - POST /api/abende/:id/end mit invalider id (z.B. 0, negativ, non-integer) → 400 mit `{error: 'invalid id'}`. KEIN Guest-Archive-UPDATE wird ausgeführt.
    - POST /api/abende/:id/end mit nicht existierender id → 404 mit `{error: 'Abend not found'}` (Detection via `info.changes === 0` aus dem ersten UPDATE). KEIN Guest-Archive-UPDATE wird ausgeführt.
    - Spieldaten in `game_players` und `throws` für archivierte Gäste bleiben unverändert (kein DELETE, nur archived-Flag).
    - Reguläre Spieler (`is_guest = 0`) werden NICHT archiviert.
  </behavior>
  <action>
    In `server/routes/abende.js` im POST `/:id/end` Handler (Z.150-160):
    Nach dem `if (!info.changes) return res.status(404).json({ error: 'Abend not found' });` Check (Z.158) und VOR `res.json({ ok: true });` (Z.159) eine zusätzliche prepared statement-Ausführung einfügen: `db.prepare('UPDATE players SET archived = 1 WHERE is_guest = 1 AND archived = 0').run();`. Kein Parameter nötig — Filter erfolgt komplett in SQL, weil zur Zeit nur ein offener Abend existieren kann (durch existierenden Guard in POST /api/abende, Z.134-137).
    Wichtig: Die zwei UPDATEs müssen in der oben angegebenen Reihenfolge ablaufen (erst abend, dann player-archive). Bei Bedarf für Atomicity optional in `db.transaction(...)()` wrappen — better-sqlite3 ist synchron, sequenzielle Ausführung ist ausreichend für diesen Use Case.
    Kommentar oberhalb des neuen Statements einfügen: `// Archive all guest players when abend ends (D-12, GUEST-04)`.
    Keine weiteren Änderungen in der Datei.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('server/routes/abende.js','utf8'); const hasArchive=/UPDATE players SET archived = 1 WHERE is_guest = 1 AND archived = 0/.test(t); const idxArchive=t.indexOf('is_guest = 1 AND archived = 0'); const idxEnd=t.indexOf(\"UPDATE abende SET ended_at\"); const idxNotFound=t.indexOf(\"'Abend not found'\"); if(!hasArchive){console.error('FAIL no guest-archive UPDATE'); process.exit(1)} if(idxArchive < idxNotFound){console.error('FAIL guest-archive runs before not-found check'); process.exit(1)} if(idxArchive < idxEnd){console.error('FAIL guest-archive runs before abend UPDATE'); process.exit(1)} console.log('OK guest auto-archive correctly placed')"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `server/routes/abende.js` contains the exact SQL string `UPDATE players SET archived = 1 WHERE is_guest = 1 AND archived = 0`
    - source-assertion: The guest-archive UPDATE appears AFTER the abend-UPDATE and AFTER the `'Abend not found'` 404-return (so it only runs when the abend was actually closed)
    - source-assertion: The guest-archive UPDATE appears inside the `router.post('/:id/end', requireSession, ...)` handler
    - behavior: When this endpoint is called with valid id, `info.changes` of the abend update is checked BEFORE the guest archive runs (404 path skips guest archive)
    - test-command: `node -c server/routes/abende.js` exits 0
  </acceptance_criteria>
  <done>
    Beim Beenden eines Abends werden alle aktiven Gäste automatisch archiviert. Spieldaten bleiben erhalten. Reguläre Spieler bleiben unangetastet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Frontend Gast-Checkbox, async addSpieler() und (Gast)-Label in 4 Render-Punkten (per D-10, D-14, D-15, GUEST-01, GUEST-02)</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html Z.170-178 (Dialog `m-neu-sp`)
    - public/index.html Z.831-833 (renderSpEmo, addSpieler, renderSpielerListe — minified)
    - public/index.html Z.1262 (renderBKWahl)
    - public/index.html Z.1385 (renderVGWahl)
    - public/index.html Z.1415 (renderFJWahl)
    - public/index.html Z.336-348, 372-387 (notify pattern, fetch + auth pattern in init)
    - .planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md (Change Points 1-4 sections)
  </read_first>
  <behavior>
    - Öffnen des "Spieler hinzufügen"-Dialogs zeigt jetzt zusätzlich eine "Gast? (wird nach Abend archiviert)"-Checkbox unterhalb des Emoji-Grids.
    - Klick auf "Hinzufügen" mit aktivierter Checkbox sendet `POST /api/players` mit `{name, emoji, is_guest: true}`, zeigt bei Erfolg "Spieler hinzugefuegt!" und schließt den Dialog.
    - Klick auf "Hinzufügen" mit deaktivierter Checkbox sendet `POST /api/players` mit `{name, emoji, is_guest: false}` (regulärer Spieler).
    - Bei Server-Fehler (Response not ok) erscheint "Fehler beim Speichern" in rot via notify.
    - Bei Erfolg wird der vom Server zurückgegebene Spieler (mit Server-vergebener `id` und `is_guest`-Feld) in `S.spieler` gepusht — NICHT mehr via `mkSp(Date.now(), ...)`.
    - Die Checkbox wird nach erfolgreichem Hinzufügen automatisch zurückgesetzt (für den nächsten Spieler).
    - In der Spielerliste (`renderSpielerListe`) erscheint hinter dem Namen eines Gastes ` (Gast)` als String-Suffix.
    - In den drei Spielauswahl-Modals (BK, VG, FJ) erscheint ebenfalls ` (Gast)` hinter dem Namen jeden Gastes — sowohl in Fuchs- als auch Jäger-Grid bei FJ, und in beiden Team-Columns bei VG.
  </behavior>
  <action>
    Vier präzise Edits in `public/index.html`:

    **Edit 1 — Dialog HTML (zwischen Z.173 Emoji-fg und Z.174 Button-Row, ein neues `<div class="fg">` einfügen):**
    Direkt nach der Zeile `<div class="fg"><label>Emoji</label><div class="egrid" id="m-sp-emoji"></div></div>` ein neues Element einfügen:
    `<div class="fg" style="display:flex;align-items:center;gap:10px;margin-top:8px"><input type="checkbox" id="m-sp-gast" style="width:18px;height:18px;cursor:pointer"><label for="m-sp-gast" style="cursor:pointer;font-size:14px">Gast? (wird nach Abend archiviert)</label></div>`

    **Edit 2 — `addSpieler()`-Funktion (Z.832) komplett ersetzen:**
    Die aktuelle Funktion `function addSpieler(){var name=document.getElementById('m-sp-name').value.trim();if(!name){notify('Name eingeben','var(--red)');return;}S.spieler.push(mkSp(Date.now(),name,selEmo));closeM('m-neu-sp');document.getElementById('m-sp-name').value='';notify('Spieler hinzugefuegt!');renderAll();}` ersetzen durch:
    `async function addSpieler(){var name=document.getElementById('m-sp-name').value.trim();if(!name){notify('Name eingeben','var(--red)');return;}var isGast=document.getElementById('m-sp-gast').checked;var res=await fetch('/api/players',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:name,emoji:selEmo,is_guest:isGast})});if(!res.ok){notify('Fehler beim Speichern','var(--red)');return;}var p=await res.json();S.spieler.push(p);closeM('m-neu-sp');document.getElementById('m-sp-name').value='';document.getElementById('m-sp-gast').checked=false;notify('Spieler hinzugefuegt!');renderAll();}`
    Wichtig: `function` wird zu `async function`; ohne await wäre `p` ein Promise — beide await-Punkte sind kritisch.

    **Edit 3 — `renderSpielerListe()` (Z.833):**
    In der `.map()`-Lambda das Pattern `'<div class="nm">'+s.name+'</div>'` ersetzen durch `'<div class="nm">'+(s.is_guest?s.name+' (Gast)':s.name)+'</div>'`. Nur diese eine Stelle in `renderSpielerListe`. Keine anderen Felder ändern.

    **Edit 4 — `renderBKWahl()` (Z.1262), `renderVGWahl()` (Z.1385), `renderFJWahl()` (Z.1415):**
    In jeder dieser drei Funktionen das Pattern `s.emoji+' '+s.name` (in `innerHTML`-Template-Strings) ersetzen durch `s.emoji+' '+(s.is_guest?s.name+' (Gast)':s.name)`. Bei `renderVGWahl` betrifft das die eine Stelle im inneren `forEach(['x','o'])`-Lambda (gleiche Stelle für beide Teams). Bei `renderFJWahl` betrifft das ZWEI Stellen (`m-fj-fuchs`-Grid und `m-fj-jaeger`-Grid). Insgesamt also: 1× BK, 1× VG, 2× FJ.

    Hinweis: `s.is_guest` kommt vom Server als 0 oder 1 (truthy/falsy korrekt im ternary). Backend liefert das Feld bereits nach Task 1.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('public/index.html','utf8'); const checks={dialog:/id=\"m-sp-gast\"/.test(t), asyncFn:/async function addSpieler/.test(t), fetchPost:/fetch\('\/api\/players'/.test(t), isGuestBody:/is_guest:isGast/.test(t), oldLocalPush:/S\.spieler\.push\(mkSp\(Date\.now\(\)/.test(t), labelTernary:(t.match(/s\.is_guest\?s\.name\+' \(Gast\)':s\.name/g)||[]).length}; const fails=[]; if(!checks.dialog) fails.push('no m-sp-gast checkbox'); if(!checks.asyncFn) fails.push('addSpieler not async'); if(!checks.fetchPost) fails.push('no fetch POST /api/players'); if(!checks.isGuestBody) fails.push('is_guest not in body'); if(checks.oldLocalPush) fails.push('legacy mkSp(Date.now()) push still present'); if(checks.labelTernary < 5) fails.push('(Gast) label ternary count='+checks.labelTernary+' expected >= 5 (list + BK + VG + 2x FJ)'); if(fails.length){console.error('FAIL: '+fails.join('; ')); process.exit(1)} console.log('OK frontend wired ('+checks.labelTernary+' ternaries)')"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `public/index.html` contains a checkbox with `id="m-sp-gast"`
    - source-assertion: `public/index.html` contains the declaration `async function addSpieler`
    - source-assertion: `public/index.html` contains a `fetch('/api/players'` call inside `addSpieler`
    - source-assertion: The fetch body contains the JSON serialization including `is_guest:isGast` (the variable name from the checkbox read)
    - source-assertion: `public/index.html` does NOT contain `S.spieler.push(mkSp(Date.now()` any more (legacy local-only push removed)
    - source-assertion: After successful POST, `document.getElementById('m-sp-gast').checked=false` is called (checkbox reset)
    - source-assertion: The ternary pattern `s.is_guest?s.name+' (Gast)':s.name` appears AT LEAST 5 times (renderSpielerListe + renderBKWahl + renderVGWahl + renderFJWahl-fuchs + renderFJWahl-jaeger)
    - behavior: When the page loads after `/api/players` returns players with `is_guest: 1`, every Gast appears with " (Gast)" suffix in the Spielerliste, BK-modal, VG-modal, and FJ-modal
  </acceptance_criteria>
  <done>
    Frontend kann Gäste anlegen, zeigt sie korrekt mit Label in allen 4 Render-Punkten an, und schickt sie persistent ans Backend. Zusammen mit Plan 10-01 (Stats-Ausschluss) und Task 2 (Auto-Archivierung) ist der komplette Gastkegler-Flow funktional.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/players | requireSession-Auth; `is_guest` aus untrusted Body |
| client → POST /api/abende/:id/end | requireSession-Auth; id-Param wird via Number() + Number.isInteger validiert |
| browser → addSpieler() → server | same-origin fetch, Session-Cookie automatisch mitgesendet |
| frontend render → DOM | bestehende innerHTML-Templates (Projekt-Pattern); keine neuen XSS-Surfaces durch hardcoded "(Gast)"-String |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | POST /api/players `is_guest` Body-Field | mitigate | Coercion `is_guest ? 1 : 0` in players.js Task 1 — beliebige truthy/falsy-Werte werden auf 0/1 normalisiert, parameterisierte INSERT verhindert SQLi |
| T-10-06 | Elevation of Privilege | Unauth user could mark guests | mitigate | POST /api/players ist hinter `requireSession` (unverändert), nur authentifizierte Spielleitung |
| T-10-07 | Information Disclosure | XSS via Spielername mit "(Gast)"-Suffix | accept | `(Gast)`-String ist hardcoded Literal; `s.name` wird wie im Bestand via innerHTML in Template-Literal injiziert — kein neuer Surface, projekt-konformes Pattern (CLAUDE.md erlaubt textContent-only nur für DOM-API-Pfade) |
| T-10-08 | Denial of Service | Massen-Gast-Erzeugung füllt DB | accept | Auth-Gate + Vereinsumfeld; SQLite-Volumen unkritisch (<5MB lifetime per CLAUDE.md) |
| T-10-09 | Tampering | Gast-Archive UPDATE könnte versehentlich reguläre Spieler treffen | mitigate | WHERE-Clause hardcoded `is_guest = 1 AND archived = 0` — reguläre Spieler (`is_guest = 0`) werden niemals erfasst |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — keine neuen Package-Installs in dieser Plan |
</threat_model>

<verification>
1. **Syntax-Check:**
   - `node -c server/db/index.js`
   - `node -c server/routes/players.js`
   - `node -c server/routes/abende.js`
2. **Server-Start:** `node server/index.js` startet ohne Migration-Fehler.
3. **DB-Inspect:** `sqlite3 data/kegelclub.db "PRAGMA table_info(players);"` zeigt `is_guest` Spalte.
4. **API-Smoke:**
   - `curl -X POST http://localhost:3000/api/players -H "Content-Type: application/json" -b "cookie" -d '{"name":"Testgast","emoji":"🦊","is_guest":true}'` → 201 mit `is_guest:1`
   - `curl http://localhost:3000/api/players` → Testgast erscheint mit `is_guest:1`
5. **Frontend-Smoke:** Im Browser den "Spieler hinzufügen"-Dialog öffnen, Checkbox aktivieren, hinzufügen — Player erscheint in Spielerliste mit "(Gast)"-Suffix.
6. **Game-Modal-Smoke:** Drei Modals öffnen (BK, VG, FJ) — Gastspieler-Chip zeigt "(Gast)" hinter dem Namen.
7. **Auto-Archive-Smoke:** Abend beenden (POST /api/abende/:id/end) → GET /api/players zeigt Gast nicht mehr (weil `archived = 1`).
8. **Stats-Integration (zusammen mit Plan 10-01):** GET /api/stats zeigt Gast nicht im `players`-Array.
</verification>

<success_criteria>
- [ ] `players.is_guest` Spalte existiert nach Server-Restart (`PRAGMA table_info(players)`)
- [ ] Zweiter Server-Start läuft ohne Fehler durch (idempotente Migration)
- [ ] `GET /api/players` Response enthält Feld `is_guest` (0 oder 1)
- [ ] `POST /api/players` mit `is_guest:true` legt Gast an
- [ ] `POST /api/abende/:id/end` archiviert alle aktiven Gäste in einem zweiten UPDATE
- [ ] Frontend-Dialog hat Checkbox `#m-sp-gast`
- [ ] `addSpieler()` ist `async`, postet an API und nutzt die Server-Response (kein `mkSp(Date.now()…)` mehr)
- [ ] `(Gast)`-Suffix erscheint in allen 4 Render-Punkten (Spielerliste, BK, VG, FJ)
- [ ] Spieldaten archivierter Gäste bleiben in `game_players` und `throws` erhalten (kein DELETE)
</success_criteria>

<output>
Create `.planning/phases/10-team-gewinner-gastkegler/10-02-SUMMARY.md` when done. Include:
- Exact diff snippets for db/index.js migrations-Array addition
- Diff for players.js GET + POST handlers
- Diff for abende.js POST /:id/end handler
- Diff for the 4 frontend edits (dialog, addSpieler, renderSpielerListe, 3 render*Wahl functions)
- Verification command outputs (syntax checks, optional curl smoke)
- Confirmation that PRAGMA shows the new column
</output>
