---
phase: 10-team-gewinner-gastkegler
plan: 01
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - server/routes/stats.js
autonomous: true
requirements:
  - STATS-01
  - STATS-02
  - GUEST-03
tags:
  - stats
  - leaderboard
  - bugfix
must_haves:
  truths:
    - "Ein Viergewinnt-Sieg eines Teams zählt jedem Team-Mitglied als Sieg im Leaderboard (nicht mehr als Draw)"
    - "Ein Fuchsjagd-Sieg der Jäger zählt jedem Jäger als Sieg im Leaderboard"
    - "Ein echter VG-Draw (volles Grid, 0 Gewinner) zählt weiterhin als Draw für alle Spieler"
    - "Historische VG/FJ-Spiele zeigen nach Server-Neustart korrekte Siege ohne DB-Migration"
    - "Gastkegler erscheinen weder in /api/stats noch in /api/stats/year Leaderboards"
    - "Mehrere Gewinner in H2H zählen jedem beteiligten Spieler als Win"
    - "Streak eines Spielers wird bei VG/FJ-Team-Sieg erhöht (nicht durch falschen Draw-Status zurückgesetzt)"
  artifacts:
    - path: "server/routes/stats.js"
      provides: "Korrekte isDraw-Semantik in allen 4 Endpoints + Guest-Ausschluss in 2 Player-Queries"
      contains: "winners.length === 0"
  key_links:
    - from: "stats.js /api/stats"
      to: "winners.length === 0 Semantik + AND is_guest = 0 in Player-Query"
      via: "drop-in replacement von !== 1 → === 0 und SELECT-Erweiterung"
      pattern: "winners\\.length === 0"
    - from: "stats.js /api/stats/year"
      to: "iteration über alle Gewinner statt winners[0]"
      via: "for (const w of winners) winsMap[w.playerId]++"
      pattern: "for \\(const w of winners\\)"
    - from: "stats.js /api/stats/h2h"
      to: "winners.some(w => w.playerId === a/b) statt winners[0].playerId"
      via: "Multi-Winner-tauglicher Check für beide Spieler"
      pattern: "winners\\.some\\("
---

<objective>
Fixes the `isDraw` bug in `server/routes/stats.js` that miscounts Viergewinnt- und Fuchsjagd-Team-Siege als Unentschieden, and extends the player-Queries in `/api/stats` and `/api/stats/year` to exclude Gastkegler.

Purpose: STATS-01 (Team-Siege korrekt zählen), STATS-02 (historische Spiele rückwirkend richtig — kostenlos, weil Stats query-time computed sind), GUEST-03 (Gäste nicht im Leaderboard).
Output: `server/routes/stats.js` mit korrekter Multi-Winner-Logik in allen 4 Endpoints (`/api/stats`, `/api/stats/year`, `/api/stats/streaks`, `/api/stats/h2h`) und `is_guest = 0` Guard in den beiden Player-Queries.
</objective>

<artifacts_this_phase_produces>
Symbols created or behaviorally changed by Plan 10-01:

- `server/routes/stats.js` — `isDraw` semantics changed from `winners.length !== 1` to `winners.length === 0` in 4 endpoints
- `server/routes/stats.js` — `/api/stats/year` per-game accumulation: `winsMap[w.playerId]++` loop over all winners (replaces `winsMap[winners[0].playerId]++`)
- `server/routes/stats.js` — `/api/stats/h2h` win attribution: `winners.some(w => w.playerId === a/b)` (replaces `winners[0].playerId === a/b`)
- `server/routes/stats.js` — `/api/stats` Player-SELECT extended with `AND is_guest = 0`
- `server/routes/stats.js` — `/api/stats/year` Player-SELECT extended with `AND is_guest = 0`

Symbols NOT changed (out-of-scope for this plan):
- `is_guest` column itself (created in Plan 10-02)
- `/api/stats/streaks` and `/api/stats/h2h` Player-SELECTs (they don't load standalone player rows — they work from `game_players` joins; the isDraw fix alone is sufficient because guests are still archived after their abend ends)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-team-gewinner-gastkegler/10-CONTEXT.md
@.planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md
@server/routes/stats.js
@server/game-types/vier-gewinnt.js
@server/game-types/fuchsjagd.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: isDraw-Fix in allen 4 Stats-Endpoints (per D-01, D-02)</name>
  <files>server/routes/stats.js</files>
  <read_first>
    - server/routes/stats.js (current implementation of /api/stats, /api/stats/year, /api/stats/streaks, /api/stats/h2h)
    - server/game-types/vier-gewinnt.js (getFinalResults — returns `winner: true` for ALL members of winning team; returns all `winner: false` only on draw)
    - server/game-types/fuchsjagd.js (getFinalResults — multi-winner case when hunters win)
    - .planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md (drop-in replacements section)
  </read_first>
  <behavior>
    - VG-Team-Sieg (Team X gewinnt 4-in-a-row, 2 Mitglieder → `winners.length === 2`): jedes Mitglied erhält +1 Win, Team O Mitglieder erhalten +1 Loss; isDraw = false
    - FJ-Jäger-Sieg (Fuchs gefangen, 10 Jäger → `winners.length === 10`): jeder Jäger erhält +1 Win, Fuchs erhält +1 Loss; isDraw = false
    - VG-Echter-Draw (volles Grid, keine 4er-Reihe → `winners.length === 0`): alle Spieler erhalten +1 Draw; isDraw = true
    - Single-Winner-Spiele (Drei in die Vollen, etc., `winners.length === 1`): Verhalten unverändert (1 Win, alle anderen Loss)
  </behavior>
  <action>
    In `server/routes/stats.js` an genau 4 Stellen die isDraw-Zeile ersetzen:
    1. `/api/stats` Endpoint, Zeile 59: `const isDraw = winners.length !== 1;` → `const isDraw = winners.length === 0;`
    2. `/api/stats/year` Endpoint, Zeile 206: identische Ersetzung
    3. `/api/stats/streaks` Endpoint, Zeile 266: identische Ersetzung
    4. `/api/stats/h2h` Endpoint, Zeile 321: identische Ersetzung
    Keine weiteren Änderungen in diesem Task — die Iteration-/some-Logik kommt in Task 2. Reine semantische Korrektur: 0 Gewinner = Draw, ≥1 Gewinner = Sieg(e). Kommentar in Zeile 56-57 (`isDraw = winners.length !== 1` Comment-Block) sinngemäß anpassen oder entfernen.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('server/routes/stats.js','utf8'); const non=t.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); const bad=(non.match(/winners\.length !== 1/g)||[]).length; const good=(non.match(/winners\.length === 0/g)||[]).length; if(bad!==0||good!==4){console.error('FAIL bad='+bad+' good='+good); process.exit(1)} console.log('OK 4x isDraw fixed')"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `server/routes/stats.js` contains EXACTLY 4 non-comment occurrences of `winners.length === 0`
    - source-assertion: `server/routes/stats.js` contains ZERO non-comment occurrences of `winners.length !== 1`
    - source-assertion: All 4 occurrences appear inside the 4 endpoint handlers (`/`, `/year`, `/streaks`, `/h2h`)
    - test-command: `node -c server/routes/stats.js` exits 0 (no syntax error)
  </acceptance_criteria>
  <done>
    isDraw bedeutet jetzt überall: `winners.length === 0` (echte Draws). Spiele mit 1+ Gewinnern werden in Task 2 dann auch korrekt attribuiert.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Multi-Winner-Attribution für Year-Leaderboard und H2H (per D-03, D-04)</name>
  <files>server/routes/stats.js</files>
  <read_first>
    - server/routes/stats.js (current `/api/stats/year` lines 205-211 and `/api/stats/h2h` lines 320-330)
    - .planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md (Year-leaderboard additional fix + H2H additional fix sections)
  </read_first>
  <behavior>
    - Year-Leaderboard: VG-Team-Sieg → jedes Team-Mitglied bekommt einen Eintrag im winsMap (statt nur `winners[0]`)
    - Year-Leaderboard: FJ-Jäger-Sieg → jeder Jäger bekommt einen Eintrag im winsMap
    - Year-Leaderboard: Single-Winner-Spiel → unverändertes Verhalten (Loop iteriert über 1 Element)
    - H2H zwischen A und B: VG-Spiel in dem beide Teil des Sieger-Teams sind → BEIDE bekommen +1 Win
    - H2H zwischen A und B: VG-Spiel in dem nur A im Sieger-Team ist → nur A bekommt +1 Win, B verliert (kein Eintrag, da B nicht zu den Gewinnern zählt)
    - H2H zwischen A und B: VG-Spiel mit `winners.length === 0` → +1 draw
  </behavior>
  <action>
    In `server/routes/stats.js`:
    1. `/api/stats/year` (Zeilen 205-210): Block ersetzen so dass nach `if (isDraw) continue;` eine Iteration über alle Gewinner stattfindet: `for (const w of winners) { winsMap[w.playerId] = (winsMap[w.playerId] || 0) + 1; }`. Damit entfällt `const winnerId = winners[0].playerId;` und `winsMap[winnerId]++`.
    2. `/api/stats/h2h` (Zeilen 320-329): Die `else if` Kette ersetzen durch zwei unabhängige `if`-Checks: `if (winners.some(w => w.playerId === a)) winsA++;` und `if (winners.some(w => w.playerId === b)) winsB++;`. Wichtig: Beide Checks müssen unabhängig sein (nicht `else if`), damit ein Team-Sieg in dem beide Spieler gewinnen sowohl `winsA++` als auch `winsB++` auslöst.
    3. Verhalten von `/api/stats` (Win/Loss/Draw-Attribution in Zeilen 61-72) und `/api/stats/streaks` (Zeilen 268-277) ist bereits durch den isDraw-Fix aus Task 1 korrekt — die existierende `r.winner`-Iteration deckt Multi-Winner-Fälle automatisch ab. KEIN weiterer Code-Change in diesen beiden Endpoints.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('server/routes/stats.js','utf8'); const hasYear=/for \(const w of winners\) \{[^}]*winsMap\[w\.playerId\]/.test(t); const hasH2H=/winners\.some\(w => w\.playerId === a\)/.test(t) && /winners\.some\(w => w\.playerId === b\)/.test(t); const noLegacy=!/winsMap\[winners\[0\]\.playerId\]/.test(t) && !/winners\[0\]\.playerId === a/.test(t); if(!hasYear||!hasH2H||!noLegacy){console.error('FAIL year='+hasYear+' h2h='+hasH2H+' nolegacy='+noLegacy); process.exit(1)} console.log('OK multi-winner attribution')"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `server/routes/stats.js` contains the pattern `for (const w of winners)` followed by `winsMap[w.playerId]` inside `/api/stats/year`
    - source-assertion: `server/routes/stats.js` contains both `winners.some(w => w.playerId === a)` AND `winners.some(w => w.playerId === b)` inside `/api/stats/h2h`
    - source-assertion: `server/routes/stats.js` does NOT contain `winsMap[winners[0].playerId]` (legacy single-winner code removed)
    - source-assertion: `server/routes/stats.js` does NOT contain `winners[0].playerId === a` (legacy single-winner check removed)
    - source-assertion: The two H2H checks are independent `if` statements (not `else if`) — both must be able to execute on the same iteration
    - test-command: `node -c server/routes/stats.js` exits 0
  </acceptance_criteria>
  <done>
    `/api/stats/year`-Response zeigt für ein VG-Team-Sieg jedem Team-Mitglied einen Win-Increment; `/api/stats/h2h` zeigt für ein Team-Sieg in dem beide H2H-Spieler gewinnen sowohl `wins_a++` als auch `wins_b++`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Guest-Ausschluss in Player-Queries für /api/stats und /api/stats/year (per D-11, GUEST-03)</name>
  <files>server/routes/stats.js</files>
  <read_first>
    - server/routes/stats.js (current player SELECTs at line 19 and line 214)
    - .planning/phases/10-team-gewinner-gastkegler/10-PATTERNS.md (player query guard section)
    - .planning/phases/10-team-gewinner-gastkegler/10-CONTEXT.md (D-11: Guests immer ausgeschlossen, auch unarchiviert)
  </read_first>
  <behavior>
    - GET /api/stats: Spieler mit `is_guest = 1 AND archived = 0` (aktiver Gast im laufenden Abend) erscheinen NICHT im `players`-Array der Response
    - GET /api/stats: Spieler mit `is_guest = 0 AND archived = 0` (reguläre aktive Spieler) erscheinen wie bisher
    - GET /api/stats/year: Gast-IDs erscheinen nicht im `leaderboard`-Array (auch wenn sie Spiele gespielt haben in dem Jahr)
    - Das `winsMap` selbst akkumuliert weiterhin Wins für Gast-IDs (da die game-iteration nicht gefiltert wird) — diese Einträge werden aber beim `.map(p => ...)` über die gefilterten `players` einfach nicht ausgegeben. Verhalten ist korrekt: Gäste sind nicht im Leaderboard sichtbar.
  </behavior>
  <action>
    In `server/routes/stats.js` exakt zwei SELECT-Statements erweitern:
    1. `/api/stats` Endpoint, Zeile 19: `'SELECT id, name, emoji FROM players WHERE archived = 0 ORDER BY id ASC'` → `'SELECT id, name, emoji FROM players WHERE archived = 0 AND is_guest = 0 ORDER BY id ASC'`
    2. `/api/stats/year` Endpoint, Zeile 214: `'SELECT id, name, emoji FROM players WHERE archived = 0'` → `'SELECT id, name, emoji FROM players WHERE archived = 0 AND is_guest = 0'`
    Keine anderen Stellen in `stats.js` ändern. `/api/stats/streaks` und `/api/stats/h2h` arbeiten über `game_players`-Joins und haben keine standalone Player-Query — diese sind bewusst nicht zu ändern (Gäste werden nach Abend-Ende archiviert, sodass historische Streaks/H2H-Daten weiterhin abrufbar bleiben).
    Voraussetzung: Plan 10-02 fügt die Spalte `is_guest INTEGER NOT NULL DEFAULT 0` zu `players` hinzu. Dieser Plan setzt voraus, dass die Migration läuft, BEVOR der Server startet (db/index.js migrations-Array → laufen synchron beim require('../db')).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('server/routes/stats.js','utf8'); const m=t.match(/FROM players WHERE archived = 0/g)||[]; const guarded=t.match(/FROM players WHERE archived = 0 AND is_guest = 0/g)||[]; if(guarded.length<2){console.error('FAIL only '+guarded.length+'/2 player queries guarded'); process.exit(1)} console.log('OK '+guarded.length+' guest-guards present')"</automated>
  </verify>
  <acceptance_criteria>
    - source-assertion: `server/routes/stats.js` contains the substring `FROM players WHERE archived = 0 AND is_guest = 0 ORDER BY id ASC` (the `/api/stats` query)
    - source-assertion: `server/routes/stats.js` contains the substring `FROM players WHERE archived = 0 AND is_guest = 0` at least 2 times total (covers `/api/stats` and `/api/stats/year`)
    - source-assertion: `server/routes/stats.js` does NOT contain a player-table SELECT with `archived = 0` that is NOT followed by `AND is_guest = 0` (other than inside string comments/docs)
    - test-command: `node -c server/routes/stats.js` exits 0
  </acceptance_criteria>
  <done>
    Beide Player-Queries filtern Gäste aus. Nach Plan 10-02 (DB-Migration + API-Erweiterung) werden Gäste im Leaderboard nicht mehr angezeigt — auch nicht während des aktiven Abends.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/stats* | Public read-only endpoints; no input mutation, no auth required |
| stats.js → SQLite | All queries use prepared statements with bound params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `/api/stats/h2h` query params `a`, `b` | mitigate | Existing validation: `Number(req.query.a)` + `Number.isInteger` + positive check (unverändert, kein neuer Surface) |
| T-10-02 | Information Disclosure | Stats leak guest-player names via `/api/stats` | mitigate | Plan 10-01 Task 3: `AND is_guest = 0` Guard in player SELECTs |
| T-10-03 | Tampering | `/api/stats/year` query param `year` | mitigate | Existing `/^\d{4}$/` regex check (unverändert) |
| T-10-04 | Denial of Service | Stats computation iterates ALL finished games | accept | Club workload: <1000 Spiele lifetime; SQLite latency <10ms. Kein Cache nötig. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — keine neuen Package-Installs in dieser Plan |
</threat_model>

<verification>
1. Run `node -c server/routes/stats.js` to ensure no syntax errors.
2. Start the server: `node server/index.js` (or via pm2/nodemon).
3. Manuelles Smoke-Test:
   - `curl http://localhost:3000/api/stats` → Response enthält `players[]` ohne Gäste
   - `curl http://localhost:3000/api/stats/year?year=2026` → Leaderboard ohne Gäste
   - `curl http://localhost:3000/api/stats/streaks` → Streaks pro player_id; VG-Team-Sieg setzt nicht zurück
   - `curl "http://localhost:3000/api/stats/h2h?a=1&b=2"` → wins_a/wins_b/draws korrekt, beide bekommen Win wenn beide Sieger
4. Optional E2E (UAT): Im laufenden System ein VG-Spiel mit Team-Sieg starten und prüfen, dass beide Sieger im Leaderboard +1 Win bekommen.
</verification>

<success_criteria>
- [ ] `winners.length === 0` ist die isDraw-Semantik in allen 4 Endpoints (4 Occurrences exakt)
- [ ] `winners.length !== 1` kommt nicht mehr vor (außerhalb von Kommentaren)
- [ ] `/api/stats/year` iteriert über alle `winners` per `for…of`
- [ ] `/api/stats/h2h` verwendet `winners.some(...)` für beide A- und B-Checks (unabhängige `if`s, nicht `else if`)
- [ ] Player-Queries in `/api/stats` und `/api/stats/year` enthalten `AND is_guest = 0`
- [ ] Server startet ohne Errors (`node server/index.js`)
- [ ] STATS-02 ist automatisch erfüllt: Stats werden query-time berechnet, alle historischen VG/FJ-Spiele zeigen jetzt korrekte Werte
</success_criteria>

<output>
Create `.planning/phases/10-team-gewinner-gastkegler/10-01-SUMMARY.md` when done. Include:
- Exact diff snippets for the 4 isDraw fixes
- The /year iteration replacement
- The /h2h .some() replacement
- The 2 player-query guards
- Confirmation `node -c server/routes/stats.js` passes
- Note that STATS-02 is satisfied without DB migration
</output>
