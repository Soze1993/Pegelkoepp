---
phase: 07-highlights-tv-layouts
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - Claude/server/routes/highlights.js
  - Claude/server/app.js
  - Claude/server/routes/games.js
  - Claude/server/server.js
autonomous: true
requirements:
  - HIGHLIGHT-01
  - HIGHLIGHT-02
  - HIGHLIGHT-03
  - HIGHLIGHT-04
must_haves:
  truths:
    - "GET /api/highlights/current returns the current KDA champion player object (id, name, emoji)"
    - "GET /api/highlights/current returns the current BK loser player object (id, name, emoji) derived from lowest bkTotal"
    - "GET /api/highlights/current returns { kda_champion: null, bk_loser: null } when no finished games of those types exist"
    - "game:finished socket event payload includes typeKey field equal to the game's type_key string"
    - "game:state socket event payload includes type_key field so TV can cache currentTypeKey on reconnect"
  artifacts:
    - path: "Claude/server/routes/highlights.js"
      provides: "GET /api/highlights/current — queries last finished KDA/BK games from DB"
      contains: "router.get('/current'"
    - path: "Claude/server/app.js"
      provides: "Route registration for /api/highlights"
      contains: "/api/highlights"
    - path: "Claude/server/routes/games.js"
      provides: "typeKey enrichment in game:finished emission"
      contains: "typeKey: game.type_key"
    - path: "Claude/server/server.js"
      provides: "type_key enrichment in game:state emission"
      contains: "type_key: game.type_key"
  key_links:
    - from: "highlights.js"
      to: "games table"
      via: "db.prepare WHERE type_key = 'kda' AND status = 'finished' ORDER BY finished_at DESC LIMIT 1"
      pattern: "type_key.*kda.*status.*finished"
    - from: "highlights.js"
      to: "reconstructState"
      via: "require('./games').reconstructState"
      pattern: "reconstructState"
    - from: "games.js game:finished emission"
      to: "tv.js game:finished handler"
      via: "io.to(game:X).emit('game:finished', { ..., typeKey: game.type_key })"
      pattern: "typeKey.*game\\.type_key"
---

<objective>
Implement the backend foundation for Phase 7: the new `/api/highlights/current` endpoint and the `typeKey` enrichment on game-related Socket.io events.

Purpose: Wave 1 — make all 5 Wave 0 test stubs go GREEN. Also add `type_key` to `game:state` so the TV's `currentTypeKey` cache is populated on page reload (addresses Pitfall 1 from RESEARCH.md).
Output: `server/routes/highlights.js` (new file), `server/app.js` (+1 line), `server/routes/games.js` (+1 field on line 195), `server/server.js` (+1 field in game:state emission).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-CONTEXT.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-RESEARCH.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-PATTERNS.md

<interfaces>
<!-- Analog: server/routes/stats.js (exact structural model for highlights.js) -->
<!-- Source: Claude/server/routes/stats.js -->
Header:
  'use strict';
  const { Router } = require('express');
  const db = require('../db');
  const gameTypes = require('../game-types');
  const { reconstructState } = require('./games');
  const router = Router();

Query pattern:
  db.prepare("SELECT id, type_key FROM games WHERE type_key = 'kda' AND status = 'finished' ORDER BY finished_at DESC LIMIT 1").get()
  db.prepare("SELECT id, type_key FROM games WHERE type_key = 'bilderkegel' AND status = 'finished' ORDER BY finished_at DESC LIMIT 1").get()

State reconstruction (stats.js lines 36–53 pattern):
  try { state = reconstructState(game); } catch (e) { /* non-fatal skip */ }

KDA winner: state.gewinner = { id, name, emoji } — set when tournament completes (kegler-des-abends.js lines 383, 441).
BK loser: derived by getBKLoserId(state) — sort players by sum of bildPts, return lowest. Use:
  (p.bildPts || []).reduce((a, b) => a + (b !== null ? b : 0), 0)

Player lookup:
  db.prepare('SELECT id, name, emoji FROM players WHERE id = ?').get(playerId)

Response shape:
  res.json({ kda_champion: null | { id, name, emoji }, bk_loser: null | { id, name, emoji } })

No auth guard — public endpoint matching stats.js pattern.
Export: module.exports = router;

<!-- app.js registration pattern (lines 62–67): -->
  app.use('/api/highlights', require('./routes/highlights'));
  — add after app.use('/api/stats', ...) and before the error middleware block.

<!-- games.js game:finished emission (line 195 — EXACT CURRENT): -->
  io.to(`game:${gameId}`).emit('game:finished', { state: newState, lastWinner });
  — change to: { state: newState, lastWinner, typeKey: game.type_key }
  game.type_key is in scope: game was queried on line 133.

<!-- server.js game:state emission: -->
  Read server.js to find the game:state emit. It currently emits { gameId, state, idle, lastWinner }.
  The game object fetched in server.js (db.prepare('SELECT * FROM games WHERE id = ?').get(activeGame.id))
  includes type_key. Add type_key: game.type_key to the payload object.
  This is the Pitfall 1 fix from RESEARCH.md — TV needs type_key on reconnect.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create highlights.js route and register in app.js</name>
  <files>Claude/server/routes/highlights.js, Claude/server/app.js</files>
  <read_first>
    - Claude/server/routes/stats.js (full file — structural analog; copy 'use strict', Router, db, gameTypes, reconstructState imports and res.json pattern verbatim)
    - Claude/server/app.js (full file — find the route block at lines 62–67, identify correct insertion point after stats before error middleware)
    - Claude/server/routes/games.js (lines 300–320 — verify reconstructState is exported with that exact name)
    - Claude/server/game-types/kegler-des-abends.js (lines 375–445 — verify state.gewinner shape: { id, name, emoji })
    - Claude/server/game-types/bilderkegel.js (lines 1–80 — verify state.players[i].bildPts shape and bkTotal logic: sum bildPts values, null counts as 0)
  </read_first>
  <behavior>
    - GET /api/highlights/current with no finished KDA/BK games returns 200 { kda_champion: null, bk_loser: null }
    - GET /api/highlights/current with a finished KDA game returns 200 with kda_champion: { id, name, emoji } of the gewinner
    - GET /api/highlights/current with a finished BK game returns 200 with bk_loser: { id, name, emoji } of the player with the lowest bkTotal
    - GET /api/highlights/current with multiple finished KDA games returns the winner from the most recent (ORDER BY finished_at DESC LIMIT 1)
  </behavior>
  <action>
    Create `Claude/server/routes/highlights.js` following the stats.js structural pattern.

    File structure:
    1. Header: 'use strict', Router, db, gameTypes, reconstructState imports.
    2. Private helper `getBKLoserId(state)`: maps state.players to `{ id, total }` using bildPts reduce (null → 0), sorts ascending by total, returns tots[0].id or null. If state or state.players is falsy, return null.
    3. `router.get('/current', (req, res) => { ... })` — no auth guard:
       - Initialize `const result = { kda_champion: null, bk_loser: null }`.
       - Query last finished KDA game: type_key = 'kda', status = 'finished', ORDER BY finished_at DESC LIMIT 1 using `.get()`.
       - If found: try { state = reconstructState(kdaGame); if state.gewinner exists, look up player by state.gewinner.id with SELECT id, name, emoji FROM players; set result.kda_champion }. Catch: skip.
       - Query last finished BK game: type_key = 'bilderkegel', same pattern.
       - If found: try { state = reconstructState(bkGame); loserId = getBKLoserId(state); if loserId, look up player; set result.bk_loser }. Catch: skip.
       - `res.json(result)`.
    4. `module.exports = router`.

    In `Claude/server/app.js`, add `app.use('/api/highlights', require('./routes/highlights'));` on the line immediately after `app.use('/api/stats', require('./routes/stats'));`. No other changes.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/routes/highlights.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>
    `node --test server/routes/highlights.test.js` exits 0 — all 4 tests (HL10–HL13) pass GREEN. App starts without errors. No existing tests broken.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich game:finished with typeKey and game:state with type_key</name>
  <files>Claude/server/routes/games.js, Claude/server/server.js</files>
  <read_first>
    - Claude/server/routes/games.js (lines 130–200 — read the full throw-handling block to understand where game variable is queried on line 133 and where game:finished is emitted on line 195; confirm game.type_key is in scope)
    - Claude/server/server.js (full file — find the game:state emit, identify which object is built and emitted, and confirm game row with type_key is available in scope at that point)
  </read_first>
  <behavior>
    - game:finished socket event payload includes `typeKey` string equal to the game's type_key (e.g. 'kda', 'bilderkegel', 'fuchsjagd', 'viergewinnt', 'normalesspiel')
    - game:state socket event payload includes `type_key` string so TV client can set currentTypeKey on reconnect to an already-running game
  </behavior>
  <action>
    In `Claude/server/routes/games.js` line 195 (the game:finished emission):
    Change `{ state: newState, lastWinner }` to `{ state: newState, lastWinner, typeKey: game.type_key }`.
    Do not change anything else on that line or in the surrounding block.
    The `game` variable containing `type_key` is already queried on line 133 within the same function scope — no new DB query needed.

    In `Claude/server/server.js`, find the `game:state` emit. Read the exact code to identify the payload object (currently emits gameId, state, idle, lastWinner fields — verify exact shape before editing). Add `type_key: game.type_key` to that payload object. The `game` DB row (fetched in server.js to get the active game) already includes all columns via SELECT * — verify game.type_key is accessible before adding the field.

    No other changes in either file.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/routes/games.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>
    `node --test server/routes/games.test.js` exits 0 — all tests pass including G-HL01 (typeKey present in game:finished payload). Full suite `node --test` exits 0 with all previously-passing tests still passing plus HL10–HL13 and G-HL01 now GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/highlights/current | Public read-only endpoint; no user input processed |
| server → DB | highlights.js reads games and players tables; no writes |
| server → Socket.io clients | typeKey added to existing game event payloads; value comes from DB games.type_key (trusted internal string) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | GET /api/highlights/current | accept | Endpoint returns only player name/emoji/id — no sensitive data; consistent with existing /api/stats public access pattern |
| T-07-02-02 | Tampering | typeKey in socket event | accept | game.type_key is an internal DB value set by the server at game creation; not derived from any client-supplied input |
| T-07-02-03 | Denial of Service | highlights.js reconstructState | mitigate | try/catch around each reconstructState call — non-fatal skip if a game fails to reconstruct; endpoint always returns 200 |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After Task 1: `cd C:/Users/tobia/Claude && node --test server/routes/highlights.test.js` exits 0 (4/4 GREEN).
After Task 2: `cd C:/Users/tobia/Claude && node --test server/routes/games.test.js` exits 0 (G-HL01 now GREEN).
Full suite: `cd C:/Users/tobia/Claude && node --test` exits 0 — all 396 tests pass (391 prior + 5 new).
Manual check: `node -e "const r = require('./server/routes/highlights'); console.log(typeof r)"` prints 'function' from Claude/ directory.
</verification>

<success_criteria>
- `server/routes/highlights.js` exists with router.get('/current') and getBKLoserId helper
- `server/app.js` contains `app.use('/api/highlights', require('./routes/highlights'))`
- `server/routes/games.js` line 195 emission includes `typeKey: game.type_key`
- `server/server.js` game:state emission includes `type_key: game.type_key`
- `node --test` exits 0 with 396 tests passing (391 prior + 5 new stubs now GREEN)
- GET /api/highlights/current returns { kda_champion: { id, name, emoji } | null, bk_loser: { id, name, emoji } | null }
</success_criteria>

<output>
Create `Claude/.planning/phases/07-highlights-tv-layouts/07-02-SUMMARY.md` when done.
</output>
