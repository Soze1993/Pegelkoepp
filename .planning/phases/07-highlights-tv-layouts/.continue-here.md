---
context: phase
phase: 07-highlights-tv-layouts
task: UAT-in-progress
total_tasks: 24-UAT-tests
status: in_progress
last_updated: 2026-05-24T00:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Fuchsjagd turn order — J→F alternating, not F→all-J. Rule: 2 start throws (Fuchs), then J1→F→J2→F repeating. After Fuchs's 6th jagd throw (8 total), `finalRound=true`. All Jäger get one last throw (one each, in order). Last throw is always a Jäger. This was fixed in commit 0dd29a7 — do NOT revert.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong FJ turn order | Implemented Fuchs-first (F→J1→J2) when rule is Jäger-first (J1→F→J2→F). Caused Fuchs to throw 12× with 2 Jäger instead of 6×. | advisory | Always re-read user rule before touching game-type applyThrow logic |
| Race condition in TV highlights | `renderIdle()` runs before `/api/highlights/current` fetch resolves → bar never rendered. Fixed: `currentIdleLastWinner` tracked, re-render on fetch complete. | advisory | Async fetches on DOMContentLoaded must re-render any already-visible UI |

<current_state>
UAT for Phase 07 is in progress. Plans 05–08 were executed (fixes for BK logic, KDA bracket, TV frontend, undo button, audio). UAT extended to 24 tests (originally 11 from plans 01–04). Session stopped at Test 12 (Fuchsjagd ends after 6 rounds).

Server is running on port 3000. Fuchsjagd end condition was just fixed (commit 0dd29a7) — server needs restart before testing Test 12.

The Drei-Vollen stechen bug (crash on tie) was fixed and confirmed working (Test 17 = pass).
TV highlights bar was fixed (race condition + wrong field names) and repositioned to fixed header next to green dot (Tests 18/21 addressed).

16 gaps remain logged in 07-UAT.md from the original session — many of these have now been addressed by plans 05–08, so during UAT we are re-verifying them via the new test cases 12–24.
</current_state>

<completed_work>

**This session (UAT extension + bug fixes):**
- Extended UAT from 11 to 24 tests (plans 05–08 summaries)
- Fixed Drei-Vollen stechen UI (crash blocker): renderNSpiel stechen branch, doNWurf stechen routing, skipStechenFn(), POST /api/games/:id/skip-stechen endpoint — commit e6c593e
- Fixed TV highlights bar field names (kdaChampionName → kda_champion.name) — commit e6c593e
- Fixed TV highlights bar race condition (renderIdle before fetch) — commit 881783d
- Moved TV highlights to persistent fixed header next to green dot — commit f4e9bb9
- Added KDA-Gewinner/BK-Verlierer labels to header — commit 527f9d2
- Fixed Fuchsjagd end condition (J→F alternating, finalRound flag, 6 jagd Fuchs throws + final Jäger round) — commit 0dd29a7

**UAT results so far (24 total):**
- Tests 1–11 (original): 9 passed, 2 issues (Test 7 Fuchsjagd, Test 8 Viergewinnt board)
- Test 17 (Drei-Vollen stechen): PASS (fixed this session)
- Test 21 (TV highlights bar): needs re-check after fixes
- Tests 12–24 (except 17, 21): still pending
</completed_work>

<remaining_work>

**UAT tests still pending (resume from Test 12):**
- Test 12: Fuchsjagd — ends after 6 rounds ← RESTART SERVER FIRST (fix just committed)
- Test 13: KDA 4-player bracket — L-R1 slot
- Test 14: Bilderkegel — early advance on max (non-Volle)
- Test 15: Bilderkegel — Kleeblatt pin formation
- Test 16: Bilderkegel — winner name in overlay
- Test 18: TV overlay — auto-dismiss (no stuck)
- Test 19: TV — game name header
- Test 20: TV Bilderkegel — Bild display
- Test 21: TV idle — highlights bar (re-verify after fixes)
- Test 22: TV Viergewinnt — real 9×9 board
- Test 23: Undo button in game views
- Test 24: Game-end audio tones

**New bug discovered (not yet investigated):**
- Spieler-Statistik: Pudel und andere Felder werden nicht befüllt — user reported during session, not investigated yet

**Original 16 gaps in 07-UAT.md:** Most addressed by plans 05–08. A few still open:
- BK mandatory stechen (Gleichstand bei BK-Verlierern) — NOT implemented in plans 05–08
</remaining_work>

<decisions_made>

- TV highlights bar: moved from idle-only to persistent fixed header (`position:fixed; top:0.6rem; right:2.5rem`) — visible during all game states, not just idle
- Fuchsjagd end condition: `finalRound` flag set after Fuchs's 6th jagd throw; all Jäger complete one final round, last throw is Jäger. End check in Jäger-phase when `finalRound && jIdx >= jaeger.length`.
- Drei-Vollen stechen: client-side stechen UI added inline in renderNSpiel; server endpoint POST /api/games/:id/skip-stechen added to games.js router.
- Fuchsjagd turn order: Jäger-first (J1→F→J2→F) confirmed correct per user. Previous "Fuchs-first" attempt reverted.
</decisions_made>

<blockers>

- **Pudel-Statistik leer**: User noticed player stats don't show pudel counts. Not investigated — needs a separate debug session.
- **BK mandatory stechen**: Bilderkegel tie for last place requires mandatory tiebreaker — not implemented in any plan. Needs a new fix plan.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-highlights-tv-layouts/07-UAT.md` — current test progress (24 tests, see which are pending)
2. `.planning/STATE.md` — project state

## Infrastructure State
- Server: running on port 3000 (node server/server.js). **Must restart** before testing Fuchsjagd (fix committed but server not reloaded).
- DB: kegelclub.db on disk, persists between restarts
- TV: open at http://localhost:3000/tv.html
- Tablet/App: open at http://localhost:3000

<context>
UAT session for Phase 07 (Highlights & TV-Layouts). We ran into several bugs mid-UAT:
1. Drei-Vollen stechen crash (renderNSpiel reading undefined player) — fixed
2. TV highlights bar not showing (field names wrong + race condition) — fixed and repositioned
3. Fuchsjagd end condition wrong (too many throws) — fixed

User also spotted that player statistics (Pudel etc.) aren't being populated — this is a separate bug to investigate after UAT completes.

The UAT is at Test 12. Fuchsjagd fix just committed, server needs restart before testing.
</context>

<next_action>
1. Restart server: kill PID on port 3000, run `node server/server.js` in Claude/ directory
2. Resume UAT at Test 12 (Fuchsjagd ends after 6 rounds): `/gsd-verify-work 7`
3. After UAT complete: investigate Pudel-Statistik bug separately
</next_action>
