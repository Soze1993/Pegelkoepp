---
context: phase
phase: 07-highlights-tv-layouts
task: UAT-in-progress
total_tasks: 24-UAT-tests
status: in_progress
last_updated: 2026-05-26T00:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Fuchsjagd turn order — J→F per Jäger (interleaved, NOT grouped). After start phase: J1→F→J2→F→... After Fuchs's 6th jagd throw, ONE final Jäger throw ends the game. Fixed in commit aec2721.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong FJ turn order | Grouped all Jäger then Fuchs (J1→J2→F) instead of interleaved (J1→F→J2→F). Now fixed. | advisory | Re-read user rule before touching FJ applyThrow |
| Wrong FJ final round | All Jäger get final throw — WRONG. Only ONE Jäger (next in sequence) gets the final throw after Fuchs's 6th jagd throw. | advisory | Checked and fixed in aec2721 |

<current_state>
UAT for Phase 07 is in progress. Tests 12 and 13 passed this session. Now at Test 14.

A new feature was added mid-UAT: "0 kein Pudel" — a throw that scores 0 but does NOT count as a Pudel (committed aec2721). Server-side all game types updated. UI: kgrid() now shows "0 (kein Pudel)" button. Submit functions (doNWurf, doFJWurf, doBKWurf, doAnkerWurf) pass meta.keinPudel.

KNOWN GAP: renderBKSpiel has its own inline kgrid loop (not calling kgrid()) — "0 (kein Pudel)" button is MISSING in BK throw input. Needs a follow-up fix.

Server must be restarted before testing (aec2721 changes game-type logic).
</current_state>

<completed_work>

**This session:**
- Test 12 PASS: Fuchsjagd ends after 6 rounds (with correct J→F interleaved turn order + one final Jäger throw)
- Test 13 PASS: KDA 4-player bracket — L-R1 slot works
- Fixed FJ turn order: J1→F→J2→F (not J1→J2→F) — committed aec2721
- Fixed FJ end condition: only 1 final Jäger throw after Fuchs's 6th jagd — committed aec2721
- Added FJ TV active-player highlight + remaining throws countdown — committed aec2721
- Added "0 kein Pudel" feature across all game types (server + UI) — committed aec2721
- Updated fuchsjagd.test.js: 13/13 pass incl. new FJ7 multi-Jäger test

**UAT results so far (24 total):**
- Tests 1–11 (original): 9 passed, 2 issues (Test 7 FJ turn order now fixed, Test 8 Viergewinnt pending)
- Test 12: PASS (FJ ends correctly)
- Test 13: PASS (KDA L-R1 bracket)
- Test 17: PASS (Drei-Vollen stechen)
- Tests 14–16, 18–24: still pending
</completed_work>

<remaining_work>

**UAT tests still pending (resume from Test 14):**
- Test 14: Bilderkegel — early advance on max (non-Volle)
- Test 15: Bilderkegel — Kleeblatt pin formation
- Test 16: Bilderkegel — winner name in overlay
- Test 18: TV overlay — auto-dismiss (no stuck)
- Test 19: TV — game name header
- Test 20: TV Bilderkegel — Bild display
- Test 21: TV idle — highlights bar (re-verify after fixes)
- Test 22: TV Viergewinnt — real 9×9 board
- Test 23: Undo button in game views
- Test 24: Game-end audio tones

**Known gaps / follow-up fixes:**
- BK throw input missing "0 kein Pudel" button (renderBKSpiel has inline kgrid loop)
- Pudel-Statistik: player stats don't show pudel counts (reported earlier, not investigated)
- BK mandatory stechen (Gleichstand bei BK-Verlierern) — not implemented
</remaining_work>

<decisions_made>

- FJ turn order: J1→F→J2→F per Jäger (interleaved). Confirmed by user.
- FJ end: after Fuchs's 6th jagd throw, only the NEXT Jäger in rotation gets one final throw. Game ends immediately after.
- "0 kein Pudel": uses meta.keinPudel flag. UI shows "0 (kein Pudel)" button below kgrid. kgrid() updated to reset selKeinPudel on normal button press.
- kleine-hausnummer: if selKeinPudel, don't convert 0→9 client-side AND don't mark as pudel server-side.
</decisions_made>

<blockers>

- **BK inline kgrid missing kP button**: renderBKSpiel (line 918 index.html) has its own inline button loop, doesn't call kgrid(). Fix: replace inline loop with `kgrid(bkK, 'bkSelK')` or add kP button manually.
- **Pudel-Statistik leer**: Player stats don't show pudel counts. Not investigated.
- **BK mandatory stechen**: Not implemented.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-highlights-tv-layouts/07-UAT.md` — current test progress
2. `.planning/STATE.md` — project state

## Infrastructure State
- Server: needs restart (aec2721 changes server-side game-type logic)
- DB: kegelclub.db on disk
- TV: http://localhost:3000/tv.html
- Tablet/App: http://localhost:3000

<next_action>
1. Restart server: kill PID on port 3000, run `node server/server.js` in Claude/ directory
2. Fix BK inline kgrid (missing "0 kein Pudel" button) — small targeted edit in renderBKSpiel
3. Resume UAT at Test 14 (Bilderkegel early advance on max)
</next_action>
