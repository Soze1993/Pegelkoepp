---
phase: 07-highlights-tv-layouts
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - Claude/public/tv.js
autonomous: true
requirements:
  - HIGHLIGHT-02
  - HIGHLIGHT-04
  - TV-01
must_haves:
  truths:
    - "When a KDA game ends, TV shows a full-screen amber overlay with 🏆 and the winner's name for 10 seconds then goes idle"
    - "When a Bilderkegeln game ends, TV shows a full-screen red overlay with 💩 and the loser's name for 10 seconds then goes idle"
    - "TV shows a split Fuchs/Jäger layout when a Fuchsjagd game is active"
    - "TV shows a Team X / VS / Team O layout when a Viergewinnt game is active"
    - "TV shows the Bilderkegeln player list with the lowest-score row highlighted in red during an active game"
    - "TV correctly caches currentTypeKey from game:started and game:state events to drive layout dispatch"
  artifacts:
    - path: "Claude/public/tv.js"
      provides: "renderEndOverlay, renderBilderkegelTV, renderFuchsjagdTV, renderViergewinntTV, currentTypeKey, updated event handlers"
      contains: "renderEndOverlay"
  key_links:
    - from: "tv.js socket.on('game:finished')"
      to: "renderEndOverlay"
      via: "function call with typeKey, state, lastWinner"
      pattern: "renderEndOverlay"
    - from: "tv.js renderGame dispatcher"
      to: "renderBilderkegelTV / renderFuchsjagdTV / renderViergewinntTV"
      via: "currentTypeKey checks before !state.players guard"
      pattern: "currentTypeKey.*bilderkegel|currentTypeKey.*fuchsjagd|currentTypeKey.*viergewinnt"
    - from: "renderEndOverlay"
      to: "renderIdle"
      via: "setTimeout(function() { renderIdle(lastWinner || null); }, 10000)"
      pattern: "renderIdle.*10000"
---

<objective>
Add all TV-side Phase 7 features to `public/tv.js`: end-of-game overlays for KDA and Bilderkegeln, three new game-type-specific TV layouts, the `currentTypeKey` module-level variable, and updated socket event handlers.

Purpose: Wave 2A — TV receives the `typeKey` from Wave 1's backend changes and now uses it to show the right overlay and layout. This is purely client-side JS with no new server dependencies.
Output: `public/tv.js` with ~200 new lines: renderEndOverlay, renderBilderkegelTV, renderFuchsjagdTV, renderViergewinntTV, updated game:started/game:state/game:finished handlers, currentTypeKey variable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-CONTEXT.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-RESEARCH.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-PATTERNS.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-UI-SPEC.md

<interfaces>
<!-- Source: Claude/public/tv.js (all patterns internal — read the full file before editing) -->

Module-level DOM refs (tv.js lines 3–7):
  const connDot      = document.getElementById('connDot');
  const idleEl       = document.getElementById('idle');
  const gameEl       = document.getElementById('game');
  const playerListEl = document.getElementById('playerList');
  const lastWinnerEl = document.getElementById('lastWinnerText');

Add after line 7:
  var currentTypeKey = null;

Current socket handlers (tv.js lines 16–26):
  socket.on('game:state',    ({ idle, state, gameId, lastWinner }) => { ... });
  socket.on('game:started',  ({ state, gameId }) => { socket.emit('join', gameId); renderGame(state); });
  socket.on('game:finished', function({ state, lastWinner }) {
    renderGame(state);
    setTimeout(function() { renderIdle(lastWinner || null); }, 3000);
  });

Replace all three handlers per PATTERNS.md Pattern 3:
  game:started  — add `type_key` to destructuring, set currentTypeKey = type_key before renderGame(state)
  game:state    — add `type_key` to destructuring, set currentTypeKey if type_key is truthy before existing logic
  game:finished — replace body with `renderEndOverlay(typeKey, state, lastWinner)` (destructure typeKey from payload)

Current renderGame (tv.js lines 37–83):
  if (state && state.bracket) { renderKDABracket(state); return; }  // KDA branch
  if (!state || !state.players) return;                               // guard
  idleEl.style.display = 'none'; gameEl.classList.add('active');
  playerListEl.replaceChildren();
  for (const player of state.players) { ... build .player-row li ... }

Extend BEFORE the !state.players guard (CRITICAL — BK has players and falls through):
  if (currentTypeKey === 'bilderkegel') { renderBilderkegelTV(state); return; }
  if (currentTypeKey === 'fuchsjagd')   { renderFuchsjagdTV(state); return; }
  if (currentTypeKey === 'viergewinnt') { renderViergewinntTV(state); return; }

Full-screen takeover pattern (from renderKDABracket lines 103–105):
  idleEl.style.display = 'none';     // step 1
  gameEl.classList.add('active');     // step 2
  var container = document.createElement('div');
  // ... build DOM with textContent only ...
  gameEl.replaceChildren(container);  // step 3

CSS custom properties available: --bg, --card, --ac, --red, --tX, --tO, --mut, --txt, --brd, --fh (Bebas Neue), --fb (DM Sans)

State shapes:
  Fuchsjagd: { fuchs: { id, name, emoji, w: [] }, jaeger: [{ id, name, emoji, w: [] }], fp: Number, done, winner }
  Viergewinnt: { tX: [{ id, name, emoji }], tO: [{ id, name, emoji }], grid: Array(9)[], done, winner }
  Bilderkegeln: { players: [{ id, name, emoji, bildPts: [null*5], pudel }], aktBildIdx, done }

bkTotal helper (for BK loser detection):
  function bkTotal(p) { return (p.bildPts || []).reduce(function(a, b) { return a + (b !== null ? b : 0); }, 0); }

getBKLoserName helper (for end overlay):
  function getBKLoserName(state) {
    if (!state || !state.players || !state.players.length) return '—';
    var withTotals = state.players.map(function(p) {
      return { name: p.name, total: bkTotal(p) };
    });
    withTotals.sort(function(a, b) { return a.total - b.total; });
    return withTotals[0].name;
  }

renderIdle signature: renderIdle(lastWinner) — always call as renderIdle(lastWinner || null)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add currentTypeKey, update socket handlers, add renderEndOverlay and getBKLoserName</name>
  <files>Claude/public/tv.js</files>
  <read_first>
    - Claude/public/tv.js (full file — read completely before making any edits; understand exact line numbers for module-level vars, socket handlers block, and renderGame; note current ES5-leaning function-declaration style used throughout)
    - Claude/public/tv.html (find the #idle, #game, #playerList, #connDot element IDs to confirm DOM refs are correct)
  </read_first>
  <action>
    Edit `Claude/public/tv.js` in three targeted places:

    1. MODULE-LEVEL VARIABLE — After the `const lastWinnerEl = ...` line (the last DOM ref), add:
       `var currentTypeKey = null;`
       This variable is read by renderGame for dispatch and set by game:started and game:state handlers.

    2. SOCKET EVENT HANDLERS — Replace the three handlers (game:state, game:started, game:finished) at lines 16–26 with updated versions that:
       - game:started: destructure `type_key` from payload; set `currentTypeKey = type_key;` before `socket.emit('join', gameId); renderGame(state);`
       - game:state: destructure `type_key` from payload; add `if (type_key) currentTypeKey = type_key;` before the existing `if (idle) renderIdle... else ...` logic
       - game:finished: destructure `typeKey` (capital K — matches backend field added in Plan 02) from payload; replace the entire body with `renderEndOverlay(typeKey, state, lastWinner);`

    3. NEW FUNCTIONS — Append after the closing of renderKDABracket (and its helpers buildTVSlotEl, kdaTVRoundLabel):
       a. `function bkTotal(p)` — sum p.bildPts reduce, null → 0.
       b. `function getBKLoserName(state)` — map players to { name, total }, sort ascending, return withTotals[0].name or '—'. If state/players falsy, return '—'.
       c. `function renderEndOverlay(typeKey, state, lastWinner)` — full-screen takeover per UI-SPEC:
          - Create overlayEl div with class `tv-end-overlay`, cssText: `width:100vw;height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:24px;background:var(--bg);padding:32px 48px;box-sizing:border-box;text-align:center`
          - Create emojiEl div, class `tv-overlay-emoji`, cssText: `font-size:10vw;line-height:1`
          - Create nameEl div, class `tv-overlay-name`, cssText: `font-family:var(--fh,"Bebas Neue",sans-serif);font-size:15vw;line-height:1;font-weight:400`
          - Create subtitleEl div, class `tv-overlay-subtitle`, cssText: `font-family:var(--fb,"DM Sans",sans-serif);font-size:4vw;font-weight:600;line-height:1.2;color:var(--txt)`
          - For typeKey === 'kda': emojiEl.textContent = '🏆'; nameEl.textContent = lastWinner || '—'; nameEl.style.color = 'var(--ac)'; subtitleEl.textContent = '— Kegler des Abends!'
          - For typeKey === 'bilderkegel': emojiEl.textContent = '💩'; nameEl.textContent = getBKLoserName(state); nameEl.style.color = 'var(--red)'; subtitleEl.textContent = '— Bilderkegeln-Verlierer!'
          - For other typeKeys: setTimeout(function() { renderIdle(lastWinner || null); }, 3000); return; (no overlay)
          - Append emojiEl, nameEl, subtitleEl to overlayEl
          - Call idleEl.style.display = 'none'; gameEl.classList.add('active'); gameEl.replaceChildren(overlayEl);
          - setTimeout(function() { renderIdle(lastWinner || null); }, 10000);
          - textContent is used for ALL name/text content — no innerHTML with DB-sourced strings anywhere in this function

    ES5 style note: Use `var` for local variables, `function` declarations (not arrow functions) for the new helpers, consistent with existing tv.js style. `renderEndOverlay` may use an arrow function for the setTimeout callback if that matches the surrounding style — check the existing code for precedent.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test 2>&1 | tail -10</automated>
  </verify>
  <done>
    Full suite passes (same or more tests GREEN). tv.js contains: `var currentTypeKey = null`, `renderEndOverlay` function, `getBKLoserName` function, `bkTotal` function. game:started handler includes `currentTypeKey = type_key`. game:finished handler calls `renderEndOverlay(typeKey, state, lastWinner)` with no setTimeout inline (setTimeout is inside renderEndOverlay). game:state handler sets `if (type_key) currentTypeKey = type_key`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend renderGame dispatcher and add renderBilderkegelTV, renderFuchsjagdTV, renderViergewinntTV</name>
  <files>Claude/public/tv.js</files>
  <read_first>
    - Claude/public/tv.js (after Task 1 edits — read the current renderGame function at its existing line range to get exact code for the insertion point; also check the full file end to know where to append the new renderer functions)
    - Claude/.planning/phases/07-highlights-tv-layouts/07-UI-SPEC.md (Component Inventory section — DOM tree diagrams and cssText values for all three layouts and the Bilderkegeln loser row highlight rule)
  </read_first>
  <action>
    Edit `Claude/public/tv.js` in two targeted places:

    1. RENDERGAME DISPATCHER — Inside the `renderGame(state)` function, add three new branches AFTER the KDA bracket branch (`if (state && state.bracket)`) and BEFORE the `!state.players` guard. Exact insertion:
       `if (currentTypeKey === 'bilderkegel') { renderBilderkegelTV(state); return; }`
       `if (currentTypeKey === 'fuchsjagd')   { renderFuchsjagdTV(state); return; }`
       `if (currentTypeKey === 'viergewinnt') { renderViergewinntTV(state); return; }`
       CRITICAL: These three lines must come before `if (!state || !state.players) return;` — otherwise BK (which has state.players) falls through to the generic renderer.

    2. THREE NEW RENDERER FUNCTIONS — Append after renderEndOverlay/getBKLoserName/bkTotal:

       `function renderBilderkegelTV(state)`:
       - Full-screen takeover: idleEl.style.display = 'none'; gameEl.classList.add('active');
       - Build a container div (width:100vw; height:100vh; background:var(--bg); padding:2vw; box-sizing:border-box)
       - Build a player list using the same .player-row li structure as the existing generic path (reuse the same CSS as existing rows: `player.emoji + ' ' + player.name` via textContent, score via bkTotal)
       - For each player, compute `bkTotal(player)`. Identify the loser: the player with the MINIMUM bkTotal. Loser detection guard: only apply the red highlight if `state.aktBildIdx > 0` OR any player has `bildPts[0] !== null` — if all bildPts entries are null (start of game), no loser highlight (per RESEARCH.md Pitfall 2).
       - Apply loser row styling to the `li` element of the minimum-bkTotal player: `border-left: 4px solid var(--red); background: rgba(224, 82, 82, 0.07); padding-left: calc(2vw - 4px)`
       - Player name uses textContent (never innerHTML with player.name).
       - gameEl.replaceChildren(container)

       `function renderFuchsjagdTV(state)`:
       - Full-screen takeover pattern.
       - Build a flex-row container (width:100vw; height:100vh; background:var(--bg); display:flex; flex-direction:row; align-items:center; padding:2vw; box-sizing:border-box; gap:0)
       - LEFT PANEL (Fuchs): flex:1; background:var(--card); border-radius:12px; padding:24px; display:flex; flex-direction:column; align-items:center; gap:16px
         - Role label div: textContent = 'FUCHS'; fontSize:13px; fontFamily:var(--fb); fontWeight:600; color:var(--mut); letterSpacing:2px
         - Player name div: textContent = state.fuchs.emoji + ' ' + state.fuchs.name; fontSize:36px; fontFamily:var(--fh); color:var(--txt); lineHeight:1
         - Throw list div: textContent = state.fuchs.w.length > 0 ? state.fuchs.w.join(', ') : '—'; fontSize:13px; fontFamily:var(--fb); color:var(--mut)
         - Score div: textContent = 'Noch: ' + String(state.fp); fontSize:72px; fontFamily:var(--fh); color:var(--ac); lineHeight:1
       - VERTICAL DIVIDER: width:1px; background:var(--brd); height:80vh; alignSelf:center; margin:0 24px; flexShrink:0
       - RIGHT PANEL (Jäger): flex:1; background:var(--card); border-radius:12px; padding:24px; display:flex; flex-direction:column; align-items:center; gap:16px
         - Role label div: textContent = 'JÄGER'; same styling as FUCHS label but color:var(--mut)
         - For each jaeger in state.jaeger: build a row div (display:flex; justify-content:space-between; width:100%; padding:8px 0)
           - Name span: textContent = jaeger.emoji + ' ' + jaeger.name; fontSize:28px; fontFamily:var(--fh); color:var(--txt)
           - Contrib span: textContent = String(jaeger.w.reduce(function(a,b){return a+b;}, 0)); fontSize:28px; fontFamily:var(--fh); color:var(--mut)
       - Player names and jaeger names use textContent only.
       - gameEl.replaceChildren(container)

       `function renderViergewinntTV(state)`:
       - Full-screen takeover pattern.
       - Build outer container (width:100vw; height:100vh; background:var(--bg); display:flex; flex-direction:row; align-items:center; justify-content:center; padding:2vw; gap:32px; box-sizing:border-box)
       - TEAM X PANEL: flex:1; background:var(--card); border-radius:12px; padding:24px; border-left:4px solid var(--tX); display:flex; flex-direction:column; gap:8px
         - Label div: textContent = 'TEAM X'; fontSize:28px; fontFamily:var(--fh); color:var(--tX)
         - For each player in state.tX: row div with name span (textContent = p.emoji + ' ' + p.name; fontSize:36px; fontFamily:var(--fh); color:var(--txt)) and score span (textContent = '—'; fontSize:36px; fontFamily:var(--fh); color:var(--tX))
         - If state.winner === 'X': add border:2px solid var(--tX) to the panel. If state.winner === 'O': set panel opacity:0.6.
       - VS CENTER: alignSelf:center; fontSize:72px; fontFamily:var(--fh); color:var(--mut); textContent = 'VS'
       - TEAM O PANEL: same structure as TEAM X but with tO accent and state.tO players. Win/lose opacity logic mirrored.
       - Player names use textContent only.
       - gameEl.replaceChildren(container)

    Style all inline via style.cssText or individual style properties — no new CSS classes to add to tv.html.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test 2>&1 | tail -10</automated>
  </verify>
  <done>
    Full suite still passes. tv.js contains renderBilderkegelTV, renderFuchsjagdTV, renderViergewinntTV functions. renderGame function contains `if (currentTypeKey === 'bilderkegel')`, `if (currentTypeKey === 'fuchsjagd')`, `if (currentTypeKey === 'viergewinnt')` checks in that order, all before the `!state.players` guard. No innerHTML with DB-sourced strings in any new function.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socket.io server → tv.js client | TV receives game events; all player name data rendered via textContent |
| TV DOM | gameEl.replaceChildren replaces all children — no persistent stale DOM accumulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering (XSS) | renderEndOverlay player name | mitigate | nameEl.textContent = lastWinner OR getBKLoserName(state) — never innerHTML; both are textContent-only paths |
| T-07-03-02 | Tampering (XSS) | renderFuchsjagdTV player names | mitigate | All name spans use .textContent; fuchs.name, jaeger[i].name set via textContent only |
| T-07-03-03 | Tampering (XSS) | renderViergewinntTV player names | mitigate | All player name spans use .textContent for emoji + name concatenation |
| T-07-03-04 | Tampering (XSS) | renderBilderkegelTV player names | mitigate | Player row names set via textContent |
| T-07-03-05 | Tampering | currentTypeKey from socket event | accept | type_key is an internal server value from games.type_key — not client-supplied; only controls which renderer function is called |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After Task 1: `cd C:/Users/tobia/Claude && node --test` exits 0 — all tests pass. tv.js contains `renderEndOverlay`, `currentTypeKey`, updated game:finished handler.
After Task 2: `cd C:/Users/tobia/Claude && node --test` exits 0. tv.js contains `renderBilderkegelTV`, `renderFuchsjagdTV`, `renderViergewinntTV`. renderGame contains the three currentTypeKey dispatch branches before the !state.players guard.
Manual TV check (see 07-VALIDATION.md): Start each game type in the app, verify TV shows the correct layout. End a KDA and BK game, verify correct overlay appears for 10 seconds.
</verification>

<success_criteria>
- tv.js contains `var currentTypeKey = null` at module level
- tv.js game:started handler sets currentTypeKey from `type_key` payload field
- tv.js game:state handler sets currentTypeKey when `type_key` is present in payload
- tv.js game:finished handler calls `renderEndOverlay(typeKey, state, lastWinner)` — no inline setTimeout or renderGame call
- tv.js contains `renderEndOverlay(typeKey, state, lastWinner)` that uses textContent for all names
- KDA overlay: amber (--ac) accent, 🏆 emoji, 10-second timeout → renderIdle
- BK overlay: red (--red) accent, 💩 emoji, loser name from getBKLoserName(state), 10-second timeout → renderIdle
- tv.js contains renderBilderkegelTV, renderFuchsjagdTV, renderViergewinntTV
- renderGame contains bilderkegel/fuchsjagd/viergewinnt branches before `!state.players` guard
- `node --test` exits 0 with all 396+ tests passing
</success_criteria>

<output>
Create `Claude/.planning/phases/07-highlights-tv-layouts/07-03-SUMMARY.md` when done.
</output>
