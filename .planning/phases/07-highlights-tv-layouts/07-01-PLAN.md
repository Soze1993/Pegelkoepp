---
phase: 07-highlights-tv-layouts
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - Claude/server/routes/highlights.test.js
  - Claude/server/routes/games.test.js
autonomous: true
requirements:
  - HIGHLIGHT-01
  - HIGHLIGHT-02
  - HIGHLIGHT-03
  - HIGHLIGHT-04
must_haves:
  truths:
    - "Test stubs for GET /api/highlights/current exist and fail (RED) before the route is implemented"
    - "Existing games.test.js is extended with a typeKey assertion that fails (RED) before games.js is patched"
    - "Test stubs cover the null case (no finished games), the KDA champion case, and the BK loser case"
  artifacts:
    - path: "Claude/server/routes/highlights.test.js"
      provides: "Integration test stubs for /api/highlights/current — HL10 through HL13"
      contains: "GET /api/highlights/current"
    - path: "Claude/server/routes/games.test.js"
      provides: "Extended with typeKey assertion on game:finished payload"
      contains: "typeKey"
  key_links:
    - from: "highlights.test.js"
      to: "highlights.js"
      via: "clearCache('./highlights') + require('../app')"
      pattern: "clearCache.*highlights"
    - from: "games.test.js"
      to: "games.js"
      via: "game:finished socket event assertion"
      pattern: "typeKey"
---

<objective>
Write failing test stubs for Phase 7 BEFORE any implementation. This establishes the behavioral contract for the backend work in Wave 1.

Purpose: Wave 0 — the RED phase. Tests must fail now and pass after Wave 1 implements the routes. This prevents silent regressions and makes the contract explicit.
Output: `server/routes/highlights.test.js` (new file, 4 tests) and extended `server/routes/games.test.js` (1 added assertion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-CONTEXT.md
@Claude/.planning/phases/07-highlights-tv-layouts/07-VALIDATION.md

<interfaces>
<!-- Analog for highlights.test.js structure: stats.test.js pattern (verified in 07-PATTERNS.md) -->
<!-- Source: Claude/server/routes/stats.test.js -->

Test framework setup (copy verbatim from stats.test.js lines 1–9):
  'use strict';
  const { test, before, after } = require('node:test');
  const assert = require('node:assert/strict');
  const fs = require('node:fs');
  const os = require('node:os');
  const path = require('node:path');
  const http = require('node:http');
  const bcrypt = require('bcryptjs');

PIN setup — MUST come before app require (stats.test.js lines 13–16):
  const PIN = '1234';
  const PIN_HASH = bcrypt.hashSync(PIN, 10);
  process.env.PIN_HASH = PIN_HASH;

Cache clear modules for before() and after() blocks:
  '../db/index', '../db/seed', '../app', './highlights', './games', './players', './auth', '../middleware/auth', '../game-types/index'

before(): set DB_PATH to tmpDir, SESSION_SECRET = 'test-secret-do-not-use-in-prod', NODE_ENV = 'test'.
  Start http.createServer(app), listen on port 0. No login needed — highlights is public.
after(): server.close(), fs.rmSync(tmpDir, { recursive: true, force: true }).

insertPlayer helper: INSERT INTO players (name, emoji, archived) VALUES (?, ?, ?) — returns lastInsertRowid.
insertFinishedGame helper:
  INSERT INTO games (type_key, status, finished_at) VALUES (?, 'finished', datetime('now'))
  + INSERT INTO game_players (game_id, player_id, seat) for each player
  + INSERT INTO throws (game_id, player_id, throw_index, value, meta) for each throw

Expected response shape: { kda_champion: null | { id, name, emoji }, bk_loser: null | { id, name, emoji } }

<!-- Existing games.test.js: extend with typeKey assertion -->
<!-- Source: Claude/server/routes/games.test.js -->
The games.test.js already has tests for game:finished events. Add an assertion that checks
the event payload includes `typeKey` equal to the game's type_key string.
Test ID: G-HL01 (to distinguish from existing G01–G20 style IDs in the file).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create highlights.test.js with stubs HL10–HL13</name>
  <files>Claude/server/routes/highlights.test.js</files>
  <read_first>
    - Claude/server/routes/stats.test.js (exact structural analog — copy before/after/insertPlayer/insertFinishedGame patterns verbatim)
    - Claude/server/routes/highlights.js (will not exist yet — verify 404 route behavior before route is added)
    - Claude/server/app.js (verify current route registrations — highlights not yet registered)
    - Claude/server/routes/games.js (understand game:finished emission to know what typeKey state shape to inject for KDA/BK throws)
    - Claude/server/game-types/kegler-des-abends.js (understand what throws produce a finished KDA state with state.gewinner)
    - Claude/server/game-types/bilderkegel.js (understand what throws produce a finished BK state — determine bkTotal logic and how many throws complete a BK game)
  </read_first>
  <action>
    Create a new file following the stats.test.js structure exactly (same 'use strict', node:test, before/after, module cache clearing pattern).

    Define `insertPlayer(name, emoji, archived)` and `insertFinishedGame(type_key, playerRows)` helpers matching the stats.test.js pattern. The `playerRows` param is an array of `{ id, throws: [values], meta: [metas] }` where meta is null for most game types.

    Write four tests. All four must fail because `highlights.js` does not exist yet and app.js does not register `/api/highlights`:

    HL10: GET /api/highlights/current returns 200 with `{ kda_champion: null, bk_loser: null }` when no finished games exist in the DB.
    - Insert 2 players, no games. Expect status 200. Expect body.kda_champion === null. Expect body.bk_loser === null.

    HL11: GET /api/highlights/current returns kda_champion after a finished KDA game exists.
    - Insert 2 players. Insert a finished KDA game. Expect body.kda_champion to be a non-null object with fields id, name, emoji.
    - Note: KDA state reconstruction requires throws. Insert enough throws so reconstructState produces a state with `state.gewinner` set. The simplest approach: insert the minimum 4-player KDA game throws that result in a complete tournament (use 4 players, force p1 to win every match by inserting higher pin throws). Assert body.kda_champion.id matches the winner player id.

    HL12: GET /api/highlights/current returns bk_loser after a finished Bilderkegeln game exists.
    - Insert 2 players. Insert a finished BK game with throws that produce distinct bkTotals. The player with the LOWER bkTotal is the loser. Assert body.bk_loser.id matches that player's id.
    - Note: A BK game has 5 images × throws-per-image. Insert enough throws to reconstruct a finished BK state where one player's sum of bildPts is lower than the other's.

    HL13: GET /api/highlights/current returns the MOST RECENT result when multiple finished games of the same type exist.
    - Insert 2 finished KDA games with different winners. Expect body.kda_champion.id matches the winner from the most recent game (later finished_at).

    Each test must independently manage its DB state (do not rely on other tests). Use before() scoped to this file only — stats.test.js pattern with tmpDir isolation.

    After writing the file, run the quick command to confirm tests fail with a "not found" or connection-refused type error (route does not exist). Failure = RED state confirmed.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/routes/highlights.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>
    highlights.test.js exists. Running `node --test server/routes/highlights.test.js` fails — all 4 tests (HL10–HL13) fail because the `/api/highlights/current` route does not exist yet. The failure output shows test errors (not syntax errors). RED state confirmed.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Extend games.test.js with typeKey assertion on game:finished payload</name>
  <files>Claude/server/routes/games.test.js</files>
  <read_first>
    - Claude/server/routes/games.test.js (full file — understand existing test structure, findable test IDs, socket event patterns, and how game:finished is currently asserted)
    - Claude/server/routes/games.js (lines 183–200 — the exact game:finished emission to understand current payload shape `{ state, lastWinner }`)
  </read_first>
  <action>
    Append a new test named `'G-HL01: game:finished event payload includes typeKey equal to the game type_key'` to the existing test file.

    This test should:
    1. Start a game of any type that has a known type_key (any existing type, e.g. 'normalesspiel' or whichever the existing tests already use — do NOT change existing test fixtures).
    2. Complete the game (submit all required throws to finish it) using the existing game-completion helper/pattern already in the file.
    3. Capture the `game:finished` socket event payload received by the socket client.
    4. Assert `payload.typeKey` equals the game's type_key string (e.g. 'normalesspiel' or whatever type is used).

    If the existing games.test.js does not have a socket client pattern to capture events, adapt from how the file currently verifies game state — check if there is a socket.io-client already imported or if tests use HTTP polling instead. Match whatever pattern exists.

    The test must FAIL because games.js line 195 does not currently include `typeKey` in the `game:finished` payload. Assert `payload.typeKey` is a string and matches the game's type — this assertion will fail until Wave 1 patches games.js.

    Do NOT break any existing tests. Only append — do not modify existing tests.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/routes/games.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>
    games.test.js now contains test G-HL01. All existing tests still pass. G-HL01 fails because typeKey is undefined in the game:finished payload. Running `node --test server/routes/games.test.js` shows all prior tests passing and G-HL01 failing. RED state for the new test confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → server routes | Tests call real HTTP routes against an in-memory DB; no production data touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | Test fixtures (player names in DB) | accept | Test DB is ephemeral tmp directory; wiped after each test run |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages — node:test, node:assert, bcryptjs all already in project |
</threat_model>

<verification>
Run `cd C:/Users/tobia/Claude && node --test server/routes/highlights.test.js` — all 4 tests must fail (route does not exist).
Run `cd C:/Users/tobia/Claude && node --test server/routes/games.test.js` — all existing tests pass, G-HL01 fails (typeKey not in payload).
Run `cd C:/Users/tobia/Claude && node --test` — full suite must show 391 passing tests with 5 new failures (4 from highlights + 1 from G-HL01). Zero previously-passing tests must newly fail.
</verification>

<success_criteria>
- `server/routes/highlights.test.js` exists with 4 tests (HL10–HL13)
- `server/routes/games.test.js` contains test G-HL01 at end of file, all prior tests unchanged
- Full suite: 391 passing + 5 failing (the 5 new stubs) — zero regressions
- RED state confirmed: all 5 new tests fail before any Wave 1 implementation
</success_criteria>

<output>
Create `Claude/.planning/phases/07-highlights-tv-layouts/07-01-SUMMARY.md` when done.
</output>
