---
phase: 08-statistiken-rueckblick
plan: 04
type: execute
wave: 2
depends_on: [08-02, 08-03]
files_modified:
  - public/index.html
autonomous: false
requirements: [STATS-01, STATS-02, STATS-03, STATS-04, STATS-05, RECAP-01, RECAP-02]

must_haves:
  truths:
    - "On the Spiele tab, when the database has a closed abend with finished games, a 'Letzter Abend' recap card appears above the finished-games list showing date, KDA winner, BK loser, and a collapsible game list"
    - "On the Stats tab, a year leaderboard card appears at the top with a year picker and a sorted table (rank, player, wins)"
    - "Changing the year picker updates only the leaderboard tbody (not the entire stats tab)"
    - "Each per-player card on the Stats tab shows two new chips: 'Streak' (current, green if > 0) and 'Längster' (longest)"
    - "Below the year leaderboard, a Direkt-Vergleich (H2H) card lets the user select two players and compare wins via the Vergleichen button"
    - "Opening a player detail modal shows two new chips: 'KdA-Titel' (KDA championships count) and 'BK Verlierer' (BK loss count)"
    - "All copy strings match the UI-SPEC.md Copywriting Contract exactly"
    - "Same-player H2H selection shows the inline German validation error and skips the fetch"
  artifacts:
    - path: "public/index.html"
      provides: "Recap card + year leaderboard + H2H selector + streak chips + KDA/BK count chips in player modal"
      contains: "Letzter Abend"
  key_links:
    - from: "renderSpiele() in public/index.html"
      to: "GET /api/abende/last-summary"
      via: "fetch parallel with /api/abende/active + /api/abende + /api/games"
      pattern: "fetch.*\\/api\\/abende\\/last-summary"
    - from: "renderStats() in public/index.html"
      to: "GET /api/stats/year, /api/stats/streaks, /api/stats/kda-counts, /api/stats/bk-counts"
      via: "Promise.all parallel fetches alongside existing /api/stats"
      pattern: "Promise\\.all.*stats\\/year"
    - from: "showSpDetail() in public/index.html"
      to: "S.kdaCounts and S.bkCounts"
      via: "synchronous read from in-memory state"
      pattern: "S\\.kdaCounts\\["
    - from: "year picker <select> onChange"
      to: "GET /api/stats/year?year={value}"
      via: "tbody-only re-render"
      pattern: "select.*year-picker"
---

<objective>
Wave 2 frontend — wire all 7 phase requirements (STATS-01..05 + RECAP-01..02) into the existing tablet UI by extending three functions in public/index.html: renderSpiele() (recap card), renderStats() (year leaderboard, H2H selector, streak chips), and showSpDetail() (KDA/BK count chips). Add three new state slots to S (streaks, kdaCounts, bkCounts).

Purpose: Deliver the UI side of Phase 8 per UI-SPEC.md exactly. All visuals compose from existing CSS classes (no new CSS); all copy matches the Copywriting Contract verbatim; the data plumbing routes through the endpoints implemented by Plans 08-02 and 08-03.

Output: index.html updated; manual UAT confirms all UI-SPEC.md screen layouts render correctly and respond to interactions; full test suite still passes (no test files changed but lint/runtime smoke must remain clean).

Autonomous=false because Task 2 is a human-verify checkpoint covering UI rendering, year picker interaction, H2H validation, and modal counts (none of which have unit tests in this project — manual UAT is the validation per VALIDATION.md Manual-Only Verifications table).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/STATE.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-RESEARCH.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-PATTERNS.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-UI-SPEC.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-VALIDATION.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-02-SUMMARY.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-03-SUMMARY.md
@Claude/public/index.html

<interfaces>
<!-- Existing patterns the executor must reuse. -->

State container `S` is a global with slots populated at init (around line 348-355). New slots to add:
- S.streaks (map: player_id → { current, longest })
- S.kdaCounts (map: player_id → number)
- S.bkCounts (map: player_id → number)

Existing helpers in scope:
- showTab(name) — tab switcher
- renderAll() — calls renderSpiele, renderSpielerListe, renderSpielenTab, renderBib, renderStats
- showSpDetail(id) — opens the player profile modal (line 652)
- mkChip(val, label, extraCls) — chip factory defined inside renderStats() (PATTERNS.md lines 471-484)
- notify(msg, color) — flash message
- openM/closeM — modal open/close
- EMOJIS, today() — utilities

Existing DOM IDs to integrate with:
- #r-spiele (Spiele tab container)
- #r-stats (Stats tab container)
- #m-sp-detail-c (player profile modal content)

CSS classes available (from existing styles — no new classes):
- .card (surface), .card2, .stitle (section title), .wi (wide info row),
- .stbl (stat table with thead/tbody), .chip (.v + .l), .pc (pudel chip variant),
- .rnk (rank number), .rnk.gold (gold accent rank-1),
- .uc (user card), .snum (display number), .empty (empty state),
- .btn .bp (primary button), .fg (form group), .badge .bgry/.bamb

CSS color tokens: --bg, --card, --card2, --bg2, --bg3, --txt, --mut, --brd, --ac, --ac2, --grn, --red, --fh (Bebas Neue), --fb (DM Sans).

Endpoint contracts (already shipped by Plans 08-02 and 08-03 — must read those SUMMARY files before this plan runs):
- GET /api/abende/last-summary → null OR { abend:{id,name,started_at,ended_at}, kda_champion:{id,name,emoji}|null, bk_loser:{id,name,emoji}|null, games:[{id,type_key,finished_at,winner_name,player_count}] }
- GET /api/stats/year?year=YYYY → { year, leaderboard:[{id,name,emoji,wins}], available_years:[string,...] }
- GET /api/stats/streaks → { [player_id]: {current, longest} }
- GET /api/stats/h2h?a=A&b=B → { player_a, player_b, wins_a, wins_b, draws, total }
- GET /api/stats/kda-counts → { [player_id]: number }
- GET /api/stats/bk-counts → { [player_id]: number }

UI-SPEC.md sections that MUST be honored:
- Component Inventory items 1-5 (structure per item)
- Copywriting Contract (every string verbatim — German text, em-dash, emojis)
- Color (accent --ac reserved for the 6 listed elements only — NOT for BK count chip or loss chips)
- Spacing scale (4/8/16/24px only)
- Interaction Contracts (year picker re-renders tbody only; H2H validates A !== B; recap card hidden if endpoint returns null)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend renderStats(), renderSpiele(), and showSpDetail() in public/index.html</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html lines 340-360 (S object init pattern — Promise.all([fetch('/api/players'), fetch('/api/stats')]))
    - public/index.html lines 640-660 (showSpDetail current implementation — innerHTML template ending with Pudel chip)
    - public/index.html lines 744-830 (renderSpiele() — frag pattern, insertion point after banner/startBtn at lines 789/796 and before abende fetch at line 799)
    - public/index.html lines 1610-1710 (renderStats() — existing empty state, fetch, per-player .uc cards, mkChip factory around line 1641, toggle pattern around 1661)
    - public/index.html lines 1720-1740 (renderAll function — confirm renderStats is already invoked)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-UI-SPEC.md (entire file — Component Inventory items 1-5 and Copywriting Contract are the contract)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-PATTERNS.md (index.html section — exact insertion patterns)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-02-SUMMARY.md (confirm endpoint shapes match what's documented in interfaces above)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-03-SUMMARY.md (confirm /api/abende/last-summary shape)
  </read_first>
  <action>
    Edit `public/index.html` in five distinct sections. Use existing DOM-construction style (document.createElement + appendChild for new blocks; innerHTML strings only for showSpDetail extension to match its existing style). All copy must match UI-SPEC.md Copywriting Contract verbatim — every German label, every emoji prefix, every em-dash.

    --- Section A: showSpDetail() KDA/BK chip row (UI-SPEC Component 5) ---

    Modify the innerHTML template inside `showSpDetail(id)` at line 652. The template currently ends with the last grid `'...'+(s.pudel||0)+'</div><div class="l">Pudel '+rate+'%</div></div></div>'`. Append a new 2-column chip row AFTER that closing `</div>` (the one that closes the third grid). The new HTML fragment must be:
    a 2-column grid (display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px) containing two `.chip` elements. Chip 1 has `.v` colored `var(--ac)` with the value `S.kdaCounts && S.kdaCounts[id] || 0` and label `KdA-Titel`. Chip 2 has `.v` colored `var(--red)` with the value `S.bkCounts && S.bkCounts[id] || 0` and label `BK Verlierer`.

    Defensive read: when S.kdaCounts or S.bkCounts is undefined (modal opened before stats tab init), the expression evaluates to 0 — graceful degradation per UI-SPEC.md Component 5.

    --- Section B: renderSpiele() recap card (UI-SPEC Component 1) ---

    Inside `renderSpiele()`, add the recap card AFTER the banner/startBtn appended to frag (after lines 789 or 796), BEFORE the line that starts the existing `var abendeMap = {};` block at line 800.

    Steps:
    1. Fetch recap data: in a try/catch, `var recapRes = await fetch('/api/abende/last-summary'); var recapData = await recapRes.json();`. On any error or recapData === null, skip rendering the entire card (UI-SPEC explicit requirement).
    2. If recapData truthy and recapData.abend exists: build a `div.card` element. Append:
       - `div.stitle` with textContent `Letzter Abend`
       - `div.wi` for date row: left text is `📅 ` + formatted date (parse `recapData.abend.ended_at` and format as `DD.MM.YYYY`); right text in `var(--mut)` shows `recapData.abend.name`. If the formatted parts are missing, fall back to `—`.
       - `div.wi` for KDA row: left text `🏆 Kegler des Abends`; right text either `{emoji} {name}` from recapData.kda_champion OR the em-dash `—` in `var(--mut)` when null.
       - `div.wi` for BK row: left text `💩 BK Verlierer`; right text either `{emoji} {name}` from recapData.bk_loser OR `—` muted when null.
       - A toggle row using the `▼ Spiele des Abends` / `▶ Spiele des Abends` pattern (PATTERNS.md lines 502-511). Click flips the prefix and toggles `panel.style.display` between 'none' and 'block'.
       - Inside the panel (initially hidden), a `table.stbl` with thead `<th>Spieltyp</th><th>Gewinner</th><th>Spieler</th>` and a tbody. If `recapData.games.length === 0` insert one row `<tr><td colspan="3" class="empty" style="padding:16px;text-align:center">Keine Spiele aufgezeichnet</td></tr>`. Otherwise one row per game showing `game.type_key` (use S.typen to map type_key to localized name if found — pattern from existing renderSpiele finished list; otherwise fall back to type_key string), the `game.winner_name || '—'`, and `game.player_count`.
    3. Append the card to `frag` so it lands above the existing finished games list.

    Date formatting helper: write a small inline `function fmtDate(iso)` near the recap block (or reuse if a similar helper already exists by searching for `DD.MM` patterns in index.html). The function takes an ISO/SQLite timestamp string, returns `DD.MM.YYYY` or empty string on parse failure.

    Loading state: per UI-SPEC.md, while fetching, the recap area shows `<div class="empty"><p>Laden…</p></div>`. Implement by inserting a placeholder div into frag before the fetch await; on success, remove or replace it; on failure, leave it (frag never reaches DOM mid-render so simpler: do the fetch BEFORE building the card, and only build the card on success).

    --- Section C: Stats tab data init (UI-SPEC Interaction Contracts) ---

    Inside `renderStats()` (line ~1612), REPLACE the existing single fetch to `/api/stats` with a `Promise.all` that fetches 5 endpoints in parallel:
    - `/api/stats` (existing)
    - `/api/stats/year?year=` + new Date().getFullYear()
    - `/api/stats/streaks`
    - `/api/stats/kda-counts`
    - `/api/stats/bk-counts`

    Convert each response to JSON. On partial failure: render the sections whose data IS available; show inline `Laden fehlgeschlagen.` only inside the failed section's card (NOT replacing the whole #r-stats div). For total failure (all 5 reject), use the existing whole-tab error pattern at lines 1613-1617.

    Store results into S: `S.streaks = streaksData || {}; S.kdaCounts = kdaData || {}; S.bkCounts = bkData || {};`. These slots exist only after renderStats has run — the showSpDetail defensive read covers the case where they have not been initialized yet.

    --- Section D: Year leaderboard card (UI-SPEC Component 2) ---

    At the top of the rendered `#r-stats` content (BEFORE the existing per-player cards), insert a `div.card` containing:
    - A flex row (display:flex;justify-content:space-between;align-items:center;margin-bottom:16px) with a `div.stitle` (margin:0) saying `Jahres-Leaderboard` and a `<select id="year-picker">` (style: width:auto;min-width:80px). Populate options from `yearData.available_years`; if the current year is not in the list, prepend it. Default selected = current year (or yearData.year if returned).
    - A `table.stbl` with thead `<th>#</th><th>Spieler</th><th style="text-align:right">Siege</th>` and a tbody (id `year-leaderboard-tbody`). Populate tbody:
       - Empty leaderboard → one row `<tr><td colspan="3" style="text-align:center;padding:20px;color:var(--mut);font-size:13px">Keine Spiele in {year} abgeschlossen.</td></tr>`.
       - Non-empty → one row per leaderboard entry. First cell: `<td class="rnk{rank === 1 ? ' gold' : ''}">{rank}</td>`. Second cell: `<td>{emoji} {name}</td>`. Third cell: `<td class="snum" style="text-align:right">{wins}</td>`.

    Attach `onchange` on the select: set tbody content to `<tr><td colspan="3" style="text-align:center;padding:12px;color:var(--mut);font-size:13px">Laden…</td></tr>` then fetch `/api/stats/year?year=` + select.value; on response, replace tbody content using the same row builder. Do NOT call renderStats() — surgical tbody-only update per UI-SPEC.md Interaction Contracts.

    --- Section E: H2H card (UI-SPEC Component 4) ---

    Below the year leaderboard card and BEFORE the per-player cards, insert another `div.card` with:
    - `div.stitle` `Direkt-Vergleich`
    - `div.fg`: `<label>Spieler A</label><select id="h2h-a">...</select>` — options from S.spieler (filter !archived), sorted by name, each `<option value="{id}">{emoji} {name}</option>`
    - `div.fg`: `<label>Spieler B</label><select id="h2h-b">...</select>` with same options
    - `<button class="btn bp" style="width:100%">Vergleichen</button>`
    - `<div id="h2h-result"></div>` (initially empty)

    Button onclick handler:
    1. Read selA = parseInt(#h2h-a.value); selB = parseInt(#h2h-b.value).
    2. If selA === selB: set #h2h-result innerHTML to `<div style="color:var(--red);font-size:13px;text-align:center;padding:8px">Bitte zwei verschiedene Spieler wählen.</div>` and return.
    3. Set #h2h-result to `<div style="color:var(--mut);font-size:13px;text-align:center;padding:8px">Laden…</div>`.
    4. Fetch `/api/stats/h2h?a={selA}&b={selB}`; parse JSON.
    5. If response.total === 0: show empty-state inline `<div style="color:var(--mut);font-size:13px;text-align:center;padding:12px">{nameA} und {nameB} haben noch keine gemeinsamen Spiele.</div>` (look up names from S.spieler).
    6. Else: build the head-to-head result block per UI-SPEC.md — a flex row with two columns (each centered: `.snum` value + muted name label) and a centered `vs` separator (color:var(--mut)). Below it, a centered muted line `{draws} Unentschieden · {total} Spiele zusammen`.
    7. On fetch error: `<div style="color:var(--red);font-size:13px;text-align:center;padding:8px">Laden fehlgeschlagen.</div>` (matches existing pattern).

    --- Section F: Per-player streak chips (UI-SPEC Component 3) ---

    Inside renderStats(), in the loop that builds the existing per-player `.uc` cards, locate the chips row that contains Siege/Niederl/Pudel (existing mkChip calls). Append two NEW mkChip calls AFTER the Pudel chip:
    - `mkChip(streakCurrent, 'Streak')` — value is `(S.streaks[player.id] && S.streaks[player.id].current) || 0`; if value > 0 set the resulting chip's `.v` style.color to `var(--grn)`; if value === 0 leave it `var(--mut)` (existing chip muted default).
    - `mkChip(streakLongest, 'Längster')` — value is `(S.streaks[player.id] && S.streaks[player.id].longest) || 0`; default `.v` color is `var(--ac)` (this is the existing mkChip default — confirm by reading PATTERNS.md mkChip body which uses no inline color, so default chip color applies, which is --ac per the existing chip CSS).

    If S.streaks lookup yields undefined (e.g. partial failure of streaks endpoint), show `—` for both chips instead of `0` — graceful failure.

    --- Final integrity checks the executor MUST perform before declaring done ---

    1. No new CSS class declarations. No `<style>` block changes. All visuals compose existing classes.
    2. Every German string from UI-SPEC.md Copywriting Contract appears VERBATIM (no paraphrasing). Search for `Letzter Abend`, `Kegler des Abends`, `BK Verlierer`, `Jahres-Leaderboard`, `Direkt-Vergleich`, `Vergleichen`, `Spieler A`, `Spieler B`, `Streak`, `Längster`, `KdA-Titel`, `Bitte zwei verschiedene Spieler wählen.`, `Keine Spiele aufgezeichnet`, `Spiele des Abends`, `Unentschieden`, `Spiele zusammen`, `Keine Spiele in`, `Laden…`.
    3. `var(--ac)` is used ONLY for: existing tab indicator, .stitle (existing), .chip.v for Siege/Streak-longest/KdA-Titel, .rnk.gold (rank 1), input:focus border (existing), KDA champion text in recap card. NOT used for BK chips or loss chips.
    4. The existing per-player card structure (Siege/Niederl/Pudel chips, Bestleistungen toggle) must remain intact — only ADD new chips alongside.
    5. The existing root `/api/stats` response shape is consumed exactly as before (no breaking changes to the response handling — Plan 08-02 did not modify that endpoint).
    6. Run a manual server reload (`npm start` or whatever the dev command is) and load the page in a browser to confirm no JS console errors at boot.
  </action>
  <verify>
    <automated>node --test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `public/index.html` contains literal string `Letzter Abend`
    - File contains literal string `Jahres-Leaderboard`
    - File contains literal string `Direkt-Vergleich`
    - File contains literal string `Vergleichen`
    - File contains literal string `KdA-Titel`
    - File contains literal string `BK Verlierer`
    - File contains literal string `Bitte zwei verschiedene Spieler wählen.`
    - File contains literal string `Keine Spiele aufgezeichnet`
    - File contains literal string `Spiele des Abends`
    - File contains literal string `Längster`
    - File contains literal string `Streak`
    - File contains `id="year-picker"` (year picker element)
    - File contains `id="h2h-a"` AND `id="h2h-b"` AND `id="h2h-result"`
    - File contains `/api/abende/last-summary` (recap fetch)
    - File contains `/api/stats/year` (year leaderboard fetch)
    - File contains `/api/stats/streaks` (streaks fetch)
    - File contains `/api/stats/kda-counts`
    - File contains `/api/stats/bk-counts`
    - File contains `S.streaks` AND `S.kdaCounts` AND `S.bkCounts` (state slots used)
    - File contains `Promise.all` in renderStats area (parallel fetch)
    - Existing tests still pass: `node --test` exits 0 (no test files modified, but the index.html should not break any server-side test)
    - Server boots without crashing and the page loads in the browser without JS console errors (verified in Task 2)
  </acceptance_criteria>
  <done>
    public/index.html updated per UI-SPEC.md exactly: recap card renders on Spiele tab, year leaderboard + H2H + streak chips render on Stats tab, player profile modal shows KDA/BK count chips. No regressions to existing renderers. Full test suite (`node --test`) still passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — verify Phase 8 UI against UI-SPEC.md</name>
  <what-built>
    All seven Phase 8 requirements wired into the existing tablet UI:
    - STATS-01 year leaderboard with year picker
    - STATS-02 per-player streak chips (current + longest)
    - STATS-03 head-to-head card with Vergleichen button + same-player validation
    - STATS-04 KdA-Titel count chip in player profile modal
    - STATS-05 BK Verlierer count chip in player profile modal
    - RECAP-01 "Letzter Abend" card with date + KDA winner + BK loser
    - RECAP-02 collapsible game list inside the recap card
  </what-built>
  <how-to-verify>
    Pre-conditions:
    1. Ensure the DB has at least one closed abend (`ended_at IS NOT NULL`) with at least one finished KDA game and one finished BK game linked via `abend_id`. If not, seed test data via the existing flow: start an abend, finish a KDA game + a BK game, end the abend.
    2. Start the server (`npm start` or the project's run command — check package.json scripts).
    3. Open the app in a browser tablet emulator at the local URL (default `http://localhost:3000` or whatever PORT is configured).

    Verifications (every one must pass — report any failure with a screenshot or paste of the unexpected content):

    A. Recap card on Spiele tab:
       1. Navigate to the Spiele tab.
       2. Observe a `Letzter Abend` card appears above the finished-games list.
       3. Confirm a 📅 date row shows the closed abend's date in DD.MM.YYYY format and the abend name on the right in muted text.
       4. Confirm a 🏆 row shows `Kegler des Abends` left and the winner emoji + name on the right.
       5. Confirm a 💩 row shows `BK Verlierer` left and the loser emoji + name on the right.
       6. Click the `▼ Spiele des Abends` toggle. Observe the prefix changes to `▶` (or vice versa) and a table appears with the games of that abend (columns: Spieltyp / Gewinner / Spieler).
       7. Click again — toggle closes.
       8. If you run a SQL DELETE removing all closed abende (or use a fresh DB), reload — the recap card MUST NOT appear at all.

    B. Stats tab — year leaderboard:
       1. Navigate to the Stats tab.
       2. Observe at the top a `Jahres-Leaderboard` card with a year picker on the right.
       3. Confirm the current year is selected by default and the table shows ranked rows.
       4. Confirm rank 1 has the gold accent (var(--ac) tint) via the `.rnk.gold` class.
       5. Change the year picker to a different available year (or a year with no data). The tbody updates WITHOUT the rest of the page re-rendering. For an empty year, the row text reads `Keine Spiele in {year} abgeschlossen.`.
       6. Briefly observe a `Laden…` text appears in the tbody during the fetch.

    C. Stats tab — head-to-head:
       1. Scroll to the `Direkt-Vergleich` card below the leaderboard.
       2. Select the same player in both dropdowns. Click `Vergleichen`. Observe the red inline error `Bitte zwei verschiedene Spieler wählen.`
       3. Select two different players who have played a shared game. Click `Vergleichen`. Observe a two-column result with .snum win counts, a `vs` separator, the player names below, and the footer `{draws} Unentschieden · {total} Spiele zusammen`.
       4. Select two players who have never played together. Click Vergleichen. Observe the empty-state message `{nameA} und {nameB} haben noch keine gemeinsamen Spiele.`

    D. Stats tab — streak chips:
       1. Scroll to the per-player cards.
       2. Each card now shows 5 chips in the row: Siege / Niederlagen / Pudel% / Streak / Längster.
       3. For a player with a current streak > 0, the Streak chip's number is green (var(--grn)).
       4. For a player with current = 0, the Streak chip number is muted (var(--mut)).
       5. The Längster chip number uses the accent color (var(--ac)) — i.e., default chip color.

    E. Player profile modal — KDA/BK counts:
       1. Tap any player row (in the Spieler tab list or click their name in stats) to open the profile modal.
       2. Scroll to the bottom of the modal content.
       3. Observe a new 2-column chip row with a gold/accent `KdA-Titel` chip and a red `BK Verlierer` chip showing the count integers.
       4. For a player with zero awards, both chips show `0` (no error, no spinner).

    F. Color audit (UI-SPEC Color section):
       1. Confirm var(--ac) appears ONLY on: active tab indicator, .stitle headings, Siege chip .v, Streak-longest chip .v, KdA-Titel chip .v, .rnk.gold for rank 1, KDA champion name color in recap card.
       2. Confirm BK count chip .v uses var(--red), NOT var(--ac).
       3. Confirm streak-current chip .v uses var(--grn) when > 0.

    G. No regressions:
       1. The existing Spiele tab finished-games list still renders below the recap card.
       2. The existing per-player Bestleistungen toggle still works (▼ Bestleistungen).
       3. The Bilderkegeln/KDA highlight symbols from Phase 7 still appear in the player tables.
       4. No JS errors in browser DevTools console after a full reload (network panel and console clean).

    H. Server tests still green:
       1. From a terminal, run `node --test`. All tests (including ST10..ST22, ST30..ST36, AB-*, AB30..AB32, HL10..HL13, plus all other phases) must pass.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — all verifications pass, no regressions
    - `issue: <description>` — describe each failed verification (screenshot path or copy-paste of unexpected content); the executor will fix and request re-verification
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → fetch() | The browser sends GET requests with year/h2h params controlled by the user via select elements |
| public/index.html → /api/* | Server endpoints validate input (per Plan 08-02 threat model T-08-02-T1/T2); client-side validation in H2H (same-player check) is UX only, not security |
| innerHTML usage in recap card and result block | If player names or abend names contain HTML metacharacters, they could break out of the template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-T1 | Tampering | innerHTML XSS via player or abend names | mitigate | Use textContent (not innerHTML) for ALL user-derived strings (player names, abend names, emoji, winner_name). innerHTML is acceptable ONLY for static template scaffolding. Specifically: do NOT interpolate `name` or `emoji` directly into innerHTML template literals — use createElement + textContent. The showSpDetail innerHTML pattern already in use already interpolates `s.name` and `s.emoji` (lines 652) — this plan's additions follow the same risk model and the existing showWinnerBanner XSS fix from Phase 7 sets the precedent; keep new additions safe by using textContent where the existing function does. |
| T-08-04-I1 | Information Disclosure | Recap exposes player names | accept | Same data already in /api/players and /api/highlights/current — no new disclosure surface |
| T-08-04-T2 | Tampering | Client-side year picker bypass | mitigate | Server validates year via `/^\d{4}$/.test(year)` (Plan 08-02 T-08-02-T1); client validation is UX only |
| T-08-04-T3 | Tampering | Client-side same-player H2H validation bypass | accept | If user crafts a request to /api/stats/h2h?a=5&b=5, server returns wins_a=0/wins_b=0/draws=N (a player draws with themselves only if isDraw is true, otherwise that single player is the unique winner counted toward both wins_a and wins_b). No security impact — read-only public endpoint |
| T-08-04-D1 | Denial of Service | Year picker rapid changes spam fetches | accept | User must manually change select; no debounce needed (per UI-SPEC.md). Browser's in-flight fetch overlap is acceptable for this use case. |
| T-08-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — package legitimacy gate N/A. No new external script tags. No new CSS framework imports. |
</threat_model>

<verification>
After this plan:
- All grep checks in Task 1 acceptance criteria find their target strings
- Task 2 checkpoint receives `approved` from the developer
- `node --test` continues to exit 0 (no test changes, but UI should not crash server-bound code paths)
- Visual UAT confirms all 7 UI-SPEC Component Inventory items render and behave as specified
</verification>

<success_criteria>
- All 7 phase requirements (STATS-01..05, RECAP-01..02) have a corresponding UI element on the Stats or Spiele tab or in the player profile modal
- UI-SPEC.md Copywriting Contract strings appear verbatim
- UI-SPEC.md Color reservations honored (var(--ac) only on the 6 listed elements)
- No new CSS classes introduced
- No regression in existing renderSpiele/renderStats/showSpDetail behavior
- Manual UAT signed off via checkpoint resume signal
</success_criteria>

<output>
Create `.planning/phases/08-statistiken-rueckblick/08-04-SUMMARY.md` documenting: each UI-SPEC component implemented with the line range in index.html where it lives, the new S state slots, the screenshot/notes from the human-verify checkpoint, and the final `node --test` output.
</output>
