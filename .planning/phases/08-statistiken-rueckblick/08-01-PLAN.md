---
phase: 08-statistiken-rueckblick
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/routes/stats.test.js
  - server/routes/abende.test.js
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04, STATS-05, RECAP-01, RECAP-02]

must_haves:
  truths:
    - "Running `node --test server/routes/stats.test.js server/routes/abende.test.js` shows 5 new failing tests for STATS-01..05 and 2 new failing tests for RECAP-01..02"
    - "All new tests fail because the implementation routes do not yet exist (404 or assert.fail RED markers)"
    - "Existing tests (ST10..ST22 in stats.test.js, AB-* in abende.test.js, HL10..HL13 in highlights.test.js) continue to pass"
    - "Helper functions insertFinishedKDAGame, insertFinishedBKGame, and insertClosedAbend are available to subsequent waves"
  artifacts:
    - path: "server/routes/stats.test.js"
      provides: "RED stubs ST30..ST36 for STATS-01..05"
      contains: "ST30"
    - path: "server/routes/abende.test.js"
      provides: "RED stubs AB30..AB32 for RECAP-01..02 + abend insertion helper"
      contains: "AB30"
  key_links:
    - from: "server/routes/stats.test.js"
      to: "/api/stats/year, /api/stats/streaks, /api/stats/h2h, /api/stats/kda-counts, /api/stats/bk-counts"
      via: "fetch via baseUrl"
      pattern: "fetch.*\\/api\\/stats\\/(year|streaks|h2h|kda-counts|bk-counts)"
    - from: "server/routes/abende.test.js"
      to: "/api/abende/last-summary"
      via: "fetch via baseUrl"
      pattern: "fetch.*\\/api\\/abende\\/last-summary"
---

<objective>
Wave 0 — establish RED test stubs for every Phase 8 requirement BEFORE any implementation exists. This locks in the contract: each test asserts a specific HTTP shape that the Wave 1 plans must satisfy.

Purpose: Nyquist validation — every requirement has an automated test before its implementation lands. Test file structure (harness, helpers) mirrors highlights.test.js exactly so the patterns are battle-tested.

Output: stats.test.js extended with 5 new ST* test blocks and helper imports; abende.test.js extended with 2 new AB* test blocks, new helper insertClosedAbend, and updated module imports + clearCache list. All new tests fail when run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/STATE.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-RESEARCH.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-PATTERNS.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-VALIDATION.md
@Claude/server/routes/highlights.test.js
@Claude/server/routes/stats.test.js
@Claude/server/routes/abende.test.js

<interfaces>
<!-- Existing helpers in highlights.test.js to copy verbatim. Executor must NOT re-derive these. -->

From server/routes/highlights.test.js lines 87-92 — insertPlayer(name, emoji, archived) → returns lastInsertRowid.

From server/routes/highlights.test.js lines 100-124 — insertFinishedGame(type_key, playerRows, finishedAt) → returns gameId. The version WITH finishedAt parameter (overloaded prepare statement). Stats.test.js currently has a version WITHOUT finishedAt at lines 109-132 — replace that block with the highlights.test.js version so the year sub-route tests can control finished_at.

From server/routes/highlights.test.js lines 144-207 — insertFinishedKDAGame(p1Id, p2Id, p3Id, p4Id, finishedAt) → returns { gameId, winnerId }. Drives KDA tournament to completion via kda.applyThrow loop. MUST be copied verbatim (the seeded shuffle requires String(gameId) as seed).

From server/routes/highlights.test.js lines 215-263 — insertFinishedBKGame(p1Id, p2Id, finishedAt) → returns { gameId, loserId }. Drives BK game to completion. p1 throws value=9, p2 throws value=1 → loserId === p2Id.

From server/routes/highlights.test.js test-shape lines 270-283 — test block pattern with fetch + assert.

Test IDs in scope: ST30 (year invalid), ST31 (year leaderboard), ST32 (streaks current+longest), ST33 (h2h shape), ST34 (h2h invalid params), ST35 (kda-counts), ST36 (bk-counts), AB30 (last-summary null when empty), AB31 (last-summary returns abend+kda_champion+bk_loser+games[]), AB32 (last-summary handles abend with no KDA/BK gracefully).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend stats.test.js with KDA/BK fixture helpers and ST30..ST36 RED stubs</name>
  <files>server/routes/stats.test.js</files>
  <read_first>
    - server/routes/stats.test.js (entire file — must understand existing harness, ST10..ST22 tests, current insertFinishedGame signature)
    - server/routes/highlights.test.js (lines 87-263 — source for insertFinishedKDAGame, insertFinishedBKGame, finishedAt-aware insertFinishedGame)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-PATTERNS.md (stats.test.js section — test ID convention starts at ST30)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-RESEARCH.md (Findings 1-5 — endpoint response shapes)
  </read_first>
  <behavior>
    - ST30: GET /api/stats/year?year=abc → 400 with JSON body { error: 'Invalid year' }
    - ST31: After inserting two finished games in year 2026 with known winner, GET /api/stats/year?year=2026 → 200 with { year: '2026', leaderboard: [...], available_years: [...] } where leaderboard contains the winner with wins >= 1 and is sorted by wins desc; available_years contains '2026'
    - ST32: After inserting 3 finished games with player p1 winning the first 2 in a row and losing the 3rd, GET /api/stats/streaks → 200 with body[String(p1)] === { current: 0, longest: 2 }
    - ST33: After inserting a finished game where players a and b both played and a won, GET /api/stats/h2h?a={a}&b={b} → 200 with { player_a: a, player_b: b, wins_a: 1, wins_b: 0, draws: 0, total: 1 }
    - ST34: GET /api/stats/h2h?a=0&b=-1 → 400 with JSON body { error: 'a and b must be positive integers' } (covers both invalid a and invalid b cases)
    - ST35: After inserting one finished KDA game with known winner, GET /api/stats/kda-counts → 200 with body[String(winnerId)] === 1
    - ST36: After inserting one finished BK game with known loser, GET /api/stats/bk-counts → 200 with body[String(loserId)] === 1
  </behavior>
  <action>
    Replace the existing insertFinishedGame function (currently lines 109-132, no finishedAt param) with the finishedAt-aware version from highlights.test.js lines 100-124 verbatim. Copy insertFinishedKDAGame (highlights.test.js 144-207) and insertFinishedBKGame (highlights.test.js 215-263) verbatim into stats.test.js immediately after insertFinishedGame.

    Append 7 new `test(...)` blocks at the bottom of the file (after the last existing ST22 test, before any trailing newline). Use the test IDs ST30 through ST36 in order. Each test follows the highlights.test.js RED-stub pattern (HL10 lines 270-283 as reference): use fetch against `${baseUrl}/api/...`, assert status code, parse body, assert response shape per the behavior block above. Do NOT use a session cookie for the new tests — stats sub-routes are public (no requireSession middleware), matching `/api/stats` itself.

    For ST31 (year leaderboard) insert two finished games via insertFinishedGame with type_key='dreiVollen' and explicit finishedAt='2026-06-01 12:00:00'. Use the dreiVollen pattern from stats.test.js ST11 (which already exists) for the throws shape — winner needs higher score than loser.

    For ST32 (streaks) insert three dreiVollen games via insertFinishedGame with monotonically increasing finished_at timestamps; in games 1 and 2, p1 has high throws (winner); in game 3, p2 has high throws (p1 loses). Assert body[String(p1Id)] equals { current: 0, longest: 2 }.

    For ST33 (h2h) insert one dreiVollen game with players a and b where a wins. Use the same throws pattern as ST11 in stats.test.js.

    For ST34 use a single fetch with `?a=0&b=-1` and assert status 400 and the exact error message.

    For ST35 use insertFinishedKDAGame with 4 freshly inserted players; capture winnerId; fetch /api/stats/kda-counts; assert body[String(winnerId)] === 1.

    For ST36 use insertFinishedBKGame with 2 freshly inserted players; capture loserId; fetch /api/stats/bk-counts; assert body[String(loserId)] === 1.

    Each test block must include a comment header in the existing style: `// ---` separator + `// ST3X: <description>` + `// Status: RED — route does not exist yet` (mirrors HL10 header at lines 265-269 of highlights.test.js).

    Do not add any `module.exports` or `'./highlights'` references — stats.test.js does not import the route module directly. The clearCache list at lines 38-47 already includes `./stats`; no list change needed because no new module imports are added.
  </action>
  <verify>
    <automated>node --test server/routes/stats.test.js 2>&amp;1 | grep -E "(# tests|# fail|ST30|ST31|ST32|ST33|ST34|ST35|ST36)"</automated>
  </verify>
  <acceptance_criteria>
    - File `server/routes/stats.test.js` contains string `ST30:` exactly once
    - File contains string `ST31:` exactly once
    - File contains string `ST32:` exactly once
    - File contains string `ST33:` exactly once
    - File contains string `ST34:` exactly once
    - File contains string `ST35:` exactly once
    - File contains string `ST36:` exactly once
    - File contains string `insertFinishedKDAGame` at least once (helper defined)
    - File contains string `insertFinishedBKGame` at least once (helper defined)
    - Existing insertFinishedGame supports finishedAt parameter (search for `finishedAt` in the helper body)
    - Command `node --test server/routes/stats.test.js` exits non-zero AND reports at least 7 failing tests
    - Pre-existing tests ST10..ST22 still PASS (count of pass lines must be >= 13 — original count before this task)
    - All new ST3X test failures are due to 404 / wrong shape from the missing routes, NOT due to harness/helper bugs (verify by reading the test output: failures must include 'status' or 'Expected 200' or similar — not 'is not a function' or 'ReferenceError')
  </acceptance_criteria>
  <done>
    7 new test blocks (ST30..ST36) appended to stats.test.js, two helpers (insertFinishedKDAGame, insertFinishedBKGame) imported, insertFinishedGame upgraded to accept finishedAt, all new tests RED (failing), all existing ST10..ST22 GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend abende.test.js with closed-abend helper and AB30..AB32 RED stubs</name>
  <files>server/routes/abende.test.js</files>
  <read_first>
    - server/routes/abende.test.js (entire file — must understand existing AB-* tests, db.prepare style, harness)
    - server/routes/highlights.test.js (lines 87-263 — source for insertPlayer, insertFinishedGame with finishedAt, insertFinishedKDAGame, insertFinishedBKGame)
    - server/routes/abende.js (lines 1-50 — confirm `abende` table columns: id, name, started_at, ended_at)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-PATTERNS.md (abende.test.js section — clearCache list addition, insertClosedAbend helper signature)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-RESEARCH.md (Finding 6 — last-summary response shape)
  </read_first>
  <behavior>
    - AB30: When zero rows exist in `abende` with ended_at IS NOT NULL, GET /api/abende/last-summary → 200 with body === null
    - AB31: After inserting a closed abend, one finished KDA game and one finished BK game (both with abend_id pointing at that abend), GET /api/abende/last-summary → 200 with body shape: { abend: { id, name, started_at, ended_at }, kda_champion: { id, name, emoji }, bk_loser: { id, name, emoji }, games: [...] }; games.length === 2; each game entry has fields { id, type_key, finished_at, winner_name, player_count }; kda_champion.id === expected KDA winnerId; bk_loser.id === expected BK loserId
    - AB32: After inserting a closed abend with zero finished games (no KDA, no BK) linked to it, GET /api/abende/last-summary → 200 with body.abend non-null, body.kda_champion === null, body.bk_loser === null, body.games is an array of length 0
  </behavior>
  <action>
    Add three new clearCache entries to BOTH the `before` block and the `after` block (currently lines 36-46 and ~76-86): add `clearCache('./highlights');`, `clearCache('./games');`, `clearCache('../game-types/index');`. These are required because Wave 1 Plan 08-03 will make abende.js import from highlights.js, games.js, and game-types/index.

    Define helpers immediately after the existing helper section (after line ~110 of abende.test.js). Copy insertPlayer, the finishedAt-aware insertFinishedGame (highlights.test.js lines 100-124), insertFinishedKDAGame (highlights.test.js lines 144-207), and insertFinishedBKGame (highlights.test.js lines 215-263) verbatim. If insertPlayer already exists in abende.test.js, do not duplicate it; otherwise copy from highlights.test.js lines 87-92.

    Add new helper insertClosedAbend(name, endedAt) that runs `INSERT INTO abende (name, started_at, ended_at) VALUES (?, datetime('now', '-2 hours'), ?)` with arguments (name || 'Test Abend', endedAt || '2026-04-01 22:00:00') and returns lastInsertRowid. Add a sibling helper linkGameToAbend(gameId, abendId) that runs `UPDATE games SET abend_id = ? WHERE id = ?` since insertFinishedKDAGame/BKGame insert games without an abend_id.

    Append three new `test(...)` blocks at the bottom of the file (after the last existing test). Use test IDs AB30, AB31, AB32. Each follows the HL10 RED-stub pattern (highlights.test.js lines 270-283): fetch against `${baseUrl}/api/abende/last-summary`, assert status + body. Do NOT pass the session cookie — last-summary must be public (matches `/api/abende/active` which is also public).

    For AB30 simply fetch without inserting any abend. The default seed (line 49) creates seed players but no abend rows; verify by reading the seed function if uncertain. If seed creates an abend it must be ended_at IS NULL (active), so the LIMIT 1 query returns nothing.

    For AB31: insertPlayer x4 (KDA), insertPlayer x2 (BK), insertClosedAbend → abendId, insertFinishedKDAGame → { gameId: kgid, winnerId }, insertFinishedBKGame → { gameId: bgid, loserId }, linkGameToAbend(kgid, abendId), linkGameToAbend(bgid, abendId). Then fetch and assert.

    For AB32: insertClosedAbend only (no games), fetch, assert body.games is an empty array, kda_champion null, bk_loser null, abend.id matches.

    Each test block includes the comment header pattern: `// ---` separator + `// AB3X: <description>` + `// Status: RED — route does not exist yet`.
  </action>
  <verify>
    <automated>node --test server/routes/abende.test.js 2>&amp;1 | grep -E "(# tests|# fail|AB30|AB31|AB32)"</automated>
  </verify>
  <acceptance_criteria>
    - File `server/routes/abende.test.js` contains string `AB30:` exactly once
    - File contains string `AB31:` exactly once
    - File contains string `AB32:` exactly once
    - File contains string `insertClosedAbend` (helper definition)
    - File contains string `linkGameToAbend` (helper definition)
    - File contains string `clearCache('./highlights')` in both before and after blocks (grep count >= 2, excluding comments)
    - File contains string `clearCache('./games')` in both before and after blocks (grep count >= 2)
    - File contains string `clearCache('../game-types/index')` in both before and after blocks (grep count >= 2)
    - Command `node --test server/routes/abende.test.js` exits non-zero AND reports at least 3 new failing tests
    - Pre-existing AB tests still pass — count of passing tests >= original count before this task
    - Failures for AB30..AB32 are 404 / wrong-shape errors, NOT harness/import errors
  </acceptance_criteria>
  <done>
    3 new test blocks (AB30..AB32) appended, helpers (insertPlayer, insertFinishedGame, insertFinishedKDAGame, insertFinishedBKGame, insertClosedAbend, linkGameToAbend) defined, clearCache list extended for ./highlights, ./games, ../game-types/index in both before and after. All new tests RED. All existing AB tests GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → DB | Tests insert directly via db.prepare; bypass HTTP validation (intentional — test fixtures) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-T1 | Tampering | Test file fixtures | accept | Tests run in isolated tmpDir SQLite db; cannot affect production data |
| T-08-01-I1 | Information Disclosure | Test PIN/secrets | accept | Test PIN is hard-coded 1234 in process.env; same pattern as highlights.test.js (already in use) |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — package legitimacy gate N/A |
</threat_model>

<verification>
Run `node --test server/routes/stats.test.js server/routes/abende.test.js` — must exit non-zero (RED tests are present); must report at least 10 new failing tests (ST30..ST36 + AB30..AB32); existing tests must remain green.
</verification>

<success_criteria>
- ST30..ST36 added to stats.test.js, all failing because routes do not exist
- AB30..AB32 added to abende.test.js, all failing because route does not exist
- Helpers (insertFinishedKDAGame, insertFinishedBKGame, insertClosedAbend, linkGameToAbend) are present and importable within their test files
- abende.test.js clearCache list extended for upcoming abende.js imports
- All existing tests (ST10..ST22, AB-*, HL10..HL13) continue to pass
- `node --test` exits non-zero overall (because of intentional RED stubs)
</success_criteria>

<output>
Create `.planning/phases/08-statistiken-rueckblick/08-01-SUMMARY.md` when done with: file paths modified, test IDs added (with one-line descriptions), and the exact `node --test` output summary line showing pass/fail counts.
</output>
