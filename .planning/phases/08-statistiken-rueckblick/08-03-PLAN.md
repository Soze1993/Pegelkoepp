---
phase: 08-statistiken-rueckblick
plan: 03
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - server/routes/abende.js
autonomous: true
requirements: [RECAP-01, RECAP-02]

must_haves:
  truths:
    - "GET /api/abende/last-summary returns 200 with body === null when no closed abend exists"
    - "GET /api/abende/last-summary returns 200 with { abend, kda_champion, bk_loser, games } when a closed abend has games"
    - "abend object contains { id, name, started_at, ended_at }"
    - "kda_champion and bk_loser are { id, name, emoji } objects or null"
    - "games array contains { id, type_key, finished_at, winner_name, player_count } per finished game linked to that abend"
    - "AB30..AB32 stubs from Plan 08-01 now PASS"
    - "Existing AB tests in abende.test.js continue to pass"
  artifacts:
    - path: "server/routes/abende.js"
      provides: "GET /api/abende/last-summary handler"
      contains: "router.get('/last-summary'"
  key_links:
    - from: "server/routes/abende.js"
      to: "abende table (closed evenings)"
      via: "SELECT WHERE ended_at IS NOT NULL ORDER BY ended_at DESC LIMIT 1"
      pattern: "ended_at IS NOT NULL"
    - from: "server/routes/abende.js"
      to: "games table filtered by abend_id"
      via: "SELECT WHERE abend_id = ? AND status = 'finished' ORDER BY id ASC"
      pattern: "abend_id = \\?"
    - from: "server/routes/abende.js"
      to: "highlights.js getBKLoserId (exported by Plan 08-02)"
      via: "require('./highlights').getBKLoserId"
      pattern: "require\\(.\\./highlights.\\)"
---

<objective>
Wave 1 backend — implement GET /api/abende/last-summary on the existing abende router. The endpoint returns the most recently closed Kegelabend with its KDA champion, BK loser, and a per-game summary list.

Purpose: Deliver RECAP-01 and RECAP-02. The recap must read from the MOST RECENT CLOSED abend (ended_at IS NOT NULL), not the globally-latest game — this is the key distinction from /api/highlights/current (see RESEARCH.md Pitfall 1).

Output: abende.js extended with a `/last-summary` handler placed BEFORE `GET /:id` (mirrors the existing `/active` placement comment); imports for gameTypes, reconstructState, and getBKLoserId added; AB30..AB32 tests transition RED → GREEN.

Note on plan parallelism: This plan modifies a different file (abende.js) than Plan 08-02 (stats.js, highlights.js). Both can execute in parallel in Wave 1. The only cross-plan dependency is the highlights.js getBKLoserId export — Plan 08-02 Task 1 must land before this plan's task runs, OR the executor of this plan must verify the export exists before starting; if missing, execute this plan after 08-02-01 completes. The wave_order tooling treats them as sequenced within Wave 1 only if a file conflict exists — which there is not — but the data dependency on highlights.js export should be honored at execute time by running 08-02 Task 1 first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/PROJECT.md
@Claude/.planning/ROADMAP.md
@Claude/.planning/STATE.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-RESEARCH.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-PATTERNS.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-VALIDATION.md
@Claude/.planning/phases/08-statistiken-rueckblick/08-01-SUMMARY.md
@Claude/server/routes/abende.js
@Claude/server/routes/highlights.js

<interfaces>
<!-- Existing types/contracts the executor must use directly. -->

From server/routes/abende.js — Router setup at top, GET /active at line 22, GET / at line 33, POST / at line 45+. New handler must be placed between line 27 (closing of /active) and line 33 (opening of GET /).

From server/routes/games.js — exports `reconstructState(game)` which accepts a game row and returns the replayed state.

From server/game-types/index.js (imported as `gameTypes`) — gameTypes[type_key].getFinalResults(state) returns Array<{ playerId, score, winner: boolean, ... }>. For KDA, state.gewinner is { id, name, emoji } or null when not done.

From server/routes/highlights.js (after Plan 08-02 Task 1 lands) — `module.exports.getBKLoserId` is a function (state) → number | null.

DB schema referenced:
- abende(id INTEGER PK, name TEXT, started_at TIMESTAMP, ended_at TIMESTAMP NULL)
- games(id, type_key, status, finished_at, abend_id NULLABLE)
- players(id, name, emoji, archived)

Response shapes AB30..AB32 expect:
- No closed abend → 200 with body === null
- Closed abend with games → 200 with { abend: { id, name, started_at, ended_at }, kda_champion: { id, name, emoji } | null, bk_loser: { id, name, emoji } | null, games: [{ id, type_key, finished_at, winner_name, player_count }] }
- Closed abend with no games → 200 with abend filled, kda_champion null, bk_loser null, games === []
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GET /api/abende/last-summary handler</name>
  <files>server/routes/abende.js</files>
  <read_first>
    - server/routes/abende.js (entire file — must understand existing route ordering, the /active placement comment at lines 17-21, GET / at line 33)
    - server/routes/highlights.js (lines 36-83 — canonical pattern for combining reconstructState + getBKLoserId + player name lookup)
    - server/routes/games.js (find `reconstructState` export and confirm it accepts the game row shape from `SELECT * FROM games WHERE ...`)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-PATTERNS.md (abende.js section — exact code block)
    - Claude/.planning/phases/08-statistiken-rueckblick/08-RESEARCH.md (Finding 6 — full handler spec; Pitfall 1 — last abend vs last game distinction; Pitfall 2 — abend_id NULL on old games)
    - server/routes/abende.test.js (Wave 0 AB30..AB32 — exact response shapes expected)
  </read_first>
  <behavior>
    - GET /api/abende/last-summary with no closed abend in DB → 200 body === null (AB30)
    - GET /api/abende/last-summary with one closed abend that has one finished KDA game and one finished BK game (both linked via abend_id) → 200 with body.abend present, body.kda_champion.id === KDA winnerId, body.bk_loser.id === BK loserId, body.games has 2 entries (AB31)
    - GET /api/abende/last-summary with one closed abend and zero linked finished games → 200 with body.abend present, body.kda_champion === null, body.bk_loser === null, body.games is empty array (AB32)
    - Endpoint is public (no requireSession) matching the existing /active handler
    - Route is matched correctly when accessed as /api/abende/last-summary, NOT captured by a hypothetical /:id (achieved by route ordering)
  </behavior>
  <action>
    Step 1 — Add three import lines after line 5 (after `const requireSession = require('../middleware/auth');`):
    Import `gameTypes` from '../game-types', import `reconstructState` destructured from './games', and import `getBKLoserId` destructured from './highlights'. The variable names must be exactly `gameTypes`, `reconstructState`, `getBKLoserId`.

    Step 2 — Insert the new handler block BETWEEN the closing of `/active` (currently at line 27: `});` after `res.json(abend || null)`) AND the start of GET / (line 29 begins the next comment block before line 33). Place this handler BEFORE `GET /` so it has the same protective ordering as `/active`. (Note: GET /:id does not currently exist in abende.js, but the placement-before-/:id rule from PATTERNS.md is preserved by putting last-summary near /active.)

    Step 3 — Add a comment header in the same style as `/active`'s header (lines 17-21): a banner comment that says GET /api/abende/last-summary is public, returns the most recently closed Kegelabend with kda_champion + bk_loser + games[], and notes the placement requirement before any future /:id route.

    Step 4 — Inside `router.get('/last-summary', (req, res) => { ... })`, implement per PATTERNS.md lines 259-318:

    a) Query `SELECT * FROM abende WHERE ended_at IS NOT NULL ORDER BY ended_at DESC LIMIT 1` via db.prepare(...).get(). If falsy, `return res.json(null)`.

    b) Query `SELECT * FROM games WHERE abend_id = ? AND status = 'finished' ORDER BY id ASC` via db.prepare(...).all(lastAbend.id). Use `ORDER BY id ASC` (NOT finished_at) per RESEARCH.md Pitfall 3 — BK exemption chain reconstruction depends on insertion order.

    c) Initialize `let kda_champion = null; let bk_loser = null; const gamesSummary = [];`.

    d) Loop over abendGames. For each game:
       - Look up `gameModule = gameTypes[game.type_key]`; if falsy `continue`.
       - In a try/catch wrapping `state = reconstructState(game); results = gameModule.getFinalResults(state);` — on catch `continue`.
       - If `game.type_key === 'kda'` AND state.gewinner AND state.gewinner.id != null: look up `SELECT id, name, emoji FROM players WHERE id = ?` with state.gewinner.id; if player row found, set `kda_champion = { id: p.id, name: p.name, emoji: p.emoji }`.
       - If `game.type_key === 'bilderkegel'`: compute `loserId = getBKLoserId(state)`; if loserId != null, look up the player row; if found, set `bk_loser = { id, name, emoji }`.
       - Compute `winners = results.filter(r => r.winner); winnerEntry = winners.length === 1 ? winners[0] : null; let winner_name = null;` If winnerEntry, look up `SELECT name FROM players WHERE id = ?` for winnerEntry.playerId and assign `winner_name = wp.name`.
       - Push to gamesSummary: `{ id: game.id, type_key: game.type_key, finished_at: game.finished_at, winner_name, player_count: results.length }`.

    e) Respond:
       res.json({
         abend: { id: lastAbend.id, name: lastAbend.name, started_at: lastAbend.started_at, ended_at: lastAbend.ended_at },
         kda_champion,
         bk_loser,
         games: gamesSummary
       });

    Step 5 — Do NOT wrap the outer handler in try/catch; the per-game try/catch is sufficient and matches highlights.js style (lines 54-57 and 76-79). Do NOT add requireSession middleware — endpoint is public.

    Do NOT modify any existing route handler (`/active`, GET /, POST /, PUT /:id/end, etc.). Order of existing handlers stays unchanged.
  </action>
  <verify>
    <automated>node --test server/routes/abende.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `server/routes/abende.js` contains literal `router.get('/last-summary'`
    - File contains require for `./highlights` (extracting getBKLoserId)
    - File contains require for `./games` (extracting reconstructState)
    - File contains require for `../game-types` (full module as gameTypes)
    - File contains literal `ended_at IS NOT NULL ORDER BY ended_at DESC LIMIT 1`
    - File contains literal `abend_id = ?` in a SELECT query
    - File contains literal `ORDER BY id ASC` in the games query (not finished_at)
    - The /last-summary handler appears BEFORE the GET /:id route if any (presently no /:id exists; verify by reading file)
    - The /last-summary handler appears BEFORE `module.exports = router;`
    - Existing route handlers (`/active`, GET /, POST /, etc.) are unchanged — grep for their first lines to confirm
    - Command `node --test server/routes/abende.test.js` exits 0 (AB30..AB32 now green, existing AB tests still green)
    - Command `node --test` exits 0 (full suite green if Plan 08-02 has also landed; otherwise only AB30..AB32 + ST30..ST36 should be the deciders)
  </acceptance_criteria>
  <done>
    GET /api/abende/last-summary implemented per spec, placed in correct route order, all three AB30..AB32 tests pass, existing abende tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (untrusted) → /api/abende/last-summary | No user-controlled query params; entire response computed from DB state |
| abende.js → DB | All access via parameterized db.prepare(...).get/all() |
| abende.js → highlights.js | In-process require; getBKLoserId is a pure function with no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-S1 | Spoofing | Public endpoint | accept | No auth required (parity with /api/abende/active and /api/highlights/current — homepage and TV need them) |
| T-08-03-T1 | Tampering | SQL injection | mitigate | No user-controlled input enters SQL; all values come from prior DB rows (lastAbend.id) or hard-coded type_key strings. Parameterized binding used throughout. |
| T-08-03-I1 | Information Disclosure | Reveals player names + KDA/BK history | accept | Same data already exposed by /api/players, /api/highlights/current, /api/stats. Internal club app. |
| T-08-03-R1 | Repudiation | Logs | accept | morgan request logging is global; no per-endpoint audit needed for read-only stats |
| T-08-03-D1 | Denial of Service | Iteration over abend games | accept | Per-abend game count is bounded (one evening = ~5-20 games); reconstruction is fast. Existing /api/highlights/current already does the same work for the latest KDA + BK game. |
| T-08-03-E1 | Elevation of Privilege | Read-only handler | mitigate | GET only; no mutations; no req.body parsing; no session writes |
| T-08-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — package legitimacy gate N/A. New imports are all in-tree: ./highlights, ./games, ../game-types. |
</threat_model>

<verification>
After this plan:
- `node --test server/routes/abende.test.js` → exit 0
- `node --test` → exit 0 (entire suite green if 08-02 has also landed)
- Manual smoke: with the dev server running and at least one closed abend + finished games linked to it, `curl http://localhost:PORT/api/abende/last-summary` returns the JSON shape above
</verification>

<success_criteria>
- /api/abende/last-summary returns the correct shape for the three states tested by AB30..AB32
- Route placement preserves existing /active and GET / behavior
- Three new requires (gameTypes, reconstructState, getBKLoserId) are added without overwriting existing imports
- Public endpoint (no requireSession)
- Uses ORDER BY id ASC for game iteration (BK exemption chain safety)
</success_criteria>

<output>
Create `.planning/phases/08-statistiken-rueckblick/08-03-SUMMARY.md` documenting: the new route path and response shape, line range where the handler was inserted, the three new import lines added, and the test output showing AB30..AB32 green.
</output>
