---
phase: 02-real-time-tv
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - server/routes/socket.test.js
  - server/db/db.test.js
  - server/routes/games.test.js
autonomous: true
requirements: []
tags:
  - test-scaffolding
  - nyquist
  - socket.io
must_haves:
  truths:
    - "socket.io-client is installed as a devDependency"
    - "server/routes/socket.test.js exists and the test runner discovers it"
    - "Failing test stubs exist for RT-01, RT-02, TV-02, TV-04, PLAY-01"
    - "db.test.js asserts that throws.meta and game_players.role columns exist (idempotent migration check)"
    - "games.test.js has a placeholder test for throws.meta persistence (GT19)"
    - "All test stubs intentionally fail (RED) or skip with a TODO marker — they wire the harness, they do not assert success yet"
  artifacts:
    - path: "package.json"
      provides: "devDependencies entry for socket.io-client"
      contains: "socket.io-client"
    - path: "server/routes/socket.test.js"
      provides: "Socket.io integration test harness with before/after hooks"
      contains: "io: ioClient"
    - path: "server/db/db.test.js"
      provides: "Migration column-existence assertions"
      contains: "throws.meta"
    - path: "server/routes/games.test.js"
      provides: "Test stub for throws.meta persistence"
      contains: "GT19"
  key_links:
    - from: "server/routes/socket.test.js"
      to: "node:test runner via npm test"
      via: "node --test pattern auto-discovery"
      pattern: "node:test"
    - from: "server/routes/socket.test.js"
      to: "socket.io-client package"
      via: "require('socket.io-client')"
      pattern: "require\\('socket.io-client'\\)"
---

<objective>
Install the test scaffolding required by 02-VALIDATION.md before any Phase 2 production code is written. Create the Socket.io integration test harness (`server/routes/socket.test.js`) with stub tests for every socket-driven requirement (RT-01, RT-02, TV-02, TV-04, PLAY-01), add migration idempotency assertions to `server/db/db.test.js`, and add a `throws.meta` persistence stub to `server/routes/games.test.js`. Stubs MUST be in a failing or t.todo state — they are the Nyquist gate; Wave 1 and Wave 2 implementations will turn them green.

Purpose: The Nyquist rule requires every <verify> to have an <automated> command. This plan creates the test files those commands will target so later plans never have to write "MISSING — Wave 0 must create {test_file} first".

Output: socket.io-client devDep installed + 1 new test file (`socket.test.js`) + 2 extended test files (`db.test.js`, `games.test.js`). Stubs intentionally RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Claude/.planning/phases/02-real-time-tv/02-CONTEXT.md
@Claude/.planning/phases/02-real-time-tv/02-RESEARCH.md
@Claude/.planning/phases/02-real-time-tv/02-PATTERNS.md
@Claude/.planning/phases/02-real-time-tv/02-VALIDATION.md
@Claude/.planning/phases/01-backend-foundation/01-04-SUMMARY.md
@Claude/package.json
@Claude/server/routes/games.test.js
@Claude/server/db/db.test.js

<interfaces>
Existing test scaffold conventions (Phase 1) — replicate exactly:

From server/routes/games.test.js (lines 1–84):
- `before()` creates `tmpDir` via `fs.mkdtempSync`, sets `process.env.DB_PATH`, `SESSION_SECRET`, `NODE_ENV=test`, `SESSION_DIR`, `PIN_HASH`.
- `clearCache(mod)` helper deletes `require.cache[require.resolve(mod)]` for `../db/index`, `../db/seed`, `../app`, `./games`, `./players`, `./auth`, `../middleware/auth`, `../game-types/index`.
- `server.listen(0, '127.0.0.1', resolve)` then `baseUrl = http://127.0.0.1:${port}`.
- `loginAndGetCookie()` POSTs to `/api/auth/login` with `{ pin: '1234' }` and returns first part of `set-cookie` header split by `;`.

From server/db/db.test.js — extend to assert column existence using `PRAGMA table_info(<table>)`.

From package.json — `devDependencies` currently has only `nodemon`. socket.io-client must be ADDED to that block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install socket.io-client devDependency and verify import resolves</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - Claude/package.json (current devDependencies block contains only nodemon)
    - Claude/.planning/phases/02-real-time-tv/02-RESEARCH.md (Environment Availability section confirms socket.io-client 4.8.3 required)
  </read_first>
  <action>
    Run `npm install --save-dev socket.io-client` from the Claude/ project root. After install, verify package.json `devDependencies` block contains a key `socket.io-client` with a semver pinned to the 4.x line (matching Socket.io server 4.8.3). Do NOT install `socket.io` (the server package) in this task — that belongs to plan 02-02. If `npm install` fails with SSL certificate errors (research noted npm registry SSL issue on this machine), retry with `npm install --save-dev socket.io-client --use-system-ca`; if still failing, use `NODE_TLS_REJECT_UNAUTHORIZED=0 npm install --save-dev socket.io-client` as a development-only fallback and add a SUMMARY note. After install, run `node -e "console.log(require('socket.io-client').io)"` to confirm the `io` factory function is importable; it should print `[Function: io]` or similar — not undefined and not throw MODULE_NOT_FOUND.
  </action>
  <verify>
    <automated>node -e "const m = require('socket.io-client'); if (typeof m.io !== 'function') process.exit(1); console.log('socket.io-client OK');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `devDependencies` object contains a key exactly named `socket.io-client`
    - package.json `devDependencies["socket.io-client"]` starts with `^4.` (e.g., `^4.8.3`)
    - Command `node -e "const m = require('socket.io-client'); console.log(typeof m.io);"` prints `function` and exits 0
    - node_modules/socket.io-client/ directory exists
    - package-lock.json includes a top-level resolution for `socket.io-client`
  </acceptance_criteria>
  <done>socket.io-client 4.x is installed in devDependencies; require('socket.io-client').io is a function.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create server/routes/socket.test.js test harness with RED stubs for RT-01, RT-02, TV-02, TV-04, PLAY-01</name>
  <files>server/routes/socket.test.js</files>
  <read_first>
    - Claude/server/routes/games.test.js (lines 1–100 — copy the before/after scaffold pattern, env var setup, clearCache pattern, loginAndGetCookie helper)
    - Claude/server/routes/auth.test.js (lines 1–53 — before/after server.listen(0) hook pattern)
    - Claude/.planning/phases/02-real-time-tv/02-PATTERNS.md (lines 405–574 — full socket.test.js scaffold template to copy)
    - Claude/.planning/phases/02-real-time-tv/02-VALIDATION.md (Per-Task Verification Map — defines which requirements this file MUST cover)
  </read_first>
  <action>
    Create `server/routes/socket.test.js` as a new file using `'use strict';` and CommonJS. The file requires `node:test`, `node:assert/strict`, `node:fs`, `node:os`, `node:path`, `node:http`, `socket.io` (Server), `socket.io-client` (io as ioClient), and `bcryptjs`. The before() hook must mirror games.test.js exactly: create a unique tmpDir via `fs.mkdtempSync`, set `process.env.DB_PATH`, `process.env.SESSION_SECRET`, `process.env.NODE_ENV='test'`, `process.env.SESSION_DIR`, `process.env.PIN_HASH = bcrypt.hashSync('1234', 10)` BEFORE requiring `../app`. Call clearCache for the same module list as games.test.js. Inside before() after server creation, attach `io = new Server(server, { cors: { origin: false } })` then `app.locals.io = io`. Register an `io.on('connection', ...)` handler that queries `"SELECT id FROM games WHERE status = 'active' ORDER BY id DESC LIMIT 1"` and emits `game:state` with `{ idle: false, gameId, state }` when a game exists or `{ idle: true, lastWinner: null }` when none does (per D-09, D-10). Then `server.listen(0, '127.0.0.1', resolve)` and connect a primary `socket = ioClient(baseUrl, { transports: ['websocket'] })`. The after() hook must call `socket.disconnect()`, `io.close()`, `server.close()`, then clearCache and rmSync(tmpDir). Define a `waitForEvent(sock, eventName, timeoutMs = 2000)` helper that wraps `sock.once(eventName, resolve)` with a setTimeout reject. Define `loginAndGetCookie()` identical to games.test.js. Add the following 5 RED stub tests, each using `t.todo` annotation OR a deliberately failing assert so the suite goes RED until Wave 1/2 lands the implementation: ST01 ('game:state idle on connect when no active game' — RT-02/TV-04), ST02 ('throw:applied emitted after POST /throws' — RT-01), ST03 ('game:state contains players array with id, name, last throw' — TV-02), ST04 ('undo:applied emitted after POST /:id/undo' — PLAY-01), ST05 ('reconnecting client receives current game:state' — RT-02). Each test body must call the helpers but assert `false, 'TODO: implemented in Wave 1/2'` OR use `t.todo('Wave 1/2')` so node:test reports them as planned-but-not-passing. Do NOT make stubs pass by relaxed asserts.
  </action>
  <verify>
    <automated>node --test server/routes/socket.test.js 2>&1 | grep -E "(ST01|ST02|ST03|ST04|ST05)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `server/routes/socket.test.js` exists
    - File starts with `'use strict';`
    - File contains `require('socket.io')` and `require('socket.io-client')`
    - File contains 5 `test(` blocks named ST01, ST02, ST03, ST04, ST05
    - File contains a `before(` block that sets `process.env.DB_PATH`
    - File contains a `before(` block that calls `new Server(server, { cors: { origin: false } })`
    - File contains the literal string `app.locals.io = io`
    - File contains a `waitForEvent` function declaration
    - Running `node --test server/routes/socket.test.js` exits with a non-zero status (stubs are RED) OR all 5 stubs are reported as `# todo` lines
    - Running `node --test server/routes/socket.test.js` does NOT throw `MODULE_NOT_FOUND` and does NOT throw an unhandled exception during before() — the harness must load cleanly
  </acceptance_criteria>
  <done>Socket.io test harness file exists with 5 named RED stub tests; node --test discovers and runs (but does not pass) all 5.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend db.test.js with migration column-existence test and add GT19 throws.meta persistence stub to games.test.js</name>
  <files>server/db/db.test.js, server/routes/games.test.js</files>
  <read_first>
    - Claude/server/db/db.test.js (read full file — extend the existing `before()` hook and add new tests at the bottom)
    - Claude/server/routes/games.test.js (read final 30 lines — append GT19 stub after the last existing GT test)
    - Claude/.planning/phases/02-real-time-tv/02-VALIDATION.md (D-12 and D-13 rows — define what these tests must assert)
    - Claude/.planning/phases/02-real-time-tv/02-PATTERNS.md (game.test.js extension pattern lines 305–401)
  </read_first>
  <action>
    In `server/db/db.test.js`, append a new test named `DB05: throws table has meta column and game_players has role column after migration`. The test must execute `db.prepare("PRAGMA table_info(throws)").all()` and assert that the returned array contains an object with `name === 'meta'`. Then `db.prepare("PRAGMA table_info(game_players)").all()` and assert the array contains `name === 'role'`. Add a second test `DB06: migrations are idempotent — re-running ALTER TABLE does not throw`. This test directly invokes the migration block by calling `db.exec("ALTER TABLE throws ADD COLUMN meta TEXT NULL")` inside try/catch and asserting the catch path was taken (since the column already exists after db/index.js init). Both tests are RED until plan 02-02 lands the migration. Mark them with `t.todo` if necessary OR write them to fail clearly with the message `'Wave 1 migration not yet implemented'`. In `server/routes/games.test.js`, append a new test `GT19: POST /throws persists meta JSON and reconstructState parses it back (grosseHaus)`. Build the test by creating a game with `type_key: 'grosseHaus'`, submitting a throw with body `{ player_id: 1, throw_index: 0, value: 5, meta: { slot: 'h' } }`, then `db.prepare('SELECT meta FROM throws WHERE game_id = ? ORDER BY id DESC LIMIT 1').get(gameId)` and assert that the returned `meta` field is a non-null string and `JSON.parse(meta)` deep-equals `{ slot: 'h' }`. Mark RED until plan 02-02. Do NOT modify any production code in this task.
  </action>
  <verify>
    <automated>node --test server/db/db.test.js server/routes/games.test.js 2>&1 | grep -E "(DB05|DB06|GT19)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - server/db/db.test.js contains a test named `DB05` that calls `PRAGMA table_info(throws)`
    - server/db/db.test.js contains a test named `DB06` that exercises the idempotent migration path
    - server/routes/games.test.js contains a test named `GT19` that asserts on `JSON.parse(throwRow.meta)`
    - Running `node --test server/db/db.test.js` reports DB05 and DB06 as either todo or failing (they MUST NOT pass)
    - Running `node --test server/routes/games.test.js` reports GT19 as either todo or failing (it MUST NOT pass — schema/INSERT does not yet support meta)
    - Existing tests (DB01-DB04, GT01-GT18 in those files) continue to pass — count `# pass` for existing tests must not drop
    - No production files (server/db/index.js, server/routes/games.js) are modified
  </acceptance_criteria>
  <done>db.test.js has DB05/DB06; games.test.js has GT19; all three are RED stubs; existing Phase 1 tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev machine | Package install crosses untrusted-network boundary; supply-chain risk |
| test process → tmpDir | Test files create/destroy temp directories — must not touch production DB_PATH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | socket.io-client npm install | accept | Package vetted in 02-RESEARCH.md Package Legitimacy Audit — canonical socket.io org package, 14+ years, 10M+ weekly downloads. No checkpoint required per researcher disposition. |
| T-02-01-W0 | Tampering | server/routes/socket.test.js harness | mitigate | Test before() must set `process.env.DB_PATH` to a `fs.mkdtempSync` tmpDir BEFORE requiring `../db/index` (test isolation pattern from games.test.js lines 26–65). Prevents test runs from corrupting `data/kegelclub.db`. |
| T-02-02-W0 | Information Disclosure | bcrypt PIN_HASH in test env | accept | PIN '1234' and its bcrypt hash live only in `process.env` inside the test process; never written to disk; tmpDir cleaned in after() hook. |
</threat_model>

<verification>
- `npm test` must run to completion (it will report failures for the new RED stubs — that is correct)
- Existing Phase 1 test count (153) must still pass: `node --test 2>&1 | grep "# pass" | tail -1` shows at least 153 passing tests
- New stub identifiers ST01, ST02, ST03, ST04, ST05, DB05, DB06, GT19 are all present in the suite output (either as failing or as todo)
- `require('socket.io-client').io` resolves to a function from the project root
</verification>

<success_criteria>
1. `package.json` lists `socket.io-client` ^4.x under `devDependencies`
2. `server/routes/socket.test.js` exists with 5 RED stubs (ST01–ST05) covering RT-01, RT-02, TV-02, TV-04, PLAY-01
3. `server/db/db.test.js` has DB05 (column existence) and DB06 (idempotency) — both RED
4. `server/routes/games.test.js` has GT19 (meta persistence) — RED
5. Running `node --test` exits non-zero (because of the intentional RED stubs) but discovers all 8 new test identifiers
6. Phase 1's 153 tests still pass — no regression from harness additions
</success_criteria>

<output>
Create `.planning/phases/02-real-time-tv/02-01-SUMMARY.md` when done. Include: dependencies installed, list of test files created/modified, count of RED stubs added (8), confirmation that Phase 1 153 tests still pass, and the exact node --test summary line.
</output>
</content>
</invoke>