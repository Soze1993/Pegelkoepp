---
phase: 04-club-features
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Claude/public/index.html
autonomous: false
requirements:
  - PERS-03
  - PERS-04
  - STAT-01
  - STAT-02
  - STAT-03

must_haves:
  truths:
    - "Spiele tab shows an 'Abend starten' button when no abend is active; clicking it opens the start modal"
    - "Start modal creates an abend via POST /api/abende; amber banner replaces the button with the abend name and a 'Beenden' button"
    - "Beenden confirms, calls POST /api/abende/:id/end, banner disappears, start button returns"
    - "Game history in Spiele tab groups games under their Abend name header; ungrouped games appear under 'Ohne Abend'"
    - "Stats tab renders real player cards with Siege/Niederlagen/Pudel% chips and expandable personal bests table"
    - "Bibliothek tab has an 'Eigene Spieltypen' section; custom types rendered with .stc.cu; delete with confirm"
    - "All user-supplied strings (abend name, type name, description) are set via textContent — no innerHTML"
  artifacts:
    - path: "Claude/public/index.html"
      provides: "All Phase 4 frontend features in the single-file app"
      contains: "S.aktAbend"
    - path: "Claude/public/index.html"
      provides: "Modal #m-start-abend in HTML"
      contains: "m-start-abend"
    - path: "Claude/public/index.html"
      provides: "Modal #m-neu-typ in HTML"
      contains: "m-neu-typ"
    - path: "Claude/public/index.html"
      provides: ".abend-banner CSS class"
      contains: "abend-banner"
  key_links:
    - from: "S.aktAbend state"
      to: "GET /api/abende/active in init()"
      via: "fetch in init() then S.aktAbend = abendData ? {id, name} : null"
      pattern: "S\\.aktAbend"
    - from: "renderStats()"
      to: "GET /api/stats"
      via: "async fetch; renders player cards from response array"
      pattern: "renderStats"
    - from: "renderBib()"
      to: "GET /api/game-types"
      via: "fetch on tab open; renders .stc.cu custom type cards"
      pattern: "api/game-types"
---

<objective>
Wire all three Phase 4 UI features into public/index.html: Kegelabend session controls (start/banner/end + game history grouping), real Stats tab with per-player cards and personal bests, and Bibliothek custom game type section with modal and delete flow.

Purpose: Delivers the user-facing club features that the Phase 4 backend (04-01) enabled.
Output: Modified public/index.html — same file, additive changes only.
</objective>

<execution_context>
@C:/Users/tobia/Claude/.planning/phases/execute-plan.md
@C:/Users/tobia/Claude/.planning/templates/summary.md
</execution_context>

<context>
@C:/Users/tobia/Claude/.planning/ROADMAP.md
@C:/Users/tobia/Claude/.planning/phases/04-club-features/04-CONTEXT.md
@C:/Users/tobia/Claude/.planning/phases/04-club-features/04-UI-SPEC.md
@C:/Users/tobia/Claude/.planning/phases/04-club-features/04-RESEARCH.md
@C:/Users/tobia/Claude/.planning/phases/03-frontend-wiring/03-03-SUMMARY.md
@C:/Users/tobia/Claude/.planning/phases/04-club-features/04-01-SUMMARY.md

<interfaces>
<!-- Current state of public/index.html — key anchors for the executor. -->

Global state object (var S = { ... } at line ~250):
  S.spieler: []          // player list from API
  S.typen: [...]         // 9 built-in game types (hardcoded)
  S.aktSpiel: null       // { gameId, state, type_key } or null
  // ADD: S.aktAbend: null  // { id, name } or null — NEW in Phase 4

init() function (line ~284) — loads players, active game, calls initSocket(), renderAll().
  // ADD: load S.aktAbend from GET /api/abende/active after loading players

renderAll() (line ~714):
  renderSpiele().catch(() => {}); renderSpielerListe(); renderSpielenTab(); renderBib(); renderStats();
  // renderStats() currently shows "Demnächst" placeholder — replace with real implementation

renderSpiele() (async function, line ~490):
  // Currently: fetches GET /api/games?status=finished, renders flat card list
  // MODIFY: also fetch GET /api/abende/active to get S.aktAbend; render abend banner above cards;
  //         group finished games by abend_id using abend headers; "Ohne Abend" at bottom
  // GET /api/games?status=finished now returns games with abend_id field (after 04-01)

renderBib() (function, line ~457):
  // Currently: renders built-in types from S.typen; custom types from S.typen where bi=false (empty)
  // MODIFY: add "Eigene Spieltypen" section below built-in types;
  //         fetch GET /api/game-types; render .stc.cu cards; include "+ Hinzufügen" button

renderStats() (function, line ~709):
  // Currently: shows empty placeholder
  // REPLACE with async function fetching GET /api/stats and rendering player cards

showTab(id) (line ~268):
  // Currently calls renderAll() on every tab switch
  // Stats and Bib tabs will be rendered by renderAll() — no change needed to showTab itself

openM(id) (line ~269):
  // Called when modals open. ADD case for 'm-start-abend' (clear input) and 'm-neu-typ' (clear inputs)

Existing modal pattern (from #m-neu-sp HTML, line ~142):
  <div class="mo" id="m-xxx"><div class="md">
    <h2>Title</h2>
    <div class="fg"><label>Label</label><input type="text" id="input-id" placeholder="..."></div>
    <div style="display:flex;gap:10px;margin-top:18px">
      <button class="btn bp" style="flex:1" onclick="submitFn()">Confirm</button>
      <button class="btn bg" onclick="closeM('m-xxx')">Abbrechen</button>
    </div>
  </div></div>

CSS classes already in index.html:
  .stc.cu — gold left border card (border-left:3px solid var(--ac))
  .chip, .chip.pc — stat chips; .chip.pc .v uses var(--red)
  .stbl — stats table (th + td styles)
  .uc, .uav, .uinfo — player card pattern
  .mo, .md — modal overlay + dialog
  .btn .bp/.bg/.bd .sm — button variants
  .stitle — section title (Bebas Neue, 20px, var(--ac))
  .badge .bamb/.bgry — inline badges
  .empty — empty state container
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Abend session controls — state, init, modal HTML, renderSpiele grouping</name>
  <files>Claude/public/index.html</files>
  <read_first>
    - Claude/public/index.html — FULL FILE before making any changes. Key sections:
        lines 1-110: CSS (add .abend-banner class at end of <style> block)
        lines 242-264: var S = {...} object definition (add S.aktAbend: null)
        lines 284-299: init() function body (add abend fetch)
        lines 490-512: renderSpiele() function (add banner, grouping)
        lines 142-155: #m-neu-sp modal HTML (pattern for #m-start-abend)
        lines 269: openM(id) function (add m-start-abend case)
        line 269: closeM(id) function
        lines 714: renderAll() (no change needed)
  </read_first>
  <behavior>
    When S.aktAbend is null: renderSpiele() prepends a "▶ Abend starten" (.btn.bp) button above the game cards.
    When S.aktAbend is non-null: renderSpiele() prepends a .abend-banner div with "📍 Abend läuft:" label, name in bold, and "■ Beenden" (.btn.bd.sm) button.
    Clicking "▶ Abend starten" opens modal #m-start-abend.
    Clicking "■ Beenden" calls confirm("Abend '{name}' beenden?") → if confirmed, POST /api/abende/:id/end → S.aktAbend = null → re-render Spiele tab.
    Modal #m-start-abend: input #m-abend-name, "Starten" button calls startAbend(), "Abbrechen" closes modal.
    startAbend(): reads #m-abend-name value, POST /api/abende with {name} (empty string → server uses default), on success: S.aktAbend = {id, name from response}, closeM, re-render Spiele (call renderSpiele directly, not renderAll).
    Finished games in renderSpiele() are grouped by abend_id. Build an abend map from GET /api/abende (list all abende). Games with abend_id render under their abend's name header; games with abend_id = null render under "Ohne Abend" at bottom. Each group header is a div with stitle style (13px, var(--mut)).
    init() — after loading S.spieler, fetch GET /api/abende/active → S.aktAbend = data ? {id: data.id, name: data.name} : null.
    init() loads S.aktAbend BEFORE calling renderAll() so the Spiele tab renders correctly on first load.
  </behavior>
  <action>
    Edit public/index.html — additive changes only. Do NOT rewrite the file. Apply targeted edits:

    1. CSS ADDITION — add at end of <style> block, before closing </style>:
       .abend-banner{background:var(--bg2);border:1px solid var(--ac);border-left:3px solid var(--ac);border-radius:10px;padding:12px 14px;display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}

    2. STATE — in var S = {...} (line ~263), add S.aktAbend: null after S.aktSpiel: null.

    3. INIT FUNCTION — after `S.spieler = await playersRes.json();` and before the active game fetch, add:
       var abendRes = await fetch('/api/abende/active');
       var abendData = await abendRes.json();
       S.aktAbend = abendData ? { id: abendData.id, name: abendData.name } : null;

    4. OPENM FUNCTION — in the openM(id) function body, add:
       if (id === 'm-start-abend') { var inp = document.getElementById('m-abend-name'); if (inp) inp.value = ''; }
       if (id === 'm-neu-typ') { var tn = document.getElementById('m-typ-name'); var td = document.getElementById('m-typ-desc'); if (tn) tn.value = ''; if (td) td.value = ''; }

    5. HTML MODALS — add two new modals BEFORE </div> (closing the .app div) and BEFORE <div class="notif" ...>:
       Modal #m-start-abend: heading "Abend starten", .fg with label "Name (optional)" + input #m-abend-name placeholder "z.B. Freitagsrunde (optional)", helper text div (11px, muted) "Leer lassen für automatischen Namen", button row: .btn.bp "Starten" onclick=startAbend(), .btn.bg "Abbrechen" onclick=closeM('m-start-abend').
       Modal #m-neu-typ: heading "Neuer Spieltyp", .fg with label "Name" + input #m-typ-name placeholder "Name...", .fg with label "Regeln / Beschreibung" + textarea #m-typ-desc placeholder "Wie wird gespielt?...", button row: .btn.bp "Hinzufügen" onclick=addCustomTyp(), .btn.bg "Abbrechen" onclick=closeM('m-neu-typ').

    6. ABEND FUNCTIONS — add after the submitUndo function block:
       async function startAbend() — reads #m-abend-name.value.trim(); POST /api/abende with JSON body {name: value} (server ignores empty string and uses default); on success (res.ok): S.aktAbend = {id: data.id, name: data.name}; closeM('m-start-abend'); renderSpiele().catch(function(){}); else notify('Fehler beim Starten', 'var(--red)').
       async function endAbend() — var name = S.aktAbend ? S.aktAbend.name : ''; if (!confirm('Abend \'' + name + '\' beenden?')) return; POST /api/abende/ + S.aktAbend.id + '/end'; on success: S.aktAbend = null; renderSpiele().catch(function(){}).

    7. RENDERSPIELE FUNCTION — replace the existing renderSpiele() async function with a new version that:
       a. Fetches GET /api/abende/active → updates S.aktAbend
       b. Fetches GET /api/abende → builds abendeMap {id: {name, started_at}}
       c. Fetches GET /api/games?status=finished → games array
       d. Builds abend banner HTML: if S.aktAbend, create .abend-banner div using createElement/textContent (NOT innerHTML for the name). Pattern: create outer div with className 'abend-banner', left div with "📍 Abend läuft: " text + bold name span (set via textContent), right button (.btn.bd.sm) with textContent "■ Beenden" and onclick = endAbend.
       e. Builds header (existing "Alle Spiele" header with "+ Neues Spiel" button).
       f. Groups games: group by abend_id. For each abend that has games: render group header div (stitle style 13px, var(--mut), textContent = abend name + " (" + formatted date + ")") + game cards. Then render "Ohne Abend" group if any games have abend_id = null.
       g. Sets el.innerHTML only for non-user-content parts. For abend name in banner: use DOM createElement + textContent to prevent XSS.
       IMPORTANT: game cards (type name, date) are from S.typen (builtin names trusted) or fallback type_key — these are safe. abend name from DB MUST use textContent.
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>
    S.aktAbend initialized in init() before renderAll().
    .abend-banner CSS class present in <style> block.
    Modals #m-start-abend and #m-neu-typ exist in HTML.
    renderSpiele() renders the banner (when active abend) or the start button (when no active abend).
    Game history groups by abend, "Ohne Abend" at bottom.
    No innerHTML usage with DB-sourced abend name (use textContent/createElement).
    node --test still passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stats tab + Bibliothek custom types section</name>
  <files>Claude/public/index.html</files>
  <read_first>
    - Claude/public/index.html — read the CURRENT file state after Task 1 edits. Key sections:
        lines ~452-457: renderBib() function (to extend with custom types section)
        lines ~708-712: renderStats() function (to replace entirely)
        lines ~714: renderAll() (no change)
        CSS chip classes (~67-69): .chip, .chip.pc, .chip .v, .chip .l
        CSS .uc, .uav, .uinfo pattern (~63-66)
        CSS .stbl (~43-46) — table style for personal bests
        CSS .stc.cu (~84-85) — gold left border
  </read_first>
  <behavior>
    renderStats() fetches GET /api/stats. Shows loading state while fetching. If response array is empty OR total wins+losses+draws for all players is zero: shows .empty state "Noch keine Spiele gespielt." etc.
    Otherwise renders one .uc card per player: .uav emoji, .uinfo .nm name; chips row (3 chips): Siege (wins, gold), Niederl. (losses, muted), Pudel% (pudel_pct, .chip.pc red). Below chips: a "▼ Bestleistungen" toggle div (13px, muted, cursor:pointer). On click: toggles visibility of a .stbl personal bests table for that player.
    Personal bests table columns: "Spieltyp" | "Bestleistung". type_key is mapped to display name via S.typen lookup (S.typen.find(t => t.id === type_key)?.name || type_key). Score displayed using .snum class. If no personal_bests: show "Noch keine Bestleistungen" row.
    renderBib() extended: after rendering built-in type cards, add "Eigene Spieltypen" section header (stitle 11px muted + "+ Hinzufügen" .btn.bp.sm button right). Fetch GET /api/game-types. For each custom type: render .stc.cu card with name (bold, .nm font-weight 600), description (muted 13px), delete button (.btn.bd.sm "Löschen" onclick=delCustomTyp(id)). If no custom types: .empty "Noch keine eigenen Spieltypen." etc.
    addCustomTyp(): reads #m-typ-name and #m-typ-desc; validates name non-empty; POST /api/game-types; on success: closeM('m-neu-typ'); renderBib(); on 409: notify('Dieser Name existiert bereits', 'var(--red)'); on other error: notify('Fehler', 'var(--red)').
    delCustomTyp(id): confirm('Spieltyp löschen?') → DELETE /api/game-types/:id → renderBib().
    showTab() calls renderAll() which calls both renderStats() and renderBib() — no change to tab switching needed.
    All name/description DOM values: use textContent or createElement — never innerHTML with DB-sourced strings.
  </behavior>
  <action>
    Edit public/index.html — additive changes only. Apply targeted edits:

    1. RENDERSTATS FUNCTION — replace the existing renderStats() function (currently at line ~709) with an async version:
       async function renderStats() {
         var el = document.getElementById('r-stats');
         el.innerHTML = '<div class="empty"><p>Laden…</p></div>';
         var stats;
         try { stats = await fetch('/api/stats').then(function(r){return r.json();}); }
         catch(e) { el.innerHTML = '<div class="empty"><div class="icon">📊</div><p>Laden fehlgeschlagen.</p></div>'; return; }
         if (!stats.length || stats.every(function(p){return !p.wins && !p.losses && !p.draws;})) {
           el.innerHTML = '<div class="stitle">Statistik</div><div class="empty"><div class="icon">🎳</div><p>Noch keine Spiele gespielt.<br><span style="color:var(--mut);font-size:12px">Spielt ein Spiel zu Ende, um Statistiken zu sehen.</span></p></div>';
           return;
         }
         // Build DOM using createElement for user-data fields
         var title = document.createElement('div'); title.className = 'stitle'; title.textContent = 'Statistik';
         var frag = document.createDocumentFragment(); frag.appendChild(title);
         stats.forEach(function(p) {
           // .uc card container
           var card = document.createElement('div'); card.className = 'uc'; card.style.flexDirection = 'column'; card.style.alignItems = 'flex-start'; card.style.cursor = 'default';
           // top row: avatar + name + chips
           var topRow = document.createElement('div'); topRow.style.cssText = 'display:flex;align-items:center;gap:12px;width:100%';
           var av = document.createElement('div'); av.className = 'uav'; av.textContent = p.emoji;
           var info = document.createElement('div'); info.className = 'uinfo';
           var nm = document.createElement('div'); nm.className = 'nm'; nm.textContent = p.name;
           info.appendChild(nm);
           var chips = document.createElement('div'); chips.style.cssText = 'display:flex;gap:6px;margin-left:auto;flex-shrink:0';
           function mkChip(val, label, extra) { var c = document.createElement('div'); c.className = 'chip' + (extra ? ' ' + extra : ''); var v = document.createElement('div'); v.className = 'v'; if (extra === 'pc') v.style.color = 'var(--red)'; v.textContent = val; var l = document.createElement('div'); l.className = 'l'; l.textContent = label; c.appendChild(v); c.appendChild(l); return c; }
           chips.appendChild(mkChip(p.wins, 'Siege'));
           chips.appendChild(mkChip(p.losses, 'Niederl.'));
           chips.appendChild(mkChip(p.pudel_pct + '%', 'Pudel%', 'pc'));
           topRow.appendChild(av); topRow.appendChild(info); topRow.appendChild(chips);
           card.appendChild(topRow);
           // bests toggle
           var bestsToggle = document.createElement('div');
           bestsToggle.style.cssText = 'font-size:13px;color:var(--mut);cursor:pointer;margin-top:10px;width:100%';
           bestsToggle.textContent = '▼ Bestleistungen';
           var bestsTable = document.createElement('div'); bestsTable.style.display = 'none'; bestsTable.style.marginTop = '8px'; bestsTable.style.width = '100%';
           if (p.personal_bests && p.personal_bests.length) {
             var tbl = document.createElement('table'); tbl.className = 'stbl';
             var thead = '<thead><tr><th>Spieltyp</th><th style="text-align:right">Bestleistung</th></tr></thead>';
             tbl.innerHTML = thead;
             var tbody = document.createElement('tbody');
             p.personal_bests.forEach(function(b) {
               var tr = document.createElement('tr');
               var tdTyp = document.createElement('td'); var typ = S.typen.find(function(t){return t.id===b.type_key;}); tdTyp.textContent = typ ? typ.name : b.type_key;
               var tdScore = document.createElement('td'); tdScore.className = 'snum'; tdScore.style.textAlign = 'right'; tdScore.textContent = b.score;
               tr.appendChild(tdTyp); tr.appendChild(tdScore); tbody.appendChild(tr);
             });
             tbl.appendChild(tbody); bestsTable.appendChild(tbl);
           } else {
             var noB = document.createElement('div'); noB.style.cssText = 'font-size:12px;color:var(--mut);padding:6px 0'; noB.textContent = 'Noch keine Bestleistungen'; bestsTable.appendChild(noB);
           }
           bestsToggle.addEventListener('click', function() {
             var open = bestsTable.style.display !== 'none';
             bestsTable.style.display = open ? 'none' : 'block';
             bestsToggle.textContent = (open ? '▼' : '▶') + ' Bestleistungen';
           });
           card.appendChild(bestsToggle); card.appendChild(bestsTable);
           frag.appendChild(card);
         });
         el.innerHTML = ''; el.appendChild(frag);
       }
       Note: The function is async; update renderAll() call to renderStats().catch(function(){}) if not already wrapped (it may need await or catch handling since renderAll is not async).

    2. RENDERBIB FUNCTION — replace renderBib() with an async version that:
       a. Renders the existing built-in types section exactly as before (copy the current card rendering logic).
       b. Appends "Eigene Spieltypen" section:
          - Header row: div with .stitle (11px, muted) textContent "Eigene Spieltypen" and .btn.bp.sm button textContent "+ Hinzufügen" onclick=openM('m-neu-typ').
          - Fetch GET /api/game-types.
          - For each custom type: createElement .stc.cu card. Title div: textContent = typ.name (bold). Description div: textContent = typ.description || '' (muted 13px). Delete button: .btn.bd.sm, textContent "Löschen", onclick closure delCustomTyp(typ.id). Append to section.
          - If no custom types: .empty div textContent.
       c. Set el.innerHTML = '' then appendChild the built fragment (or use innerHTML for the static built-in section + createElement for custom type names/descriptions).

    3. ADD FUNCTIONS after the endAbend() function:
       async function addCustomTyp() — reads name = document.getElementById('m-typ-name').value.trim(); if (!name) { notify('Name eingeben', 'var(--red)'); return; } desc = document.getElementById('m-typ-desc').value.trim(); res = await fetch('/api/game-types', {method:'POST', headers:{'content-type':'application/json'}, body: JSON.stringify({name, description: desc || null})}); if (res.status === 409) { notify('Dieser Name existiert bereits', 'var(--red)'); return; } if (!res.ok) { notify('Fehler', 'var(--red)'); return; } closeM('m-neu-typ'); renderBib().catch(function(){});
       async function delCustomTyp(id) — if (!confirm('Spieltyp löschen?')) return; res = await fetch('/api/game-types/' + id, {method:'DELETE'}); if (!res.ok) { notify('Fehler', 'var(--red)'); return; } renderBib().catch(function(){});

    4. RENDERALL UPDATE — renderAll() currently calls renderStats() and renderBib() synchronously. Since both are now async, update to add .catch(function(){}) wrappers:
       function renderAll(){renderSpiele().catch(function(){}); renderSpielerListe(); renderSpielenTab(); renderBib().catch(function(){}); renderStats().catch(function(){});}

    TEXTCONTENT RULE (hard requirement per T-02-02): All DB-sourced strings (abend name, custom type name, custom type description) MUST be set via textContent or DOM methods — never via innerHTML interpolation.
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>
    renderStats() is async and fetches /api/stats — renders player cards with chips and expandable personal bests.
    renderBib() includes "Eigene Spieltypen" section below built-in types.
    addCustomTyp() and delCustomTyp() functions exist.
    Custom type names/descriptions set via textContent throughout.
    renderAll() uses .catch(function(){}) wrappers on all async renders.
    node --test still passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — full Phase 4 feature walk-through</name>
  <what-built>
    All Phase 4 frontend features wired in public/index.html:
    - Kegelabend session controls (start/banner/end) in Spiele tab
    - Game history grouped by abend under named headers
    - Stats tab with real player cards, win/loss/pudel% chips, expandable personal bests
    - Bibliothek tab with "Eigene Spieltypen" section, add modal, delete with confirm
  </what-built>
  <how-to-verify>
    Prerequisites: npm start, browser logged in (use correct PIN).

    === Kegelabend (PERS-04) ===
    1. Go to Spiele tab. Expect: "▶ Abend starten" gold button at top.
    2. Click "▶ Abend starten" → modal opens. Enter name "Freitagsrunde" → click "Starten".
       Expect: amber .abend-banner appears "📍 Abend läuft: Freitagsrunde" with "■ Beenden" button. Button is gone.
    3. Start and complete one game of any type. Return to Spiele tab.
       Expect: finished game appears under "Freitagsrunde" group header.
    4. Click "■ Beenden" → confirm dialog "Abend 'Freitagsrunde' beenden?" → confirm.
       Expect: banner disappears, "▶ Abend starten" button returns.
    5. Reload page (F5). Expect: Spiele tab still shows no active abend (start button).
    6. Start another abend with NO name entered → click "Starten".
       Expect: banner shows today's date in German format (e.g. "Fr. 23.05.").

    === Statistics (STAT-01, STAT-02, STAT-03) ===
    7. Click Stats tab. If no finished games: expect empty state "Noch keine Spiele gespielt."
    8. If finished games exist: expect player cards with emoji, name, 3 chips (Siege, Niederl., Pudel%).
    9. Click "▼ Bestleistungen" on a player card → table expands showing game type + score. Arrow flips to ▶.
       Click again → table collapses.
    10. Verify a known game: e.g. after a dreiVollen win with score 18, player's Siege chip shows >= 1.

    === Custom Game Types (PERS-03) ===
    11. Click Bibliothek tab. Scroll to "Eigene Spieltypen" section.
        Expect: "+ Hinzufügen" button. Empty state "Noch keine eigenen Spieltypen."
    12. Click "+ Hinzufügen" → modal opens. Enter name "Kurs-Kegeln", description "3 Runden auf Hindernisparcours". Click "Hinzufügen".
        Expect: modal closes; custom type card appears with gold left border.
    13. Check card: name "Kurs-Kegeln" visible (textContent not innerHTML).
    14. Click "Löschen" → confirm dialog → confirm.
        Expect: card removed, empty state returns.
    15. Restart server (npm restart) and reload browser. Check Bibliothek tab.
        Expect: previously created custom types still appear (if any were created before step 14).

    === Regression ===
    16. node --test — all tests pass (0 failures).
    17. Start and finish a game end-to-end through existing game UI — ensure Phase 3 wiring is unbroken.
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any failures with which step failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB→browser (DOM) | custom type names/descriptions and abend names are user-supplied; must never be set via innerHTML |
| browser→API (write) | POST /api/abende, POST /api/game-types, DELETE /api/game-types require session — enforced server-side; browser UI gated by auth check in init() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering (XSS) | renderBib custom type name/description | mitigate | Use textContent/createElement for all DB-sourced strings; never innerHTML with DB values |
| T-04-08 | Tampering (XSS) | renderStats player name | mitigate | player.name set via textContent in createElement approach |
| T-04-09 | Tampering (XSS) | abend banner name display | mitigate | abend name set via span.textContent inside createElement — not string interpolation |
| T-04-10 | Spoofing | POST /api/abende, POST /api/game-types from UI | accept | Server enforces requireSession; UI calls are opportunistic helpers only |
| T-04-SC | Tampering | npm installs | accept | No new packages in Phase 4 |
</threat_model>

<verification>
Full phase verification (frontend):

1. node --test — 0 failures (all prior + backend tests still green)
2. Spiele tab shows "Abend starten" button when no active abend; amber banner when active
3. Starting abend with empty name → server-assigned German date default appears in banner
4. Finishing abend clears banner; reload confirms S.aktAbend loaded correctly from API
5. Game history groups by abend name; "Ohne Abend" shows games without abend_id
6. Stats tab renders player cards with win/loss/pudel% chips from real API data
7. Personal bests table toggles open/closed per player
8. Bibliothek "Eigene Spieltypen" section: add modal → custom type card appears; delete → gone
9. Custom type name/description rendered via textContent (verify by entering `<b>XSS</b>` as name — must appear as literal text, not bold)
10. Server restart preserves custom types in DB; Bibliothek still shows them after reload
</verification>

<success_criteria>
- All visual elements from UI-SPEC.md sections 1, 2, 3 implemented in public/index.html
- D-01 through D-12 from CONTEXT.md are all addressed in this plan or 04-01
- S.aktAbend = null | {id, name} in global state object
- .abend-banner CSS class present in <style> block
- #m-start-abend and #m-neu-typ modals in HTML
- renderStats() is async, fetches /api/stats, renders .uc cards with chips and .stbl bests
- renderBib() fetches /api/game-types, renders .stc.cu custom type cards
- All DB-sourced strings set via textContent — no innerHTML injection (T-02-02)
- Human checkpoint approved
</success_criteria>

<output>
Create Claude/.planning/phases/04-club-features/04-02-SUMMARY.md when done.
Format: standard SUMMARY.md template from @C:/Users/tobia/Claude/.planning/templates/summary.md
</output>
