---
phase: 01-backend-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - server/routes/games.js
  - server/routes/games.test.js
  - server/app.js
  - server/server.js
autonomous: false
requirements:
  - BACK-03
  - PERS-02
tags:
  - games-api
  - throw-persistence
  - crash-recovery

must_haves:
  truths:
    - "POST /api/games with valid session + valid type_key + player_ids creates a row in `games`, rows in `game_players`, an in-memory state via initState, and returns 201 { id, type_key, status: 'active' }"
    - "POST /api/games without session returns 401; with unknown type_key returns 400; with invalid player_ids returns 400"
    - "POST /api/games/:id/throws with valid session writes a row to `throws` synchronously BEFORE returning the response, then updates in-memory state via applyThrow"
    - "Duplicate throw submission (same game_id + player_id + throw_index) returns 409 (UNIQUE constraint surfacing)"
    - "GET /api/games/:id returns the current state — if in-memory cache is empty, the server reconstructs the state by calling initState(players) then replaying all throws from the DB in throw_index order"
    - "After server restart, GET /api/games/:id of an active game returns the SAME state that was live before the restart (crash recovery — BACK-03/C2 verified end-to-end)"
    - "When a throw makes the game finished, the games row status flips to 'finished' and finished_at is set"
    - "The in-memory activeGames Map is rebuilt at server startup for all rows with status='active'"
  artifacts:
    - path: "server/routes/games.js"
      provides: "POST /, GET /:id, POST /:id/throws, plus rebuildActiveGames helper exported for server.js"
      contains: "activeGames"
    - path: "server/app.js"
      provides: "Mounts /api/games router (extended from Plans 01+02)"
      contains: "/api/games"
    - path: "server/server.js"
      provides: "Calls rebuildActiveGames(db) AFTER seed and BEFORE listen — crash recovery"
      contains: "rebuildActiveGames"
  key_links:
    - from: "server/routes/games.js"
      to: "server/game-types/index.js"
      via: "require('../game-types')[type_key]"
      pattern: "gameTypes\\["
    - from: "server/routes/games.js"
      to: "server/db/index.js"
      via: "db.prepare('INSERT INTO throws ...').run(...)"
      pattern: "INSERT INTO throws"
    - from: "server/routes/games.js"
      to: "server/middleware/auth.js"
      via: "requireSession on POST routes"
      pattern: "requireSession"
    - from: "server/server.js"
      to: "server/routes/games.js"
      via: "require('./routes/games').rebuildActiveGames(db)"
      pattern: "rebuildActiveGames"
---

<objective>
Wire the three previous plans into a single working game flow: PIN-authenticated client starts a game (Plan 02 auth + Plan 03 module via type_key), submits throws that are persisted synchronously (BACK-03 + C2 + C3 from CONTEXT.md), and after a server crash the game state reconstructs identically from the `throws` table.

Purpose: Close Phase 1 by proving the end-to-end persistence loop. After this plan, every Phase 1 success criterion from ROADMAP.md is satisfied; Phase 2 (Socket.io real-time + TV display) builds on this exact route surface.

Output:
- server/routes/games.js with three handlers (start, submit throw, get state) and an in-memory `Map<gameId, state>` cache
- A `rebuildActiveGames(db)` helper that runs at server startup to restore in-memory state for any `status='active'` games left in the DB
- Integration tests proving crash recovery: kill the in-memory Map, reconstruct from DB, assert state equivalence
- All ROADMAP Phase 1 success criteria green
</objective>

## Phase Goal

**As a** Server während eines Kegelabends, **I want to** jeden Wurf sofort persistent zu speichern und nach einem Crash den exakten Spielzustand aus der DB wiederherzustellen, **so that** ein laufendes Spiel niemals verloren geht — auch nicht durch Stromausfall, Restart oder Bug.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-PATTERNS.md
@.planning/phases/01-backend-foundation/01-VALIDATION.md
@.planning/phases/01-backend-foundation/SKELETON.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md
@.planning/phases/01-backend-foundation/01-03-SUMMARY.md
@server/app.js
@server/server.js
@server/db/index.js
@server/middleware/auth.js
@server/game-types/index.js
@server/game-types/drei-vollen.js
@server/game-types/fuchsjagd.js
@kegelclub_12.html

<interfaces>
<!-- Interfaces from previous plans (already in codebase) -->

From server/db/index.js (Plan 01): better-sqlite3 instance, schema executed, WAL on.
From server/middleware/auth.js (Plan 02): `module.exports = requireSession` middleware.
From server/game-types/index.js (Plan 03): `{ [type_key]: module }` map for all 9 games.

Game module contract (Plan 03 — locked):
{ id, name, initState(players, config?), applyThrow(state, playerId, value, meta?), isFinished(state), getFinalResults(state) }

<!-- Interfaces this plan ESTABLISHES — Phase 2 (Socket.io) consumes them. -->

server/routes/games.js (created here):
```javascript
const router = require('express').Router();
// ... handlers ...
module.exports = router;
module.exports.activeGames = activeGames;       // Map<gameId, state>
module.exports.reconstructState = reconstructState; // (gameRow) → state
module.exports.rebuildActiveGames = rebuildActiveGames; // (db) → void; called from server.js at startup
```

API contract:
- POST /api/games
    auth: required
    body: { type_key: string, player_ids: number[], config?: object }
    → 201 { id, type_key, status: 'active' } | 400 | 401
- GET /api/games/:id
    auth: NONE (Phase 2 TV display reads this)
    → 200 { game, state, finished: boolean, results: Result[]|null } | 404
- POST /api/games/:id/throws
    auth: required
    body: { player_id: number, throw_index: number, value: number, meta?: object }
    → 200 { state, finished } | 400 | 401 | 404 | 409 (duplicate throw)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement games router (start + get + submit throw) with synchronous DB-first writes, in-memory cache, and reconstructState; wire into app.js</name>
  <files>server/routes/games.js, server/routes/games.test.js, server/app.js</files>
  <read_first>
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-PATTERNS.md (`server/routes/games.js` section — the full pattern including activeGames Map, reconstructState, synchronous DB write before applyThrow, UNIQUE→409 mapping)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-RESEARCH.md (REST API Design § Games; Pattern 5 module interface; Pitfalls 2, 4; Security Domain — prepared statements)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-CONTEXT.md (C2 crash recovery, C3 UNIQUE constraint, BACK-03)
    - C:/Users/tobia/Claude/server/game-types/index.js (Plan 03 — map shape)
    - C:/Users/tobia/Claude/server/game-types/drei-vollen.js (Plan 03 — sample module; simplest applyThrow shape)
    - C:/Users/tobia/Claude/server/game-types/fuchsjagd.js (Plan 03 — most complex applyThrow; verify your reconstruct loop works for it)
    - C:/Users/tobia/Claude/server/db/schema.sql (Plan 01 — table shapes, UNIQUE constraint)
    - C:/Users/tobia/Claude/server/middleware/auth.js (Plan 02 — requireSession export)
    - C:/Users/tobia/Claude/server/app.js (Plan 02 state — must add /api/games mount in the same place pattern)
  </read_first>
  <behavior>
    **Auth + validation tests:**
    - GT1: POST /api/games without session → 401.
    - GT2: POST /api/games with session and unknown type_key (e.g., 'asdf') → 400 with error message.
    - GT3: POST /api/games with session and empty player_ids array → 400.
    - GT4: POST /api/games with session and player_ids referencing archived or nonexistent players → 400.
    - GT5: POST /api/games/:id/throws without session → 401.
    - GT6: POST /api/games/:id/throws on a nonexistent game id → 404.

    **Happy path tests (using dreiVollen — the simplest module):**
    - GT7: POST /api/games with `{ type_key: 'dreiVollen', player_ids: [1,2] }` returns 201 with `{ id: <int>, type_key: 'dreiVollen', status: 'active' }`. The games row exists in DB with status='active'. Two game_players rows exist with seat 0 and 1.
    - GT8: POST /api/games/:id/throws with `{ player_id: 1, throw_index: 0, value: 5 }` returns 200. A throws row exists in DB matching the input. The response body's `state.players[0].wuerfe` contains [5].
    - GT9: Submitting 6 throws (3 each for 2 players) ends the game: response has `finished: true`, the games row status flips to 'finished', `finished_at` is non-null. activeGames Map no longer contains this gameId after the last throw.

    **Duplicate-throw protection:**
    - GT10: Submitting two throws with identical (game_id, player_id, throw_index) returns 409 on the second submission; the second insert does NOT mutate the in-memory state (UNIQUE constraint is checked BEFORE applyThrow runs).

    **GET (public, used by Phase 2 TV display):**
    - GT11: GET /api/games/:id of an active game (with NO session cookie) returns 200 with `{ game, state, finished: false, results: null }`. Confirms the route is unauthenticated (so the TV can read it in Phase 2).
    - GT12: GET /api/games/:id on a finished game includes `results` (the array from getFinalResults).

    **In-memory cache + DB-source-of-truth:**
    - GT13: Start a game, submit 2 throws, then `activeGames.delete(gameId)` directly (simulates a cache eviction). The next GET /api/games/:id rebuilds the state by calling reconstructState — the returned state's wuerfe arrays match the 2 submitted throws.
    - GT14: Same scenario but with fuchsjagd — submit a known throw sequence, delete from cache, GET, assert state matches the live state computed inline by running initState + applyThrow on the same data.

    **Crash recovery proof (the BACK-03 test):**
    - GT15: Start a game, submit 3 throws, then COMPLETELY tear down the in-memory state (close DB? not needed; just delete activeGames entries). Then re-open the DB from the SAME file path and call `rebuildActiveGames(db)` (the helper exported from games.js — implemented in Task 2). Assert that activeGames now contains the gameId with a state identical to a fresh in-line replay of the 3 throws. This proves the entire crash-recovery loop end-to-end inside one test process.
  </behavior>
  <action>
    Write tests FIRST.

    1. Create server/routes/games.js. The full skeleton follows PATTERNS.md `server/routes/games.js`. Key additions / contracts to enforce:

       **Module top:**
       ```
       const { Router } = require('express');
       const db = require('../db');
       const requireSession = require('../middleware/auth');
       const gameTypes = require('../game-types');
       const router = Router();

       // In-memory active game states. Source of truth = throws table; this is a perf cache.
       const activeGames = new Map();
       ```

       **POST / — start a game** (requireSession). Validate type_key in gameTypes; resolve every player_id (must be active in players table); INSERT games row → INSERT game_players rows (in a single `db.transaction`); call `module.initState(players)` and `activeGames.set(gameId, state)`; respond 201.

       **GET /:id — read current state** (NO requireSession; needed by TV in Phase 2). Lookup the game row; if missing → 404. Look up `activeGames.get(id)`; if absent, call `reconstructState(game)`. Return `{ game, state, finished: module.isFinished(state), results: finished ? module.getFinalResults(state) : null }`.

       **POST /:id/throws — submit a throw** (requireSession). The exact ordering MUST be:
         a. Validate game exists and status='active' (404 if not).
         b. Validate `value` is integer, `player_id` is integer, `throw_index` is integer. 400 otherwise.
         c. Synchronously INSERT into throws table. Wrap in try/catch; if the error message contains 'UNIQUE', return 409 `{ error: 'Duplicate throw' }`. (RESEARCH.md Security Domain — UNIQUE race surface; CONTEXT.md C3.)
         d. ONLY after a successful INSERT: get current state from activeGames or reconstruct; call `module.applyThrow(state, player_id, value, meta)`; `activeGames.set(gameId, newState)`.
         e. If `module.isFinished(newState)`: UPDATE games SET status='finished', finished_at=datetime('now') WHERE id=?; activeGames.delete(gameId).
         f. Respond 200 with `{ state: newState, finished }`.

       This DB-first order is non-negotiable (CONTEXT.md C2 + RESEARCH.md "Synchronous DB Write Before Response" shared pattern). Test GT10 enforces it.

       **reconstructState(game) helper** (per PATTERNS.md):
       ```
       function reconstructState(game) {
         const module = gameTypes[game.type_key];
         const players = db.prepare(
           'SELECT p.id, p.name, p.emoji FROM players p ' +
           'JOIN game_players gp ON p.id = gp.player_id ' +
           'WHERE gp.game_id = ? ORDER BY gp.seat'
         ).all(game.id);
         const throws = db.prepare(
           'SELECT player_id, throw_index, value FROM throws ' +
           'WHERE game_id = ? ORDER BY throw_index ASC'
         ).all(game.id);
         let state = module.initState(players);
         for (const t of throws) {
           state = module.applyThrow(state, t.player_id, t.value);
         }
         return state;
       }
       ```
       Note: PATTERNS.md notes that some games need `meta` for applyThrow (grosseHaus slot, vier-gewinnt). The `throws` table currently stores only (game_id, player_id, throw_index, value) — no meta column. For Phase 1, persistence of meta is OUT OF SCOPE: dreiVollen and fuchsjagd (the test targets) do not need meta. Document this limitation in a code comment near reconstructState:
       ```
       // NOTE: The throws table does not yet store throw metadata (e.g., the slot for
       // grosseHaus, the pudel flag for viergewinnt). Phase 1 reconstructs only the
       // games whose modules do not require meta. Persisting meta is a Phase 2 task
       // before grosseHaus / kleineHaus / viergewinnt games can be played through
       // the API. See SUMMARY.md notes.
       ```
       This is an explicit, documented limitation — not a silent gap.

       **rebuildActiveGames(db) helper** — IMPLEMENTED in Task 2 but EXPORTED from games.js. Stub it here with an empty body and a TODO that Task 2 fills in:
       ```
       function rebuildActiveGames(database) {
         // Implemented in Task 2 (crash recovery wiring).
       }
       ```

       **Exports:**
       ```
       module.exports = router;
       module.exports.activeGames = activeGames;
       module.exports.reconstructState = reconstructState;
       module.exports.rebuildActiveGames = rebuildActiveGames;
       ```

    2. Edit server/app.js to mount the games router. Add ONE line near the other route mounts:
       ```
       app.use('/api/games', require('./routes/games'));
       ```

    3. Create server/routes/games.test.js implementing tests GT1-GT15. Setup pattern (reuse from Plan 02 tests):
       - Tmp DB path via fs.mkdtempSync
       - Bcrypt hash of test PIN '1234' → process.env.PIN_HASH
       - SESSION_SECRET set
       - require('../app') → require('../db') (the same instance shared with the app) → require('../db/seed')(db)
       - Login helper that returns the session cookie string
       - For tests that need cache eviction (GT13, GT14, GT15), `require('../routes/games').activeGames.delete(gameId)` directly.
       - For GT15, also `require('../routes/games').rebuildActiveGames(db)`. In this task the helper is a no-op, so GT15 will FAIL — that's expected. Task 2 implements it and makes GT15 pass.

    4. Run `node --test server/routes/games.test.js`. Tests GT1-GT14 must pass. GT15 will fail until Task 2 is complete — mark it as expected and continue.

    Do NOT modify the throws table schema in this task. Do NOT add a meta column. The Phase 1 success-criterion test target (dreiVollen + fuchsjagd) does not need it. The limitation is documented in code and SUMMARY.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/routes/games.test.js 2>&1 | tee /tmp/games-task1.log; grep -E "ok 14|tests 14" /tmp/games-task1.log >/dev/null && echo "14 of 15 expected" || true</automated>
  </verify>
  <done>
    games.js exists with three handlers and exports activeGames, reconstructState, and a stub rebuildActiveGames; app.js mounts /api/games; tests GT1-GT14 (14 of 15) pass; GT15 fails with a clear message about rebuildActiveGames being a no-op (Task 2 completes it); schema unchanged; DB-first write ordering verified by GT10 (duplicate throw returns 409 before applyThrow runs).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rebuildActiveGames(db) and wire it into server.js; crash-recovery integration test (GT15) goes green</name>
  <files>server/routes/games.js, server/server.js, server/routes/games.test.js</files>
  <read_first>
    - C:/Users/tobia/Claude/server/routes/games.js (current state from Task 1 — the stub rebuildActiveGames + reconstructState helper are already there)
    - C:/Users/tobia/Claude/server/server.js (current state from Plan 01)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-CONTEXT.md (C2 — every throw written immediately; server reload reconstructs from throws)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-PATTERNS.md (`server/server.js`, `server/routes/games.js` — the activeGames pattern)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-RESEARCH.md (Pitfall 4 — Fuchsjagd state machine reconstructs identically from throw history; this is the test target for GT15)
  </read_first>
  <behavior>
    - RB1: `rebuildActiveGames(db)` queries `SELECT id FROM games WHERE status = 'active'`. For each row, it calls `reconstructState(row)` and inserts the result into the `activeGames` Map.
    - RB2: Calling `rebuildActiveGames(db)` on a DB with no active games is a no-op (does not throw, leaves activeGames untouched).
    - RB3: Calling `rebuildActiveGames(db)` is idempotent: calling it twice in a row produces the same Map contents as calling it once.
    - RB4: GT15 (from Task 1) now passes: tear down activeGames mid-test, call rebuildActiveGames(db), and the reconstructed state equals an inline replay of the recorded throws.
    - RB5: server.js calls rebuildActiveGames(db) AFTER seed(db) and BEFORE server.listen — verified by adding a log line and checking it appears in startup output, OR by writing a small smoke test that boots the app via require and asserts activeGames is populated when an active game exists in the DB.
  </behavior>
  <action>
    1. Edit server/routes/games.js: replace the stub `rebuildActiveGames` with the real implementation. Place it next to `reconstructState` (just above the `module.exports` block):
       ```
       function rebuildActiveGames(database) {
         // Use the passed-in database param if provided (e.g., from tests with isolated DB).
         // Fall back to the module-level `db` import otherwise.
         const conn = database || db;
         const activeRows = conn.prepare(
           "SELECT id, type_key, status, started_at, finished_at FROM games WHERE status = 'active'"
         ).all();
         for (const game of activeRows) {
           const module = gameTypes[game.type_key];
           if (!module) {
             console.warn(`rebuildActiveGames: skipping game ${game.id} — unknown type_key ${game.type_key}`);
             continue;
           }
           const state = reconstructState(game);
           activeGames.set(game.id, state);
         }
       }
       ```
       Note: `reconstructState` reads from the module-level `db` (closed over via the existing import). If tests inject a different `database` arg, the reconstruction will still query the module-level `db`. For Phase 1, this is acceptable because tests in this codebase set `process.env.DB_PATH` BEFORE require, so the module-level `db` IS the test's DB. Document this assumption in a code comment:
       ```
       // NOTE: reconstructState queries the module-level `db`. Callers in tests must
       // set process.env.DB_PATH before requiring this module so the singleton points
       // at the test DB. This holds for our test setup (see games.test.js) and for
       // server.js where DB_PATH is read from .env at boot time.
       ```

    2. Edit server/server.js to call rebuildActiveGames between seed and listen:
       ```
       require('dotenv').config();
       const http = require('http');
       const app = require('./app');
       const db = require('./db');
       const seed = require('./db/seed');
       const { rebuildActiveGames } = require('./routes/games');

       seed(db);
       rebuildActiveGames(db);

       const PORT = process.env.PORT || 3000;
       const server = http.createServer(app);
       server.listen(PORT, () => {
         const active = require('./routes/games').activeGames.size;
         console.log(`Pegelköpp server listening on port ${PORT} (${active} active game(s) recovered)`);
       });
       ```
       The recovery count is logged for human verification in Task 3.

    3. Edit server/routes/games.test.js: add RB1, RB2, RB3 if not already covered as part of GT15's chain. GT15 should now pass without any change. Re-run:
       `node --test server/routes/games.test.js` → ALL 15 tests green.

    4. Run the full suite: `node --test`. Expected count = 15 (Plan 01) + 23 (Plan 02) + ~9 contract tests × 9 modules + index test (Plan 03) + 15 (this plan) ≈ 100+ tests, all green. The exact number depends on how many sub-tests each module test file produced; the only hard requirement is that ALL tests pass.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/routes/games.test.js && node --test</automated>
  </verify>
  <done>
    rebuildActiveGames(db) is implemented and idempotent; server.js calls it between seed and listen; the startup log reports the count of recovered active games; GT15 passes; full `node --test` suite is green; no tests skipped, no tests pending.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — End-to-end Phase 1 success criteria (live server crash recovery)</name>
  <what-built>
    The complete Phase 1 backend foundation: PIN-authenticated REST API, 9 game-type modules, synchronous throw persistence to SQLite (WAL), and live crash-recovery of active games via rebuildActiveGames(). This checkpoint verifies all 5 Phase 1 success criteria from ROADMAP.md against a running server.
  </what-built>
  <how-to-verify>
    Two terminals. In Terminal A you will run the server; in Terminal B you will hit the API with curl. Follow each step in order. If ANY step fails, type the failure description instead of 'approved'.

    **Setup (one-time):**
    1. In Terminal A, create `.env` from the template (do NOT commit it):
       ```
       cp .env.example .env
       ```
    2. Generate a real PIN hash for the PIN `1234`:
       ```
       node -e "require('bcryptjs').hash('1234',10).then(h => console.log(h))"
       ```
       Copy the output (starts with `$2a$10$...`) and replace `PIN_HASH=` in `.env` with `PIN_HASH=<that hash>`.
    3. Generate a session secret:
       ```
       node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
       ```
       Replace `SESSION_SECRET=` in `.env` with `SESSION_SECRET=<that hex>`.
    4. Delete any leftover data: `rm -rf data` (PowerShell: `Remove-Item -Recurse -Force data`).

    **ROADMAP Phase 1 SC #1 — Server starts and players endpoint works:**
    5. Terminal A: `npm start`. Expected log includes `Pegelköpp server listening on port 3000 (0 active game(s) recovered)`.
    6. Terminal B: `curl http://localhost:3000/api/players` → JSON array of 12 players.

    **ROADMAP Phase 1 SC #2 — Player CRUD + archive:**
    7. Terminal B: Log in (capture the cookie):
       ```
       curl -i -X POST http://localhost:3000/api/auth/login -H 'content-type: application/json' -d '{"pin":"1234"}'
       ```
       Copy the `Set-Cookie: connect.sid=...` value (only the `connect.sid=...` part up to the first `;`).
    8. Create a player:
       ```
       curl -X POST http://localhost:3000/api/players -H 'content-type: application/json' -H 'cookie: <connect.sid=...>' -d '{"name":"Manni","emoji":"🍻"}'
       ```
       Expected: 201 JSON with the new id, name, emoji.
    9. Rename that player (use the id from step 8):
       ```
       curl -X PUT http://localhost:3000/api/players/<id> -H 'content-type: application/json' -H 'cookie: <cookie>' -d '{"name":"Manfred","emoji":"🍺"}'
       ```
       Expected: 200 `{ok:true}`.
    10. Archive that player:
        ```
        curl -X PUT http://localhost:3000/api/players/<id>/archive -H 'cookie: <cookie>'
        ```
        Expected: 200 `{ok:true}`.
    11. List players again: `curl http://localhost:3000/api/players` — Manfred no longer appears (archived).

    **ROADMAP Phase 1 SC #3 — Auth boundary:**
    12. `curl -X POST http://localhost:3000/api/players -H 'content-type: application/json' -d '{"name":"X"}'` (NO cookie) — expected: 401.
    13. `curl http://localhost:3000/tv` — expected: 200 HTML, no auth required.
    14. `curl -X POST http://localhost:3000/api/auth/login -H 'content-type: application/json' -d '{"pin":"wrong"}'` — expected: 401 with `{"error":"Falscher PIN"}`.

    **ROADMAP Phase 1 SC #4 — All 9 game modules execute:**
    15. Terminal B: `npm test`. Expected: all tests pass including 9 module test files. The test summary should mention zero failures.

    **ROADMAP Phase 1 SC #5 — Throw persistence + crash recovery (THE CRITICAL TEST):**
    16. Terminal B: Start a `dreiVollen` game with two of the seeded players (use ids 1 and 2):
        ```
        curl -X POST http://localhost:3000/api/games -H 'content-type: application/json' -H 'cookie: <cookie>' -d '{"type_key":"dreiVollen","player_ids":[1,2]}'
        ```
        Note the returned game id (call it `G`).
    17. Submit one throw:
        ```
        curl -X POST http://localhost:3000/api/games/<G>/throws -H 'content-type: application/json' -H 'cookie: <cookie>' -d '{"player_id":1,"throw_index":0,"value":7}'
        ```
        Expected: 200 with a state object showing player 1's wuerfe contains [7].
    18. Read state back: `curl http://localhost:3000/api/games/<G>` — confirm player 1 wuerfe is [7].
    19. **Kill the server** in Terminal A (Ctrl+C — a hard SIGINT, simulating crash).
    20. **Restart**: `npm start` in Terminal A. Expected log line:
        `Pegelköpp server listening on port 3000 (1 active game(s) recovered)`.
    21. Terminal B: `curl http://localhost:3000/api/games/<G>` — expected: same state as step 18; player 1 wuerfe is [7]; status is still 'active'. The throw survived the crash.
    22. Submit a second throw to confirm the rebuilt state accepts new throws:
        ```
        curl -X POST http://localhost:3000/api/games/<G>/throws -H 'content-type: application/json' -H 'cookie: <new-cookie-from-login-again>' -d '{"player_id":1,"throw_index":1,"value":3}'
        ```
        Expected: 200; player 1 wuerfe is now [7,3]. (You will need to log in again because the previous session was destroyed; the session store survives but the cookie is fresh.)

    **Duplicate throw safety:**
    23. Submit the EXACT same throw twice: `(player_id=2, throw_index=0, value=5)` then immediately the same again. First → 200; second → 409 `{"error":"Duplicate throw"}`. (CONTEXT.md C3 enforced.)

    **WAL files present:**
    24. `ls data/` — expected: `kegelclub.db`, `kegelclub.db-wal`, `kegelclub.db-shm`, `sessions.db` (and possibly `sessions.db-*` files). Confirms WAL mode active.

    **Final stop:** Ctrl+C in Terminal A. All Phase 1 success criteria from ROADMAP.md should now be checked.
  </how-to-verify>
  <resume-signal>Type 'approved' to mark Phase 1 complete and proceed to `/gsd:verify-work 01` (or Phase 2 planning if you skip the formal verify step). If any step failed, type the step number and the actual vs expected output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/games/:id/throws | Untrusted throw payload (player_id, value, throw_index) — must be validated before DB write |
| DB throws table → reconstructState | Trusted but defensive (state machine must handle whatever ordering the DB returns) |
| Server restart → activeGames Map | Recovery path: DB is source of truth, in-memory is rebuilt from scratch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Tampering | Concurrent duplicate throw submission (e.g., tablet double-tap, two tablets submitting same throw_index) | mitigate | UNIQUE constraint on (game_id, player_id, throw_index) in schema (Plan 01); games.js translates the DB error to HTTP 409 (Task 1); verified by GT10 + Task 3 step 23 |
| T-01-19 | Tampering | Submitting a throw to a finished game | mitigate | games.js checks status='active' BEFORE writing; returns 404 otherwise (Task 1 step 1d) |
| T-01-20 | Information Disclosure | GET /api/games/:id is unauthenticated (intentional, for TV display in Phase 2) — could leak game state to anyone with the URL | accept | Private internal app behind a club PIN at the network level (deferred to Phase 5 with HTTPS); leaking game scores has no value to an outside party (REQUIREMENTS.md Out of Scope: public homepage). Documented in SKELETON.md auth boundary. |
| T-01-21 | Repudiation | Crash mid-throw write → state divergence | mitigate | better-sqlite3 INSERT is synchronous + WAL atomic — partial writes impossible; if the INSERT fails the throw is not persisted AND not applied to memory (DB-first ordering — Task 1 step 1c→1d) |
| T-01-22 | Denial of Service | Malformed type_key in body causing module crash | mitigate | games.js validates `gameTypes[type_key]` exists before any module call (Task 1); 400 on missing/unknown |
| T-01-23 | Tampering | Type confusion on throws body (value as string "7", player_id as object) | mitigate | Task 1 step 1b: explicit integer check on value, player_id, throw_index → 400 if not |
| T-01-24 | Information Disclosure | Stack traces in error responses | mitigate | Error middleware from Plan 01 returns only `{error: err.message}`; stack logged to stderr only |
</threat_model>

<verification>
- Task 1 verify: `node --test server/routes/games.test.js` — 14 of 15 tests green (GT15 fails until Task 2)
- Task 2 verify: `node --test server/routes/games.test.js` — 15 of 15 green; `node --test` — entire Phase 1 suite green
- Task 3 verify: All 24 manual steps map directly to one of the 5 ROADMAP Phase 1 success criteria:
  - SC #1: steps 5, 6
  - SC #2: steps 7-11
  - SC #3: steps 12-14
  - SC #4: step 15 (the test suite includes all 9 module test files)
  - SC #5: steps 16-22 (live crash + recovery + replay correctness)
- Documented gap: throws table does not yet store meta (slot, pudel flag). dreiVollen and fuchsjagd work today; grosseHaus, kleineHaus, viergewinnt require a Phase 2 schema migration before they can play through the API. The 9 modules themselves are tested and ready (Plan 03).
</verification>

<success_criteria>
- [ ] BACK-03 + PERS-02 requirements covered: throws persist immediately and games survive restart (Task 3 step 21)
- [ ] All 5 ROADMAP Phase 1 success criteria pass under Task 3 manual verification
- [ ] DB-first write ordering enforced and tested (GT10 + Task 3 step 23 → 409 on duplicate)
- [ ] rebuildActiveGames called between seed and listen in server.js (Task 3 step 20 log line)
- [ ] activeGames Map is a cache — DB is the source of truth (GT13, GT14, GT15)
- [ ] GET /api/games/:id is unauthenticated (Task 3 step 18; needed by Phase 2 TV)
- [ ] No SQL string interpolation anywhere in games.js (grep gate below)
- [ ] No module imports DOM or browser APIs (Plan 03 sanity already verified; spot-check games.js for the same)
- [ ] Documented limitation about meta column persistence — explicit code comment + SUMMARY.md note for Phase 2
- [ ] `npm test` returns full Phase 1 suite green
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation/01-04-SUMMARY.md` when done. Include:
- Files created vs modified in this plan
- Test counts: GT1-GT15 (15) + any sub-tests = N; full Phase 1 suite total = M
- Output of Task 3 step 20 startup log (confirming '1 active game(s) recovered')
- Explicit list of which Phase 1 success criteria are satisfied by which test (mapping)
- Phase 2 carryover notes:
  - Throws table needs a `meta TEXT` column before grosseHaus/kleineHaus/viergewinnt are playable through the API (migration task)
  - Socket.io will publish to the same activeGames Map exported from games.js
  - GET /api/games/:id is already unauthenticated and ready for the TV display to poll initial state on reconnect
</output>
