---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - .env.example
  - server/server.js
  - server/app.js
  - server/db/index.js
  - server/db/schema.sql
  - server/db/seed.js
  - server/db/db.test.js
  - server/routes/players.js
  - server/routes/players.test.js
  - public/tv.html
  - public/index.html
autonomous: false
requirements:
  - PERS-01
  - PERS-02
  - AUTH-02
tags:
  - skeleton
  - express
  - sqlite
  - node

must_haves:
  truths:
    - "Server starts with `npm start` and listens on PORT (default 3000)"
    - "GET /api/players returns 200 with a JSON array of seeded Pegelköpp members"
    - "GET /tv returns 200 with the placeholder TV HTML and requires NO authentication"
    - "SQLite database file exists at data/kegelclub.db with WAL mode active"
    - "Restarting the server does not duplicate the seeded players"
  artifacts:
    - path: "package.json"
      provides: "Dependencies + npm scripts (start, dev, test, seed)"
      contains: '"better-sqlite3"'
    - path: ".env.example"
      provides: "PIN_HASH, SESSION_SECRET, PORT, NODE_ENV placeholders"
      contains: "PIN_HASH="
    - path: "server/db/index.js"
      provides: "better-sqlite3 instance with WAL mode, exports `db`"
      contains: "journal_mode = WAL"
    - path: "server/db/schema.sql"
      provides: "DDL for players, games, game_players, throws, game_type_defs"
      contains: "UNIQUE (game_id, player_id, throw_index)"
    - path: "server/db/seed.js"
      provides: "Idempotent seed of 12 Pegelköpp members"
      contains: "COUNT(*)"
    - path: "server/app.js"
      provides: "Express app with helmet, morgan, JSON body, static + /tv + /api/players routes"
      contains: "app.use(helmet"
    - path: "server/server.js"
      provides: "HTTP entry point; runs seed; listens on PORT"
      contains: "server.listen"
    - path: "server/routes/players.js"
      provides: "GET /api/players (public, active only)"
      contains: "archived = 0"
    - path: "public/tv.html"
      provides: "Placeholder TV display"
      contains: "Pegelköpp"
  key_links:
    - from: "server/server.js"
      to: "server/app.js"
      via: "require('./app')"
      pattern: "require.*\\./app"
    - from: "server/app.js"
      to: "server/routes/players.js"
      via: "app.use('/api/players', ...)"
      pattern: "/api/players"
    - from: "server/routes/players.js"
      to: "server/db/index.js"
      via: "require('../db/index')"
      pattern: "db\\.prepare"
    - from: "server/db/index.js"
      to: "server/db/schema.sql"
      via: "db.exec(schema)"
      pattern: "schema\\.sql"
---

<objective>
Build the Walking Skeleton for the Pegelköpp backend: the thinnest possible end-to-end working slice that proves the architecture. After this plan, `npm start` boots an Express server that serves a public TV placeholder page AND returns the seeded player list from SQLite as JSON.

Purpose: Establish the load-bearing axis (HTTP → Express → better-sqlite3 → JSON) and lock the project layout, database setup (WAL mode), and idempotent seeding before any feature work. Every subsequent plan in Phase 1 builds on this skeleton.

Output:
- Working Express 5 server with `npm start` / `npm run dev` / `npm test` scripts
- SQLite database at `data/kegelclub.db` with WAL mode, 5 tables (4 used + 1 reserved), 12 seeded players
- Public `GET /api/players` endpoint returning JSON
- Public `GET /tv` route serving placeholder HTML
- Test scaffolding (`node:test`) with first integration tests for DB + players route
- `SKELETON.md` already written (committed alongside this plan)
</objective>

## Phase Goal

**As a** Kegelclub-Mitglied am Spielabend, **I want to** the backend server alive with persistent member data, **so that** every subsequent feature (PIN-Login, Spiele, TV-Live-Anzeige) can be built on a verifiable foundation without re-scaffolding.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-PATTERNS.md
@.planning/phases/01-backend-foundation/01-VALIDATION.md
@.planning/phases/01-backend-foundation/SKELETON.md
@CLAUDE.md
@kegelclub_12.html

<interfaces>
<!-- Interfaces this plan ESTABLISHES — downstream plans (02, 03, 04) consume them. -->

From server/db/index.js (created here):
```javascript
// Default export: a configured better-sqlite3 Database instance.
// WAL mode + busy_timeout(5000) + foreign_keys(ON) are already set.
// Schema (server/db/schema.sql) has been executed.
// Usage: const db = require('./db'); db.prepare('SELECT ...').all();
module.exports = db;  // better-sqlite3 Database
```

From server/db/seed.js (created here):
```javascript
// Idempotent seed — does nothing if players table is non-empty.
// Called once from server.js after db init.
module.exports = function seed(db) { /* ... */ };
```

From server/routes/players.js (created here, GET-only in this plan):
```javascript
// Express Router. In Plan 01, only GET /api/players exists.
// Plan 02 will add POST/PUT/PUT-archive with requireSession middleware.
module.exports = router;
```

Player row shape stored in DB:
{
  id: number (autoincrement),
  name: string,
  emoji: string (default '🎳'),
  archived: 0 | 1 (default 0),
  created_at: string (ISO datetime from SQLite datetime('now'))
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Node project, install dependencies, write .env.example, .gitignore, package.json scripts</name>
  <files>package.json, .gitignore, .env.example</files>
  <read_first>
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-RESEARCH.md (Standard Stack section — exact versions)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-CONTEXT.md (D2 — .env structure)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/SKELETON.md (Locked Architecture)
    - C:/Users/tobia/Claude/CLAUDE.md (project-level conventions)
  </read_first>
  <action>
    Initialize a new Node.js project at the repo root (C:/Users/tobia/Claude/). Do NOT use TypeScript — pure CommonJS JavaScript only.

    1. Create package.json with:
       - "name": "pegelkoepp"
       - "version": "0.1.0"
       - "description": "Pegelköpp Kegelclub App — Backend"
       - "main": "server/server.js"
       - "engines": { "node": ">=22" }
       - "scripts":
         - "start": "node server/server.js"
         - "dev": "nodemon server/server.js"
         - "test": "node --test"
         - "test:games": "node --test server/game-types/*.test.js"
         - "seed": "node -e \"require('./server/db/seed')(require('./server/db'))\""

    2. Install runtime dependencies (use exact versions verified in RESEARCH.md):
       `npm install express@5.2.1 better-sqlite3@12.10.0 express-session@1.19.0 connect-sqlite3@0.9.16 bcryptjs@3.0.3 dotenv@17.4.2 helmet@8.1.0 morgan@1.10.1 cors@2.8.6`

    3. Install dev dependency:
       `npm install -D nodemon@3.1.14`

    4. Do NOT install `express-async-errors` — Express 5 forwards async errors natively (RESEARCH.md State of the Art).

    5. Create .gitignore at repo root with:
       - node_modules/
       - data/                (SQLite files)
       - .env                 (secrets)
       - npm-debug.log*
       - .DS_Store

    6. Create .env.example at repo root with placeholder values (per D2):
       ```
       # PIN hash — generate via: node -e "require('bcryptjs').hash('your-pin-here',10).then(console.log)"
       PIN_HASH=$2a$10$REPLACE_ME_WITH_BCRYPT_HASH_OF_YOUR_PIN

       # Session signing secret — generate via: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
       SESSION_SECRET=REPLACE_ME_WITH_32_BYTE_HEX_STRING

       # HTTP port (default 3000)
       PORT=3000

       # Set to 'production' on the VPS; 'development' locally
       NODE_ENV=development

       # CORS origin — leave empty for same-origin (recommended)
       CORS_ORIGIN=
       ```
       Do NOT create .env (only .env.example).

    7. Verify install succeeded: `npm ls express better-sqlite3` should report no missing/invalid.

    If `npm install better-sqlite3` fails on Windows because of missing C++ build tools, surface the exact error and STOP — do not silently retry. The user must install Visual C++ Build Tools (https://visualstudio.microsoft.com/visual-cpp-build-tools/) before continuing.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && test -f package.json && test -f .gitignore && test -f .env.example && test -d node_modules/better-sqlite3 && test -d node_modules/express && node -e "const pkg = require('./package.json'); if (pkg.dependencies['express'] !== '5.2.1') process.exit(1); if (pkg.dependencies['better-sqlite3'] !== '12.10.0') process.exit(1); if (!pkg.scripts.start || !pkg.scripts.test) process.exit(1);"</automated>
  </verify>
  <done>
    package.json has correct dependencies (express@5.2.1, better-sqlite3@12.10.0, etc.) and scripts (start, dev, test, test:games, seed); .gitignore excludes node_modules/, data/, .env; .env.example contains PIN_HASH=, SESSION_SECRET=, PORT=, NODE_ENV=; node_modules/ populated; no install errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build DB layer (schema.sql + db/index.js with WAL) and idempotent seed; write db.test.js covering WAL pragma + seed-once</name>
  <files>server/db/schema.sql, server/db/index.js, server/db/seed.js, server/db/db.test.js</files>
  <read_first>
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-CONTEXT.md (§ SQLite Schema lines 89-135; D5 schema strategy; C5 WAL; C3 UNIQUE constraint)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-PATTERNS.md (`server/db/index.js`, `server/db/schema.sql`, `server/db/seed.js` sections including Seed Guard Pattern)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-RESEARCH.md (Pattern 1 SQLite Setup; Pitfalls 1, 6, 7)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-VALIDATION.md (Wave 0 — db.test.js required)
  </read_first>
  <behavior>
    - Test 1: Opening the DB sets journal_mode to 'wal' (case-insensitive); `db.pragma('journal_mode', { simple: true })` returns 'wal'.
    - Test 2: Opening the DB sets `foreign_keys = ON`; `db.pragma('foreign_keys', { simple: true })` returns 1.
    - Test 3: All 5 tables exist after init: players, games, game_players, throws, game_type_defs (query sqlite_master).
    - Test 4: The throws table has the UNIQUE constraint on (game_id, player_id, throw_index): inserting two rows with the same triple throws an error containing 'UNIQUE'.
    - Test 5: `seed(db)` inserts exactly 12 players on first call when the table is empty; the count after seeding equals 12.
    - Test 6: Calling `seed(db)` again does NOT insert duplicates; count remains 12 after the second call.
    - Test 7: All seeded players have non-empty `name` and `emoji` and `archived = 0`.
    - Test 8: Tests use an isolated DB path (e.g., a tmp file via `fs.mkdtempSync`) and clean up afterwards — they MUST NOT touch `data/kegelclub.db`.
  </behavior>
  <action>
    Write tests FIRST, then schema + db/index.js + seed.js until tests pass.

    1. Create server/db/schema.sql with the exact 5-table DDL from CONTEXT.md lines 89-135 (players, games, game_players, throws, game_type_defs). Verbatim — including the UNIQUE constraint on throws and the default `(datetime('now'))` for created_at.

    2. Create server/db/index.js (per PATTERNS.md `server/db/index.js`):
       - Require better-sqlite3, path, fs
       - DB_PATH = process.env.DB_PATH || path.join(__dirname, '../../data/kegelclub.db')
       - `fs.mkdirSync(path.dirname(DB_PATH), { recursive: true })` BEFORE `new Database()` (RESEARCH.md Open Question 3)
       - Open `new Database(DB_PATH)`
       - **First three operations** (order matters per CONTEXT.md C5 + RESEARCH.md Pitfalls 1, 6):
         - `db.pragma('journal_mode = WAL')`
         - `db.pragma('busy_timeout = 5000')`
         - `db.pragma('foreign_keys = ON')`
       - Read schema.sql and `db.exec(schema)`
       - `module.exports = db`

    3. Create server/db/seed.js exporting `function seed(db)`:
       - Query `SELECT COUNT(*) AS n FROM players` — return early if n > 0 (Seed Guard Pattern; Pitfall 7)
       - Prepare `INSERT INTO players (name, emoji) VALUES (?, ?)`
       - Wrap in `db.transaction()`
       - Insert exactly 12 placeholder Pegelköpp members with the comment `// TODO: replace with actual Pegelköpp member names and emojis (Pegelköpp #2)` immediately above the array (RESEARCH.md Assumption A3). Suggested placeholder names + emojis:
         ```
         { name: 'Anna',   emoji: '🌟' },
         { name: 'Ben',    emoji: '🔥' },
         { name: 'Clara',  emoji: '🎯' },
         { name: 'David',  emoji: '⚡' },
         { name: 'Eva',    emoji: '🌙' },
         { name: 'Felix',  emoji: '🦊' },
         { name: 'Greta',  emoji: '🌻' },
         { name: 'Henrik', emoji: '🐺' },
         { name: 'Ida',    emoji: '🌈' },
         { name: 'Jonas',  emoji: '🚀' },
         { name: 'Klara',  emoji: '🎵' },
         { name: 'Lukas',  emoji: '🎳' }
         ```
       - `module.exports = seed`

    4. Create server/db/db.test.js using `node:test` and `node:assert/strict`. To avoid touching the real DB, the test must:
       - Set `process.env.DB_PATH` to a fresh tmp file path created with `fs.mkdtempSync(os.tmpdir() + path.sep + 'pegel-test-')` BEFORE requiring `../db/index`
       - Use `delete require.cache[require.resolve(...)]` so each test gets a fresh DB instance, OR open a sibling DB by requiring `better-sqlite3` directly inside tests and re-running the same setup logic
       - Implement all 8 tests from <behavior> above
       - Clean up the tmp dir in a `t.after(...)` or `process.on('exit', ...)` hook

    5. Run `node --test server/db/db.test.js` — all tests must pass.

    Do NOT add a versioned migration tool (knex, db-migrate). D5 forbids it for Phase 1; deferred to Phase 5.

    Do NOT add any code that runs `seed()` at import time. `seed()` is called explicitly from server.js (next task).
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/db/db.test.js</automated>
  </verify>
  <done>
    schema.sql contains all 5 CREATE TABLE statements with the UNIQUE constraint on throws; db/index.js sets WAL + busy_timeout + foreign_keys pragmas as first ops; seed.js is idempotent (guarded by COUNT check) and inserts 12 placeholders in a transaction; all 8 unit tests in db.test.js pass with `node --test`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Express app + server.js + GET /api/players + static /tv route; write players.test.js for the GET endpoint and TV route</name>
  <files>server/app.js, server/server.js, server/routes/players.js, server/routes/players.test.js, public/tv.html, public/index.html</files>
  <read_first>
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-PATTERNS.md (`server/app.js`, `server/server.js`, `server/routes/players.js` sections — note Plan 01 only implements GET /api/players, not POST/PUT)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-RESEARCH.md (Pattern 2 session setup, Pattern 6 error middleware, REST API Design table — Auth + Static + Players routes)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-CONTEXT.md (Auth Boundary section: `/tv` unauthenticated)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/SKELETON.md (Auth Boundary, Locked Architecture)
    - C:/Users/tobia/Claude/.planning/phases/01-backend-foundation/01-VALIDATION.md (Wave 0 — players.test.js + tv.test.js)
    - C:/Users/tobia/Claude/server/db/index.js (created in Task 2 — module shape)
    - C:/Users/tobia/Claude/server/db/seed.js (created in Task 2)
    - C:/Users/tobia/Claude/.env.example (created in Task 1)
  </read_first>
  <behavior>
    - Test 1: GET /api/players returns HTTP 200 and a Content-Type containing 'application/json'.
    - Test 2: GET /api/players returns a JSON array of length 12 after seeding; each item has shape { id: number, name: string, emoji: string }.
    - Test 3: GET /api/players does NOT include the `archived` column or `created_at` in the response (only id, name, emoji).
    - Test 4: GET /api/players omits players whose archived = 1 (insert a row with archived=1 directly in test setup and assert it's not in the response).
    - Test 5: GET /tv returns HTTP 200, Content-Type containing 'text/html', and the response body contains the literal string 'Pegelköpp'.
    - Test 6: GET /tv works WITHOUT any Cookie header — no 401, no redirect.
    - Test 7: GET /api/players ordering is by `id ASC` (insert players in non-id order in test, assert response order).
    - Tests boot the app via `require('../app')` (not the live server) and exercise it with the built-in `node:test` + `fetch`-style requests using a freshly listened HTTP server inside the test (e.g., `const server = app.listen(0); const { port } = server.address(); fetch(`http://127.0.0.1:${port}/api/players`)`). Tests must close the server in `t.after()`.
    - Tests use an isolated tmp DB path via `process.env.DB_PATH` (same approach as Task 2).
  </behavior>
  <action>
    Write tests FIRST. The test file requires `../app` and exercises both routes against an ephemeral HTTP listener.

    1. Create server/routes/players.js with **only** GET /api/players in this plan (POST/PUT/archive come in Plan 02):
       ```
       const { Router } = require('express');
       const db = require('../db');
       const router = Router();

       router.get('/', (req, res) => {
         const players = db.prepare(
           'SELECT id, name, emoji FROM players WHERE archived = 0 ORDER BY id ASC'
         ).all();
         res.json(players);
       });

       module.exports = router;
       ```
       The SELECT MUST be exactly `SELECT id, name, emoji FROM players WHERE archived = 0 ORDER BY id ASC` so test 3 (no archived/created_at in response) and test 7 (id ordering) pass deterministically.

    2. Create server/app.js per PATTERNS.md `server/app.js`:
       - Require express, session, connect-sqlite3, helmet, morgan, cors, path, dotenv
       - Call `require('dotenv').config()` at the top (no-op if .env missing; tests don't need it)
       - Mount middleware in order: `helmet()`, `morgan('dev')`, `cors({ origin: process.env.CORS_ORIGIN || false, credentials: true })`, `express.json()`, `express.urlencoded({ extended: false })`
       - Mount `express-session` with `connect-sqlite3` store: `db: 'sessions.db'`, `dir: './data'`, secret from `process.env.SESSION_SECRET || 'dev-only-secret-replace-me'` (the fallback exists ONLY so the dev server starts without .env; production must provide one), cookie flags httpOnly + sameSite:'strict' + secure when NODE_ENV='production' + maxAge 7 days
       - Route mounting (order matters — public routes before any future auth gates):
         - `app.get('/tv', (req, res) => res.sendFile(path.join(__dirname, '../public/tv.html')))`
         - `app.use(express.static(path.join(__dirname, '../public')))`
         - `app.use('/api/players', require('./routes/players'))`
       - Error middleware LAST:
         ```
         app.use((err, req, res, next) => {
           console.error(err.stack);
           res.status(err.status || 500).json({ error: err.message || 'Internal server error' });
         });
         ```
       - `module.exports = app`

       Do NOT install or import `express-async-errors` — Express 5 forwards async errors natively (RESEARCH.md State of the Art + Pitfall 5).

    3. Create server/server.js per PATTERNS.md `server/server.js`:
       ```
       require('dotenv').config();
       const http = require('http');
       const app = require('./app');
       const db = require('./db');
       const seed = require('./db/seed');

       seed(db);

       const PORT = process.env.PORT || 3000;
       const server = http.createServer(app);
       server.listen(PORT, () => {
         console.log(`Pegelköpp server listening on port ${PORT}`);
       });
       ```

    4. Create public/tv.html — minimal placeholder visible at 5m. Required body content (test 5 checks for 'Pegelköpp'):
       ```html
       <!doctype html>
       <html lang="de">
       <head>
         <meta charset="utf-8">
         <title>Pegelköpp — TV</title>
         <style>
           html, body { margin: 0; padding: 0; background: #111; color: #fff;
             font-family: system-ui, sans-serif; height: 100vh; }
           main { display: flex; align-items: center; justify-content: center;
             height: 100vh; font-size: 8vw; }
         </style>
       </head>
       <body>
         <main>Pegelköpp</main>
       </body>
       </html>
       ```

    5. Create public/index.html — minimal placeholder (Phase 3 wires the actual frontend):
       ```html
       <!doctype html>
       <html lang="de">
       <head><meta charset="utf-8"><title>Pegelköpp</title></head>
       <body><h1>Pegelköpp</h1><p>Backend läuft. UI folgt in Phase 3.</p></body>
       </html>
       ```

    6. Create server/routes/players.test.js implementing all 7 tests from <behavior>. Setup pattern:
       - Set `process.env.DB_PATH` to a tmp file BEFORE `require('../app')`
       - Set `process.env.SESSION_SECRET = 'test-secret-do-not-use-in-prod'` BEFORE require
       - After `require('../app')`, also require `../db/seed` and call `seed(db)` to populate the 12 players
       - For test 4, manually INSERT a row with archived=1 via `db.prepare(...).run(...)` and assert it does not appear in the GET response
       - Use `app.listen(0)` to get an ephemeral port; close in `t.after(...)`

    7. Manually verify (NOT a test): run `npm start` once, then `curl http://localhost:3000/api/players` → JSON array, and `curl http://localhost:3000/tv` → HTML containing 'Pegelköpp'. Stop the server (Ctrl+C). Confirm `data/kegelclub.db`, `data/kegelclub.db-wal`, `data/kegelclub.db-shm` files exist on disk.

    Do NOT add any auth middleware in this plan — Plan 02 adds it. The /api/players GET endpoint stays public per the REST API Design table.
  </action>
  <verify>
    <automated>cd C:/Users/tobia/Claude && node --test server/routes/players.test.js</automated>
  </verify>
  <done>
    GET /api/players returns a 200 JSON array of 12 seeded players (id, name, emoji only); archived players are filtered out; ordering by id ASC; GET /tv returns 200 HTML containing 'Pegelköpp' without any auth; all 7 tests in players.test.js pass; `npm start` boots the server and both endpoints respond.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — Walking Skeleton end-to-end smoke test</name>
  <what-built>
    A working Node.js/Express 5 server with SQLite (WAL mode), 12 seeded players, public GET /api/players endpoint, and public /tv placeholder page. This is the foundation for Plans 02-04 of Phase 1 and all of Phase 2.
  </what-built>
  <how-to-verify>
    Open a terminal in C:/Users/tobia/Claude/ and do EACH step in order. If ANY step fails, type the failure description instead of 'approved'.

    1. **Clean boot test:**
       - Delete the `data/` directory if it exists: `rm -rf data` (or PowerShell `Remove-Item -Recurse -Force data`)
       - Run `npm start`
       - **Expected:** Console prints `Pegelköpp server listening on port 3000`, no errors, no crashes.

    2. **Player API test:**
       - In a second terminal, run `curl http://localhost:3000/api/players`
       - **Expected:** JSON array of 12 objects, each with `{id, name, emoji}` — for example `[{"id":1,"name":"Anna","emoji":"🌟"}, ...]`. Response is pretty-printable JSON.

    3. **TV route test (unauthenticated):**
       - Open `http://localhost:3000/tv` in a browser
       - **Expected:** Black page with the white text "Pegelköpp" visible, large (8vw). NO login prompt, NO 401 error.

    4. **WAL mode verification:**
       - With the server still running, check the `data/` directory: `ls data/`
       - **Expected:** Three files exist: `kegelclub.db`, `kegelclub.db-shm`, `kegelclub.db-wal`. The presence of `-shm` and `-wal` confirms WAL mode is active.

    5. **Idempotent seed test:**
       - Stop the server (Ctrl+C).
       - Run `npm start` again. Wait for the listen log.
       - In the other terminal, run `curl http://localhost:3000/api/players` again.
       - **Expected:** Still exactly 12 players (no duplicates from a second seed run).

    6. **Test suite:**
       - Stop the server (Ctrl+C).
       - Run `npm test`.
       - **Expected:** All tests in `server/db/db.test.js` and `server/routes/players.test.js` pass (15 tests total: 8 db + 7 routes). No failing assertions.

    7. **No-secrets-in-git check:**
       - Run `git status` (if the repo is initialized — if not, skip this step).
       - **Expected:** `.env.example` may appear (committed); `.env` and `data/` must NOT appear (gitignored). If git is not initialized yet, manually confirm `.env.example` exists at repo root and `.env` does NOT exist.
  </how-to-verify>
  <resume-signal>Type 'approved' to continue to Plan 02 (PIN auth + player CRUD), or describe the specific failure (which step, what was expected vs. what happened) for fixup.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Express | Untrusted HTTP requests (tablet, phone, TV browser) |
| Express → SQLite (better-sqlite3) | App writes; must be prepared statements only |
| Filesystem → process | `.env` loaded once at boot via dotenv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` file (PIN_HASH, SESSION_SECRET) | mitigate | `.env` in `.gitignore`; `.env.example` committed with placeholders only; verified in Task 4 step 7 |
| T-01-02 | Tampering | SQL queries in `routes/players.js` | mitigate | Only `db.prepare(...).all()` with no string interpolation in this plan; pattern locked for future routes |
| T-01-03 | Denial of Service | Concurrent SQLite writes during throw bursts | mitigate | `journal_mode = WAL` + `busy_timeout = 5000` set as the first DB pragmas (Task 2); verified by db.test.js test 1 |
| T-01-04 | Tampering | Missing security HTTP headers | mitigate | `helmet()` is the first middleware mounted in app.js (Task 3); covers CSP, HSTS, X-Frame-Options |
| T-01-05 | Information Disclosure | Error stack traces leaking internals | mitigate | Express 5 error middleware returns `{error: err.message}` only; full stack logged to stderr, not response (Task 3) |
| T-01-06 | Spoofing | Session cookie without secure flags | mitigate | express-session configured with httpOnly + sameSite:strict + secure-in-prod + 7-day maxAge (Task 3) — even though no protected routes exist yet, the cookie infrastructure is locked here |
| T-01-SC | Tampering | npm package supply chain | mitigate | All 10 runtime + 1 dev package [VERIFIED: npm registry] in RESEARCH.md Package Legitimacy Audit (10+ years old, official GitHub repos, no postinstall scripts). No [ASSUMED] or [SUS] packages — no blocking-human checkpoint needed for installs. |
</threat_model>

<verification>
- DB layer: `node --test server/db/db.test.js` — 8 tests green (WAL pragma, foreign_keys, 5 tables, UNIQUE constraint, seed-once, seed shape, isolated DB)
- Routes layer: `node --test server/routes/players.test.js` — 7 tests green (GET shape, archived filter, TV unauth, id ordering)
- Full suite: `node --test` — 15 tests green
- Manual smoke (Task 4 checkpoint): `npm start` → both endpoints reachable; WAL files present on disk; seed idempotent across restarts
- Security: `.env.example` committed; `.env` and `data/` gitignored; helmet headers active; no string-interpolated SQL anywhere in server/
</verification>

<success_criteria>
- [ ] `npm start` boots Express on PORT (default 3000) with no errors
- [ ] `GET /api/players` → 200 JSON array of 12 seeded players (id, name, emoji)
- [ ] `GET /tv` → 200 HTML containing "Pegelköpp"; no auth required
- [ ] `data/kegelclub.db`, `data/kegelclub.db-wal`, `data/kegelclub.db-shm` exist after first run
- [ ] Second `npm start` does NOT re-seed (still exactly 12 players)
- [ ] `npm test` → all 15 tests pass
- [ ] `.env` does NOT exist (only `.env.example`)
- [ ] No `express-async-errors` in node_modules (Express 5 native async handling)
- [ ] All locked decisions implemented: D1 (seed script), D2 (.env), D3 (folder layout exact), D5 (schema.sql idempotent); C2 (DB-first writes — pattern established), C3 (UNIQUE constraint in schema), C5 (WAL pragmas as first DB ops)
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation/01-01-SUMMARY.md` when done. Summary must include:
- Files created with line counts
- Test results (15 tests green)
- Manual smoke output from Task 4
- Any deviations from CONTEXT.md / RESEARCH.md (should be zero)
- Notes for Plans 02-04 (e.g., "auth.js will mount BEFORE players write routes — public GET is already wired")
</output>
