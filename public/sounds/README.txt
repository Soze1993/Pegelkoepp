Drop MP3 files here:

  kda-end.mp3   — played when Kegler des Abends finishes (index + TV screen)
  bk-end.mp3    — played when Bilderkegel finishes (index only)

The files are served at /sounds/<filename> from the Express static middleware.
Any standard MP3 works. Keep files small (< 5 MB) for fast load on game end.
